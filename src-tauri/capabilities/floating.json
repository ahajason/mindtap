{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "floating",
  "description": "Capability for the floating window",
  "windows": ["floating"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}