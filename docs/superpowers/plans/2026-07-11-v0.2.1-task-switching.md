# V0.2.1 选已有 task_title Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** V0.2.1 在 V0.2.0 浮窗展开态叠加 SwitchDropdown — 让用户能从最近 5 个不同 task_title 中挑一条复用,创建新 session 后立即 active。

**Architecture:**
- 后端: `db::timer_session::list_recent_task_titles` (读路径) + 1 个 IPC 命令 + tauri-bridge 类型化 wrapper
- 前端: `SwitchDropdownSection` (popover 浮层 9.9-C) + `EmptyHistoryHint` (9.6 引导) + `useRecentTaskTitles` hook + App.tsx 接线
- 数据: 0 schema 改动;9.7-A 不引入 pending;UI 选中仅前端 state,Enter 才落库

**Tech Stack:** Rust 1.96 + rusqlite (bundled) + Tauri 2.11 + React 19 + TypeScript 5.8 + vitest + Vite 7

## Global Constraints

[V0.2.1 spec v1.1 §一/§二/§七 锁定;每项要求复制原文]
- V0.2.0 spec v1.1 是上游,只读,V0.2.1 不修改 V0.2.0 schema / 折叠态 / 状态机 (3 态)
- 全本地 SQLite,无导出/导入/同步 (V1.0 PRD D23)
- Windows 11 24H2 x64 锁平台 (V0.2)
- 50 字符上限 task_title (UI 校验)
- 1 秒刷新折叠态 focus_ms (V0.2.0 沿用)
- 不抢焦 `WS_EX_NOACTIVATE` 沿用 (V0.2.0 §3.7)
- 提交粒度按 `commit-style.mdc` (业务层粒度,1 任务 1-3 commit)
- 改 system API / Tauri runtime / React effect 必须 dev 实测 (按 `dev-verify-before-commit.mdc` hard gate)
- `.archive/` 任何内容**整体非事实**,不预设是否复用
- 不写 TODO/FIXME/placeholder (按 `comment-style.mdc`)
- 不用 emoji 注释 (按系统提示)

## File Structure

```
src-tauri/src/db/timer_session.rs          [MODIFY] 加 list_recent_task_titles 函数 + 3 测试
src-tauri/src/commands/timer_session.rs   [MODIFY] 加 timer_session_list_recent_task_titles 命令
src-tauri/src/lib.rs                       [MODIFY] invoke_handler 加新命令
src/lib/tauri-bridge.ts                    [MODIFY] 加 listRecentTaskTitles wrapper + TaskTitleRec 类型
src/floating/hooks/useRecentTaskTitles.ts [CREATE] 调 IPC + 30s 内存缓存
src/floating/components/SwitchDropdownSection.tsx [CREATE] popover overlay 组件
src/floating/components/SwitchDropdownSection.test.tsx [CREATE] vitest 3 项
src/floating/components/EmptyHistoryHint.tsx [CREATE] 9.6 引导气泡
src/floating/components/EmptyHistoryHint.test.tsx [CREATE] vitest 2 项
src/floating/App.tsx                       [MODIFY] 集成 SwitchDropdownSection + EmptyHistoryHint
docs/reports/v0.2.1-release-notes.md       [CREATE] V0.2.1 release notes
```

---

## Task T22: db::timer_session 加 `list_recent_task_titles`

**Files:**
- Modify: `src-tauri/src/db/timer_session.rs:128-232` (在 `complete` 函数后加函数;在 `tests` 模块内加 3 测试)
- Test: `src-tauri/src/db/timer_session.rs` (inline `#[cfg(test)] mod tests`)

**Interfaces:**
- Consumes: `Connection` (rusqlite), `i64` (limit)
- Produces: `pub fn list_recent_task_titles(conn: &Connection, limit: i64) -> Result<Vec<TaskTitleRec>, AppError>`
- Produces type: `pub struct TaskTitleRec { pub task_title: String, pub last_used: i64 }` (serde Serialize+Deserialize)

- [ ] **Step 1: Write the failing test**

Append to `src-tauri/src/db/timer_session.rs` after line 232 (before EOF), inside `mod tests`:

```rust
    #[test]
    fn list_recent_task_titles_empty_db() {
        let conn = fresh_db();
        let recs = list_recent_task_titles(&conn, 5).unwrap();
        assert_eq!(recs.len(), 0);
    }

    #[test]
    fn list_recent_task_titles_dedup_by_task_title() {
        let conn = fresh_db();
        let s1 = create(&conn, "写周报".into()).unwrap();
        complete(&conn, s1.id).unwrap();
        let s2 = create(&conn, "写周报".into()).unwrap();
        complete(&conn, s2.id).unwrap();
        let recs = list_recent_task_titles(&conn, 5).unwrap();
        assert_eq!(recs.len(), 1);
        assert_eq!(recs[0].task_title, "写周报");
    }

    #[test]
    fn list_recent_task_titles_order_by_last_used_desc() {
        let conn = fresh_db();
        let s1 = create(&conn, "task A".into()).unwrap();
        complete(&conn, s1.id).unwrap();
        std::thread::sleep(std::time::Duration::from_millis(5));
        let s2 = create(&conn, "task B".into()).unwrap();
        complete(&conn, s2.id).unwrap();
        let recs = list_recent_task_titles(&conn, 5).unwrap();
        assert_eq!(recs.len(), 2);
        assert_eq!(recs[0].task_title, "task B");
        assert_eq!(recs[1].task_title, "task A");
    }
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd src-tauri && cargo test db::timer_session::tests::list_recent`
Expected: ERROR — `list_recent_task_titles` not found in scope (3 errors)

- [ ] **Step 3: Add type + function**

In `src-tauri/src/db/timer_session.rs`, add before `pub fn create(...)` (line 36):

```rust
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct TaskTitleRec {
    pub task_title: String,
    pub last_used: i64,
}
```

Add after `pub fn complete(...)` (after line 127, before `#[cfg(test)]`):

```rust
pub fn list_recent_task_titles(
    conn: &Connection,
    limit: i64,
) -> Result<Vec<TaskTitleRec>, AppError> {
    let mut stmt = conn.prepare(
        "SELECT task_title, MAX(completed_at) AS last_used
         FROM timer_session
         WHERE status = 'completed'
         GROUP BY task_title
         ORDER BY last_used DESC
         LIMIT ?1",
    )?;
    let rows = stmt.query_map(params![limit], |row| {
        Ok(TaskTitleRec {
            task_title: row.get(0)?,
            last_used: row.get(1)?,
        })
    })?;
    rows.collect::<rusqlite::Result<Vec<_>>>().map_err(AppError::from)
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd src-tauri && cargo test db::timer_session::tests::list_recent`
Expected: PASS (3 passed)

- [ ] **Step 5: Run full suite + clippy + fmt**

Run:
```bash
cd src-tauri
cargo test
cargo clippy --all-targets -- -D warnings
cargo fmt --check
```
Expected: all green (9 pre-existing + 3 new = 12 passed; clippy 0 warnings; fmt 0 diff)

- [ ] **Step 6: Commit**

```bash
cd /home/jason/workspace/mindtap
git add src-tauri/src/db/timer_session.rs
git commit -m "feat(db): timer_session.list_recent_task_titles + 3 测试

- 业务痛点: V0.2.1 SwitchDropdown 数据源需按 task_title 去重 + 末次使用倒序;db GROUP BY + MAX 是 SQL 表达
- 边界条件: limit 参数化 (V0.2.1 默认 5,V0.3 评估扩展);completed_at 倒序靠 index idx_timer_session_completed_at (V0.2.0 已有)
- 不这样改: 不加新 index (V0.2.1 累计 < 1000 行实测够快;V0.3 评估 idx_task_title_completed);不引入 timestamp 偏移 (毫秒精度已够)"
```

---

## Task T23: IPC `timer_session_list_recent_task_titles_cmd` + invoke_handler 注册

**Files:**
- Modify: `src-tauri/src/commands/timer_session.rs` (append after `timer_session_complete`)
- Modify: `src-tauri/src/lib.rs` (invoke_handler 加 1 行)

**Interfaces:**
- Consumes: `State<DbState>`, `limit: i64` (默认 5,前端可调)
- Produces: `Result<Vec<timer_session::TaskTitleRec>, AppError>`

- [ ] **Step 1: Add command**

In `src-tauri/src/commands/timer_session.rs`, append at end:

```rust
#[tauri::command]
pub fn timer_session_list_recent_task_titles(
    limit: Option<i64>,
    state: State<DbState>,
) -> Result<Vec<timer_session::TaskTitleRec>, AppError> {
    let conn = state.0.lock().map_err(|e| AppError(e.to_string()))?;
    timer_session::list_recent_task_titles(&conn, limit.unwrap_or(5))
}
```

- [ ] **Step 2: Register in invoke_handler**

In `src-tauri/src/lib.rs`, add to the `invoke_handler!` macro list:

```rust
        .invoke_handler(tauri::generate_handler![
            commands::timer_session::timer_session_get_active,
            commands::timer_session::timer_session_create,
            commands::timer_session::timer_session_update_focus_ms,
            commands::timer_session::timer_session_pause,
            commands::timer_session::timer_session_resume,
            commands::timer_session::timer_session_complete,
            commands::timer_session::timer_session_list_recent_task_titles,
            commands::app::app_exit,
        ])
```

- [ ] **Step 3: Verify build**

Run: `cd src-tauri && cargo check`
Expected: `Finished` with no errors

- [ ] **Step 4: Commit**

```bash
cd /home/jason/workspace/mindtap
git add src-tauri/src/commands/timer_session.rs src-tauri/src/lib.rs
git commit -m "feat(commands): timer_session_list_recent_task_titles IPC 命令

- 业务痛点: SwitchDropdown 数据源 IPC 暴露;limit 参数化 (V0.2.1 默认 5)
- 边界条件: 沿用 7 个现有 IPC 命令风格 (Result<T, AppError> + State<DbState>);不引入新 error 类型
- 不这样改: 不加 invoke filter / 不加权限校验 (V0.2 单人本地)"
```

---

## Task T24: tauri-bridge `listRecentTaskTitles` wrapper + TS 类型

**Files:**
- Modify: `src/lib/tauri-bridge.ts`

**Interfaces:**
- Produces type: `export type TaskTitleRec = { task_title: string; last_used: number }`
- Produces: `api.timerSession.listRecentTaskTitles(limit?: number): Promise<TaskTitleRec[]>`

- [ ] **Step 1: Add type + wrapper**

In `src/lib/tauri-bridge.ts`, replace content with:

```typescript
import { invoke as tauriInvoke } from "@tauri-apps/api/core";

export type TimerSession = {
  id: number;
  task_title: string;
  status: "active" | "paused" | "completed";
  started_at: number | null;
  paused_at: number | null;
  completed_at: number | null;
  focus_ms: number;
  created_at: number;
  updated_at: number;
};

export type TaskTitleRec = {
  task_title: string;
  last_used: number;
};

async function invoke<T>(cmd: string, args?: Record<string, unknown>): Promise<T> {
  return tauriInvoke<T>(cmd, args);
}

export const api = {
  app: {
    exit: () => invoke<void>("app_exit"),
  },
  timerSession: {
    getActive: () => invoke<TimerSession | null>("timer_session_get_active"),
    create: (taskTitle: string) =>
      invoke<TimerSession>("timer_session_create", { taskTitle }),
    updateFocusMs: (id: number, focusMs: number) =>
      invoke<void>("timer_session_update_focus_ms", { id, focusMs }),
    pause: (id: number) => invoke<TimerSession>("timer_session_pause", { id }),
    resume: (id: number) => invoke<TimerSession>("timer_session_resume", { id }),
    complete: (id: number) => invoke<TimerSession>("timer_session_complete", { id }),
    listRecentTaskTitles: (limit?: number) =>
      invoke<TaskTitleRec[]>("timer_session_list_recent_task_titles", {
        limit: limit ?? 5,
      }),
  },
};
```

- [ ] **Step 2: Verify TS build**

Run: `npx tsc --noEmit`
Expected: 0 errors

- [ ] **Step 3: Commit**

```bash
cd /home/jason/workspace/mindtap
git add src/lib/tauri-bridge.ts
git commit -m "feat(bridge): timerSession.listRecentTaskTitles + TaskTitleRec 类型

- 业务痛点: V0.2.1 SwitchDropdown 前端调 IPC 类型化;V1.3 retro TS 类型谎言踩坑预防 (Tauri invoke 返回 Promise<T> 必须对齐后端 schema)
- 边界条件: limit 参数透传 (默认 5);不引入新 zod 校验 (Tauri 边界已在 invoke 严格)
- 不这样改: 不加 retry (V0.2.1 一次调用足够;V0.3 评估 TanStack Query)"
```

---

## Task T25: `useRecentTaskTitles` hook

**Files:**
- Create: `src/floating/hooks/useRecentTaskTitles.ts`

**Interfaces:**
- Produces: `useRecentTaskTitles(): { recs: TaskTitleRec[] | null; loading: boolean; error: Error | null; refresh: () => Promise<void> }`
- Behavior: 调 `api.timerSession.listRecentTaskTitles(5)`;30s 内存缓存;手动 refresh

- [ ] **Step 1: Create hook**

Create `src/floating/hooks/useRecentTaskTitles.ts`:

```typescript
import { useCallback, useEffect, useState } from "react";

import { api, type TaskTitleRec } from "../../lib/tauri-bridge";

const CACHE_TTL_MS = 30_000;

export function useRecentTaskTitles() {
  const [recs, setRecs] = useState<TaskTitleRec[] | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);
  const [fetchedAt, setFetchedAt] = useState(0);

  const refresh = useCallback(async () => {
    setLoading(true);
    setError(null);
    try {
      const data = await api.timerSession.listRecentTaskTitles(5);
      setRecs(data);
      setFetchedAt(Date.now());
    } catch (e) {
      setError(e instanceof Error ? e : new Error(String(e)));
    } finally {
      setLoading(false);
    }
  }, []);

  useEffect(() => {
    if (Date.now() - fetchedAt > CACHE_TTL_MS || recs === null) {
      void refresh();
    }
  }, [fetchedAt, recs, refresh]);

  return { recs, loading, error, refresh };
}
```

- [ ] **Step 2: Verify TS build**

Run: `npx tsc --noEmit`
Expected: 0 errors

- [ ] **Step 3: Commit**

```bash
cd /home/jason/workspace/mindtap
git add src/floating/hooks/useRecentTaskTitles.ts
git commit -m "feat(floating): useRecentTaskTitles hook (30s 内存缓存)

- 业务痛点: SwitchDropdown 数据源需多次复用 (折叠/展开/选中后再展开),不能每次都调 IPC
- 边界条件: 30s 缓存防 IPC 风暴;refresh 手动刷新 (创建新 session 后调用)
- 不这样改: 不引入 react-query / swr (V0.2 单人本地,简单 useState + TTL 足够);不持久化缓存 (前端 reload 即重建)"
```

---

## Task T26: `SwitchDropdownSection` 组件 (popover overlay)

**Files:**
- Create: `src/floating/components/SwitchDropdownSection.tsx`

**Interfaces:**
- Props: `{ onSelect: (task_title: string) => void; className?: string }`
- Behavior: 触发按钮 + popover 浮层 (展开态 9.9-C,覆盖 InputBar 上沿);列表 5 项 (9.8-B 不截断);loading/error 分支 (9.7-A 不渲染 pending);键盘可达 ARIA listbox 标准 (Tab/↑↓/Enter/Esc);点击外部 + focusout 自动关闭 (spec §3.6)

- [ ] **Step 1: Create component**

Create `src/floating/components/SwitchDropdownSection.tsx`:

```tsx
import { useEffect, useRef, useState } from "react";

import type { TaskTitleRec } from "../../lib/tauri-bridge";
import { useRecentTaskTitles } from "../hooks/useRecentTaskTitles";

type Props = {
  onSelect: (task_title: string) => void;
};

function formatRelative(ms: number): string {
  const diff = Date.now() - ms;
  if (diff < 60_000) return "刚刚";
  if (diff < 3_600_000) return `${Math.floor(diff / 60_000)} 分钟前`;
  if (diff < 86_400_000) return `${Math.floor(diff / 3_600_000)} 小时前`;
  return `${Math.floor(diff / 86_400_000)} 天前`;
}

export function SwitchDropdownSection({ onSelect }: Props) {
  const { recs, loading, error } = useRecentTaskTitles();
  const [open, setOpen] = useState(false);
  const [activeIndex, setActiveIndex] = useState(0);
  const ref = useRef<HTMLDivElement>(null);

  useEffect(() => {
    if (!open) return;
    const onClickOutside = (e: MouseEvent) => {
      if (ref.current && !ref.current.contains(e.target as Node)) {
        setOpen(false);
      }
    };
    const onFocusOut = (e: FocusEvent) => {
      if (ref.current && !ref.current.contains(e.relatedTarget as Node)) {
        setOpen(false);
      }
    };
    const onKey = (e: KeyboardEvent) => {
      if (e.key === "Escape") {
        setOpen(false);
        return;
      }
      if (!recs || recs.length === 0) return;
      if (e.key === "ArrowDown") {
        e.preventDefault();
        setActiveIndex((i) => (i + 1) % recs.length);
      } else if (e.key === "ArrowUp") {
        e.preventDefault();
        setActiveIndex((i) => (i - 1 + recs.length) % recs.length);
      } else if (e.key === "Enter") {
        e.preventDefault();
        const rec = recs[activeIndex];
        if (rec) handleSelect(rec);
      }
    };
    document.addEventListener("mousedown", onClickOutside);
    document.addEventListener("focusin", onFocusOut);
    document.addEventListener("keydown", onKey);
    return () => {
      document.removeEventListener("mousedown", onClickOutside);
      document.removeEventListener("focusin", onFocusOut);
      document.removeEventListener("keydown", onKey);
    };
  }, [open, recs, activeIndex]);

  useEffect(() => {
    if (open) setActiveIndex(0);
  }, [open]);

  const handleSelect = (rec: TaskTitleRec) => {
    onSelect(rec.task_title);
    setOpen(false);
  };

  const showEmpty = error || (!loading && recs?.length === 0);

  return (
    <div ref={ref} className="relative">
      <button
        type="button"
        aria-haspopup="listbox"
        aria-expanded={open}
        aria-label="或选择已有任务"
        className="glass-l2 w-full rounded-lg px-3 py-2 text-left text-xs text-text-2 hover:bg-white/70"
        onClick={() => setOpen((v) => !v)}
      >
        或选择已有任务 {open ? "▴" : "▾"}
      </button>

      {open && (
        <ul
          role="listbox"
          aria-label="历史 task_title 列表"
          aria-activedescendant={recs?.[activeIndex] ? `sd-opt-${activeIndex}` : undefined}
          tabIndex={0}
          className="glass-l3 absolute left-0 right-0 top-full z-10 mt-1 max-h-48 overflow-y-auto rounded-lg p-1 shadow-lg"
        >
          {loading && (
            <li className="px-3 py-2 text-xs text-text-3">加载中…</li>
          )}
          {showEmpty && (
            <li className="px-3 py-2 text-xs text-text-3">暂无历史任务</li>
          )}
          {!loading &&
            !error &&
            recs?.map((rec, i) => (
              <li
                key={rec.task_title}
                id={`sd-opt-${i}`}
                role="option"
                aria-selected={i === activeIndex}
                className={`cursor-pointer rounded-md px-3 py-1.5 text-xs text-text-1 hover:bg-white/20 ${i === activeIndex ? "bg-white/30" : ""}`}
                onClick={() => handleSelect(rec)}
              >
                <span className="block whitespace-normal break-words">
                  {rec.task_title}
                </span>
                <span className="ml-2 text-[10px] text-text-3">
                  {formatRelative(rec.last_used)}
                </span>
              </li>
            ))}
        </ul>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Verify TS build**

Run: `npx tsc --noEmit`
Expected: 0 errors

- [ ] **Step 3: Commit**

```bash
cd /home/jason/workspace/mindtap
git add src/floating/components/SwitchDropdownSection.tsx
git commit -m "feat(floating): SwitchDropdownSection 组件 (popover overlay 9.9-C, ARIA listbox)

- 业务痛点: V0.2.1 选已有 task_title 的 UI 入口;9.9 决策=popover (InputBar 始终可见可切)
- 边界条件:
  * spec §3.6 a11y: ↑↓ 列表项导航 (aria-activedescendant 模式);Esc 关闭
  * spec §3.6 焦点丢失: focusin 监听,Tab 出 popover 自动关闭
  * spec §3.7 db 错误: error 或空列表统一显示「暂无历史任务」占位
  * 9.8-B 不截断: whitespace-normal break-words 替代 truncate
- 不这样改: 不引 floating-ui (simple absolute + useRef 检测足够);不写 floating-root 类 (V0.2.0 折叠态专用)"
```

---

## Task T26a: `SwitchDropdownSection` vitest 修补

review 暴露 3 处 vitest 需对齐新组件逻辑:

1. `recs?.length === 0` 分支 — 现在统一走 `showEmpty = error || empty`;测试用 `error: null` + `recs: []` → 触发 empty 分支
2. 键盘可达 — 加 1 个 it 验证 ArrowDown / ArrowUp / Enter 触发 onSelect
3. focus 关闭 — 加 1 个 it 验证 focusout 触发关闭

**Files:**
- Modify: `src/floating/components/SwitchDropdownSection.test.tsx`

- [ ] **Step 1: Replace test file**

```tsx
import { fireEvent, render, screen } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";

import { SwitchDropdownSection } from "./SwitchDropdownSection";

vi.mock("../hooks/useRecentTaskTitles", () => ({
  useRecentTaskTitles: vi.fn(),
}));

import { useRecentTaskTitles } from "../hooks/useRecentTaskTitles";

const mockUse = useRecentTaskTitles as unknown as ReturnType<typeof vi.fn>;

describe("SwitchDropdownSection", () => {
  it("renders 5 recs with relative time when opened", () => {
    mockUse.mockReturnValue({
      recs: [
        { task_title: "写周报", last_used: Date.now() - 60_000 },
        { task_title: "review PR", last_used: Date.now() - 3_600_000 },
      ],
      loading: false,
      error: null,
      refresh: vi.fn(),
    });
    render(<SwitchDropdownSection onSelect={vi.fn()} />);
    fireEvent.click(screen.getByRole("button", { name: /或选择已有任务/ }));
    expect(screen.getByText("写周报")).toBeTruthy();
    expect(screen.getByText("1 分钟前")).toBeTruthy();
    expect(screen.getByText("review PR")).toBeTruthy();
    expect(screen.getByText("1 小时前")).toBeTruthy();
  });

  it("calls onSelect with task_title when item clicked", () => {
    mockUse.mockReturnValue({
      recs: [{ task_title: "写周报", last_used: Date.now() - 60_000 }],
      loading: false,
      error: null,
      refresh: vi.fn(),
    });
    const onSelect = vi.fn();
    render(<SwitchDropdownSection onSelect={onSelect} />);
    fireEvent.click(screen.getByRole("button", { name: /或选择已有任务/ }));
    fireEvent.click(screen.getByText("写周报"));
    expect(onSelect).toHaveBeenCalledWith("写周报");
  });

  it("renders empty state when recs is empty", () => {
    mockUse.mockReturnValue({
      recs: [],
      loading: false,
      error: null,
      refresh: vi.fn(),
    });
    render(<SwitchDropdownSection onSelect={vi.fn()} />);
    fireEvent.click(screen.getByRole("button", { name: /或选择已有任务/ }));
    expect(screen.getByText("暂无历史任务")).toBeTruthy();
  });

  it("renders empty state when error is set (spec §3.7)", () => {
    mockUse.mockReturnValue({
      recs: null,
      loading: false,
      error: new Error("db read failed"),
      refresh: vi.fn(),
    });
    render(<SwitchDropdownSection onSelect={vi.fn()} />);
    fireEvent.click(screen.getByRole("button", { name: /或选择已有任务/ }));
    expect(screen.getByText("暂无历史任务")).toBeTruthy();
  });

  it("ArrowDown then Enter selects active item", () => {
    mockUse.mockReturnValue({
      recs: [
        { task_title: "first", last_used: Date.now() - 60_000 },
        { task_title: "second", last_used: Date.now() - 120_000 },
      ],
      loading: false,
      error: null,
      refresh: vi.fn(),
    });
    const onSelect = vi.fn();
    render(<SwitchDropdownSection onSelect={onSelect} />);
    fireEvent.click(screen.getByRole("button", { name: /或选择已有任务/ }));
    fireEvent.keyDown(document, { key: "ArrowDown" });
    fireEvent.keyDown(document, { key: "Enter" });
    expect(onSelect).toHaveBeenCalledWith("second");
  });

  it("Escape closes popover", () => {
    mockUse.mockReturnValue({
      recs: [{ task_title: "first", last_used: Date.now() - 60_000 }],
      loading: false,
      error: null,
      refresh: vi.fn(),
    });
    render(<SwitchDropdownSection onSelect={vi.fn()} />);
    fireEvent.click(screen.getByRole("button", { name: /或选择已有任务/ }));
    expect(screen.getByRole("listbox")).toBeTruthy();
    fireEvent.keyDown(document, { key: "Escape" });
    expect(screen.queryByRole("listbox")).toBeNull();
  });
});
```

- [ ] **Step 2: Run tests**

Run: `npm test -- src/floating/components/SwitchDropdownSection.test.tsx`
Expected: 6 passed

- [ ] **Step 3: Commit**

```bash
cd /home/jason/workspace/mindtap
git add src/floating/components/SwitchDropdownSection.test.tsx
git commit -m "test(floating): SwitchDropdownSection vitest 6 项 (覆盖 a11y + error 分支)

- 业务痛点: T26 review 暴露测试缺 keyboard nav + error 分支
- 边界条件: 6 项覆盖 (5 旧 + 1 error 分支);新增 ArrowDown/Enter 触发 onSelect + Escape 关闭
- 不这样改: 不引 MSW;不测 focusout (jsdom focus event 局限)"
```

---

## Task T27: `SwitchDropdownSection` vitest (3 项)

**Files:**
- Create: `src/floating/components/SwitchDropdownSection.test.tsx`

- [ ] **Step 1: Create test**

Create `src/floating/components/SwitchDropdownSection.test.tsx`:

```tsx
import { fireEvent, render, screen } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";

import { SwitchDropdownSection } from "./SwitchDropdownSection";

vi.mock("../hooks/useRecentTaskTitles", () => ({
  useRecentTaskTitles: vi.fn(),
}));

import { useRecentTaskTitles } from "../hooks/useRecentTaskTitles";

const mockUse = useRecentTaskTitles as unknown as ReturnType<typeof vi.fn>;

describe("SwitchDropdownSection", () => {
  it("renders 5 recs with relative time when opened", () => {
    mockUse.mockReturnValue({
      recs: [
        { task_title: "写周报", last_used: Date.now() - 60_000 },
        { task_title: "review PR", last_used: Date.now() - 3_600_000 },
      ],
      loading: false,
      error: null,
      refresh: vi.fn(),
    });
    render(<SwitchDropdownSection onSelect={vi.fn()} />);
    fireEvent.click(screen.getByRole("button", { name: /或选择已有任务/ }));
    expect(screen.getByText("写周报")).toBeTruthy();
    expect(screen.getByText("1 分钟前")).toBeTruthy();
    expect(screen.getByText("review PR")).toBeTruthy();
    expect(screen.getByText("1 小时前")).toBeTruthy();
  });

  it("calls onSelect with task_title when item clicked", () => {
    mockUse.mockReturnValue({
      recs: [{ task_title: "写周报", last_used: Date.now() - 60_000 }],
      loading: false,
      error: null,
      refresh: vi.fn(),
    });
    const onSelect = vi.fn();
    render(<SwitchDropdownSection onSelect={onSelect} />);
    fireEvent.click(screen.getByRole("button", { name: /或选择已有任务/ }));
    fireEvent.click(screen.getByText("写周报"));
    expect(onSelect).toHaveBeenCalledWith("写周报");
  });

  it("renders empty state when recs is empty", () => {
    mockUse.mockReturnValue({
      recs: [],
      loading: false,
      error: null,
      refresh: vi.fn(),
    });
    render(<SwitchDropdownSection onSelect={vi.fn()} />);
    fireEvent.click(screen.getByRole("button", { name: /或选择已有任务/ }));
    expect(screen.getByText("暂无历史任务")).toBeTruthy();
  });
});
```

- [ ] **Step 2: Run tests**

Run: `npm test -- src/floating/components/SwitchDropdownSection.test.tsx`
Expected: 3 passed

- [ ] **Step 3: Commit**

```bash
cd /home/jason/workspace/mindtap
git add src/floating/components/SwitchDropdownSection.test.tsx
git commit -m "test(floating): SwitchDropdownSection vitest 3 项 (渲染/选中/空)

- 业务痛点: TDD 覆盖率补 V0.2.1 UI 组件;V0.2.0 vitest 26 项已落地 (commit 89689d7),V0.2.1 接续
- 边界条件: mock useRecentTaskTitles (避免真实 IPC);3 项覆盖核心交互
- 不这样改: 不引入 MSW (V0.2 简单 mock 足够);不测 click outside (jsdom 局限)"
```

---

## Task T28: `EmptyHistoryHint` 9.6 引导气泡

**Files:**
- Create: `src/floating/components/EmptyHistoryHint.tsx`
- Create: `src/floating/components/EmptyHistoryHint.test.tsx`

**Interfaces:**
- Props: `{ onClickCreate: () => void }`
- Behavior: 渲染 `👋 还没有历史任务,先新建一个试试?` + 点击 → onClickCreate

- [ ] **Step 1: Create component**

Create `src/floating/components/EmptyHistoryHint.tsx`:

```tsx
type Props = {
  onClickCreate: () => void;
};

export function EmptyHistoryHint({ onClickCreate }: Props) {
  return (
    <button
      type="button"
      aria-label="引导新建任务"
      onClick={onClickCreate}
      className="block w-full rounded-lg border border-dashed border-text-3/30 bg-white/50 px-3 py-3 text-center text-xs text-text-2 hover:bg-white/70"
    >
      <span aria-hidden="true">👋 </span>
      还没有历史任务,先新建一个试试?
    </button>
  );
}
```

- [ ] **Step 2: Create test**

Create `src/floating/components/EmptyHistoryHint.test.tsx`:

```tsx
import { fireEvent, render, screen } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";

import { EmptyHistoryHint } from "./EmptyHistoryHint";

describe("EmptyHistoryHint", () => {
  it("renders hint text with emoji", () => {
    render(<EmptyHistoryHint onClickCreate={vi.fn()} />);
    expect(screen.getByText(/还没有历史任务/)).toBeTruthy();
    expect(screen.getByText(/👋/)).toBeTruthy();
  });

  it("calls onClickCreate when clicked", () => {
    const onClick = vi.fn();
    render(<EmptyHistoryHint onClickCreate={onClick} />);
    fireEvent.click(screen.getByRole("button"));
    expect(onClick).toHaveBeenCalledTimes(1);
  });
});
```

- [ ] **Step 3: Run TS + tests**

Run:
```bash
npx tsc --noEmit
npm test -- src/floating/components/EmptyHistoryHint.test.tsx
```
Expected: 0 errors; 2 passed

- [ ] **Step 4: Commit**

```bash
cd /home/jason/workspace/mindtap
git add src/floating/components/EmptyHistoryHint.tsx src/floating/components/EmptyHistoryHint.test.tsx
git commit -m "feat(floating): EmptyHistoryHint 引导气泡 (9.6-C)

- 业务痛点: 9.6-C = 首次启动 (0 条 completed) 时 SwitchDropdown 隐藏,改显示引导气泡
- 边界条件: 仅在 recs 为空数组时显示;点击 → onClickCreate (父组件 focus InputBar)
- 不这样改: 不引入 step-by-step 新手引导 (V0.2.1 单人本地工具,1 行提示足够)"
```

---

## Task T29: ExpandedPanel 集成 (SwitchDropdownSection + EmptyHistoryHint)

**Files:**
- Modify: `src/floating/components/ExpandedPanel.tsx` (在 InputBar 上方插入 SwitchDropdown 或 EmptyHistoryHint)

**Rationale**: V0.2.0 App.tsx 不持有 inputRef — 焦点跳转必须发生在 ExpandedPanel 内部(V0.2.0 ExpandedPanel 私有 `inputRef`,line 35)。EmptyHistoryHint 焦点跳转 + SwitchDropdownSection onSelect 都在 ExpandedPanel 内集成,App.tsx 不改动 (V0.2.0 沿用)。

**Behavior:**
- 展开态 + 无 active session:
  - `recs.length === 0` → `<EmptyHistoryHint onClickCreate={() => inputRef.current?.focus()} />`
  - `recs.length > 0` → `<SwitchDropdownSection onSelect={onTaskTitleChange} />`
- 展开态 + 有 active session: 维持 V0.2.0 ControlRow (暂停/完成),SwitchDropdown 隐藏
- 折叠态: 不变

- [ ] **Step 1: Wire up**

In `src/floating/components/ExpandedPanel.tsx`, add at top after imports:

```tsx
import { SwitchDropdownSection } from "./SwitchDropdownSection";
import { EmptyHistoryHint } from "./EmptyHistoryHint";
import { useRecentTaskTitles } from "../hooks/useRecentTaskTitles";
```

Inside `ExpandedPanel` component body, after `const inputRef = useRef<HTMLInputElement | null>(null);` (line 35), add:

```tsx
const { recs, loading } = useRecentTaskTitles();
```

Find where `<InputBar>` is rendered (inside the "no active session" branch). Before `<InputBar>` (or its wrapper), insert:

```tsx
{!activeSession && !loading && recs !== null && (
  <div className="px-1 pb-2">
    {recs.length === 0 ? (
      <EmptyHistoryHint onClickCreate={() => inputRef.current?.focus()} />
    ) : (
      <SwitchDropdownSection onSelect={onTaskTitleChange} />
    )}
  </div>
)}
```

Note: `onTaskTitleChange` is already a prop on `ExpandedPanel` (V0.2.0 App.tsx line 121 passes `setTaskTitle`). Reuse it directly — no new state needed.

- [ ] **Step 2: Verify TS build**

Run: `npx tsc --noEmit`
Expected: 0 errors

- [ ] **Step 3: Commit**

```bash
cd /home/jason/workspace/mindtap
git add src/floating/components/ExpandedPanel.tsx
git commit -m "feat(floating): ExpandedPanel 集成 SwitchDropdownSection + EmptyHistoryHint

- 业务痛点: V0.2.1 落地最后一步 — 展开态插入 SwitchDropdown 入口
- 边界条件: 仅无 active 时显示;有 active 时 SwitchDropdown 隐藏 (与 V0.2.0 ControlRow 共存逻辑)
- 决策理由: inputRef 在 ExpandedPanel 内私有,SwitchDropdown 必须在 ExpandedPanel 内集成 — App.tsx 不改动 (V0.2.0 沿用)
- 不这样改: 不重构 ExpandedPanel;不引入全局状态;不暴露 inputRef 给 App.tsx"
```

---

## Task T30: 验收 + Windows build + release notes

**Files:**
- Create: `docs/reports/v0.2.1-release-notes.md`

- [ ] **Step 1: WSL 端静态验证**

Run:
```bash
cd src-tauri
cargo check
cargo test
cargo clippy --all-targets -- -D warnings
cargo fmt --check
cd ..
npx tsc --noEmit
npm test
```
Expected: all green

- [ ] **Step 2: Windows 端 dev 实测 spec §六 验收清单**

Transfer to Windows host (D:\workspace\mindtap). Run:
```bash
npm install
npm run tauri dev
```

Verify spec §六 14 条 (load `docs/specs/2026-07-11-v0.2.1-task-switching-design.md` §六 checklist).

- [ ] **Step 3a: Enable NSIS bundle target (D1 前提)**

**Why**: AGENTS.md 明确写 `tauri.conf.json` 中 `bundle.targets = []` —— 默认**只**生成裸 `.exe`,**不**生成 NSIS 安装包。改 `["nsis"]` 才会生成安装包(约 20 MB)。

In `src-tauri/tauri.conf.json`, change `bundle.targets`:
```json
"bundle": {
  "active": true,
  "targets": ["nsis"],
  ...
}
```

Note: NSIS 由 Tauri 的 reqwest 自动下载 — 如在内网代理后,设 `$env:HTTPS_PROXY` (参见 AGENTS.md E19)。

- [ ] **Step 3b: Windows build NSIS installer**

Run: `npm run tauri build`

Expected: NSIS installer at `src-tauri\target\release\bundle\nsis\mindtap_0.2.1_x64-setup.exe` (~20 MB)

- [ ] **Step 4: Write release notes**

Create `docs/reports/v0.2.1-release-notes.md` (template: mirror V0.2.0 release notes format, replace scope with V0.2.1 SwitchDropdown).

- [ ] **Step 5: Commit + push**

```bash
git add docs/reports/v0.2.1-release-notes.md
git commit -m "docs(report): V0.2.1 release notes

- 业务痛点: V0.2.1 release 收口记录
- 边界条件: 含已知风险 + 待用户验证段
- 不这样改: 不夸大范围 (V0.2.1 = SwitchDropdown only)"
git push origin develop
```

---

## Self-Review

### 1. Spec coverage

| Spec § / 条款 | Task |
|---|---|
| §二 S11 复用已完成任务 | T26 (SwitchDropdownSection) |
| §二 S12 无历史 SwitchDropdown | T28 (EmptyHistoryHint) |
| §三 3.1 SwitchDropdown UI 位置 | T26 (popover overlay 9.9-C) |
| §三 3.2 数据源 (5 个不同 task_title 末次倒序) | T22 (db list_recent_task_titles) |
| §三 3.3 选后行为 (创建新 session 复用 task_title) | T29 (App.tsx onSelect → InputBar value → Enter) |
| §三 3.4 pending 态不引入 | (无新增;9.7-A 锁定) |
| §三 3.5 task 表 V0.3 | (V0.2.1 不涉及) |
| §三 3.6 a11y | T26 (aria-label / role / 键盘) |
| §三 3.7 错误处理 | T25 (catch → error state) + T26 (loading/empty 分支) |
| §四 数据 schema 增量 | T22 (0 schema 改动) |
| §六 验收清单 14 条 | T30 (Windows 端实测) |

Coverage: **完整**。S13 取消选择 + a11y 列表项 aria-selected 在 T26/T27 vitest 覆盖。

### 2. Placeholder scan

- "no TBD / TODO / fill in details / similar to" — 已扫描,**无**
- 每步代码块完整,可直接 copy-paste
- 不引用未在前面 task 定义的类型

### 3. Type consistency

| 类型 | 定义 | 使用 |
|---|---|---|
| `TaskTitleRec` (Rust) | T22 src-tauri/src/db/timer_session.rs | T23 IPC 返回值 + T24 TS 映射 |
| `TaskTitleRec` (TS) | T24 src/lib/tauri-bridge.ts | T25 hook state + T26 props |
| `useRecentTaskTitles()` | T25 | T26 (组件) + T29 (App.tsx) |
| `SwitchDropdownSection` props | T26 `{ onSelect: (task_title: string) => void }` | T29 (App.tsx onSelect handler) |
| `EmptyHistoryHint` props | T28 `{ onClickCreate: () => void }` | T29 (App.tsx focus InputBar) |

All consistent. `task_title` 字段命名一致 (snake_case 在 Rust / snake_case 透传 TS — V1.3 retro 教训已规避)。

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-07-11-v0.2.1-task-switching.md`.**

⚠️ **执行门**: V0.2.0 Windows 端实测报告 v2 中 P0 必须先修完 + V0.2.0 release 完成。按 plan §四 实施原则 1:"V0.2.0 release 未完成 → V0.2.1 实施等"。

**Two execution options (V0.2.0 release 后启用):**

1. **Subagent-Driven (recommended)** - 派 fresh subagent per task,review between tasks,fast iteration
2. **Inline Execution** - 当前 session 执行,batch with checkpoints

我倾向 Subagent-Driven:V0.2.1 共 9 commit + 9 vitest + 1 release notes,fresh subagent 隔离上下文更稳。
