# docs/

修改文档前先读 `governance/doc-layers.md`，以它为唯一的归位细则。

- 每个大需求一个目录，目录内 `index.md` 是唯一入口；除 `index.md` 外不互相链接。
- 产品需求、领域决策、视觉契约、任务范围和发版记录各归其层；技术实现细节留在代码与测试。
- task.md 只写业务边界与验收意图，不记录文件清单、提交 hash 或易过期状态。
