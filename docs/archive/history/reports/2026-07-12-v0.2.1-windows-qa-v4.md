# V0.2.1 Windows 端 QA 实测计划 v4 + 实测填写模板

> **本文件 = Windows 端 agent 验收任务脚本**
> 严格按下方步骤执行, 不自由发挥。agent 只填实测数据, 不推断根因 / 不给修复方案 (V3 反思 D4)。
>
> **创建**: 2026-07-12 (V0.2.1 patch 收口)
> **关联**: V3 报告 `docs/reports/2026-07-11-v0.2.0-windows-qa-v3.md` + V0.2.1 final fix retro `docs/reports/v0.2.0-final-fix-retrospective.md` + spec v1.2 `docs/specs/2026-07-11-v0.2.0-floating-window-design.md`

---

## 一、任务范围

Windows 端 agent 唯一任务: **对 V0.2.1 patch 6 commit 跑实测, 填写本模板, commit v4 QA 报告**。

**绝对禁止**:
- ❌ 改任何源码 (本任务只测不修)
- ❌ amend 任何 commit (v4 报告 = 新 commit, 不动前 6 commit)
- ❌ 推断 root cause / 给修复方案 (V3 反思 D4)
- ❌ 擅自 kill / stop / close 用户可能还在用的环境 (V3 反思 D1)
- ❌ 测试期间 commit 任何东西 (V3 反思 D2; v4 报告 = 全部 18 条 GUI + 5 P0 验证齐了才 commit)
- ❌ rebase / amend / force push (V3 反思 D3)
- ❌ 引用 `.archive/` 代码作为依据 (只作事件清单 + 沿用路径参考, 反模式 9)
- ❌ "我假设可以工作" / "应 PASS" (按 V3 报告反馈执行)

---

## 二、前置阅读 (必读)

1. **本文件 (v4 验收模板)** — 严格按下方步骤执行
2. **V3 报告**: `docs/reports/2026-07-11-v0.2.0-windows-qa-v3.md` — 5 P0 ❌ 项起点
3. **V0.2.1 final fix retro**: `docs/reports/v0.2.0-final-fix-retrospective.md` — 5 P0 修复 commit 列表 + 沿用 .archive 来源
4. **spec v1.2**: `docs/specs/2026-07-11-v0.2.0-floating-window-design.md` (§三 + §六 验收标准 + §九 9 项决策表)
5. **浮窗可见性 7 层 Checklist**: `docs/architecture/v0.2.0-floating-visibility-checklist.md`
6. **V3 agent errors**: `docs/reports/2026-07-11-v3-qa-agent-errors.md` — agent 9 个错 + D1-D6 防御清单

---

## 三、起点确认 (Step 1)

```bash
cd D:\workspace\mindtap
git fetch origin
git status
git log --oneline -8
```

**预期**:
```
branch: develop
HEAD: 70bcdbf docs(retro): V0.2.0 final fix retrospective — 5 P0 反复犯教训...
前 7 commits:
  70bcdbf docs(retro): V0.2.0 final fix retrospective
  ad1e14b chore(release): V0.2.0 → V0.2.1 patch
  b99667b docs(spec): V0.2 spec v1.2 — 纠 4 处偏离
  f089d26 test(floating): 折叠态 drag/click 测试
  c008c3c fix(bridge): P0 不抢焦清理
  73749be fix(floating): P0 折叠态端到端修复
  1192515 docs(report): V0.2.0 Windows 端 QA 实测报告 v3
working tree: clean
```

如果 HEAD ≠ `70bcdbf` 或 working tree 不 clean → **停止**, 通知用户。

---

## 四、静态 6 关 (Step 2 — agent 自跑, 全过才能进 Step 3)

```bash
cd D:\workspace\mindtap
cargo check --manifest-path src-tauri/Cargo.toml
cargo test --manifest-path src-tauri/Cargo.toml --lib
cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets
cd src-tauri && cargo fmt --check && cd ..
npx tsc --noEmit
npx vitest run
npm run build
```

**预期**:
- cargo check: `Finished dev profile [unoptimized + debuginfo]`
- cargo test: `9 passed; 0 failed; 0 ignored` (timer_session 9 个 unit test)
- cargo clippy: `Finished dev profile`, **0 warnings**
- cargo fmt: clean, **无 diff 输出**
- tsc: **0 errors** (空输出)
- vitest: `Test Files 21 passed (21)` + `Tests 75 passed (75)` (含 commit f089d26 新增 2 个 drag 用例)
- npm run build: 产出 6 个 assets 文件
  - `dist/assets/floating-*.css` ≥ 30 KB (P0-3 修复 hard gate, 当前 38.88 KB)
  - `dist/assets/main-*.css` ~41 KB
  - `dist/assets/floating.html` 存在
  - `dist/assets/floating-*.js` ~24 KB

**实测填写** (按模板):

| 验证项 | 命令 | 结果 |
|---|---|---|
| cargo check Finished | `cargo check --manifest-path src-tauri/Cargo.toml` | ✅ / ❌ (贴最后 1 行) |
| cargo test 9 passed | `cargo test --manifest-path src-tauri/Cargo.toml --lib 2>&1 \| tail -1` | ✅ / ❌ (贴输出) |
| cargo clippy 0 warn | `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets 2>&1 \| tail -1` | ✅ / ❌ (贴输出) |
| cargo fmt clean | `cd src-tauri && cargo fmt --check; cd ..` | ✅ / ❌ |
| tsc 0 err | `npx tsc --noEmit 2>&1` | ✅ / ❌ |
| vitest 75 passed | `npx vitest run 2>&1 \| tail -4` | ✅ / ❌ (贴输出) |
| floating-*.css ≥ 30 KB | `ls -la dist/assets/floating-*.css` | ✅ / ❌ (贴字节数) |
| floating.html 存在 | `ls dist/floating.html` | ✅ / ❌ |

**任一项 ❌ → 停止, 通知用户, 不进 Step 3**。

---

## 五、npm install (Step 3 — 仅当 package.json / package-lock.json 变更)

```bash
# 仅当 package.json 或 package-lock.json 在 commit ad1e14b 变更时
git diff 70bcdbf~7 70bcdbf~5 -- package.json package-lock.json
```

如果输出空 → **跳过 npm install**, 直接进 Step 4。

如果非空 → 跑:
```bash
npm install
```

**实测填写**: 跑了 `npm install` / 跳过了 (按实际)。

---

## 六、agent runtime 自测 12 条 (Step 4 — agent 可自跑)

> **agent 物理能力**: 起 binary + 验证 PID/内存/DB/窗口/WebView/DevTools, 但**不能替用户做 GUI 交互** (点击 / 拖动 / 输入)。
> agent 不能测的项标 ❓, 等 Step 5 用户实测反馈填入。

```bash
# Step 4.1: 起 dev (后台或另一终端)
npm run tauri dev
```

### 6.1 Process / WebView 物理层 (agent 自测)

| # | 项 | 验证命令 / DevTools | 预期 | 实测 |
|---|---|---|---|---|
| 1 | 启动后 mindtap.exe 进程存活 | Task Manager 看 `mindtap.exe` PID + WorkingSet ~50 MB | ✅ 进程存活 | (填) |
| 2 | webview 已创建 (floating.html 加载) | DevTools Console 无 "window load failed" | ✅ 无 load error | (填) |
| 3 | webview visible (折叠态浮窗显示在主屏右下角) | 屏幕右下角 -100px 看到浮窗 | ✅ 显示 | (填) |
| 4 | webview 内容加载 (React mount) | DevTools Console 无 error;`useActiveTask` 无 unhandled rejection | ✅ mount OK | (填) |
| 5 | 折叠态物理尺寸 320×36 | DevTools Elements 检查 `.floating-root` width=320 height=36 | ✅ 尺寸对 | (填) |
| 6 | 展开态物理尺寸 360×280 | 点开浮窗后 DevTools Elements 检查 `.floating-root.expanded` width=360 height=280 | ✅ 尺寸对 | (填) |
| 7 | 折叠态 root div className 含 `floating-root folded` | DevTools Console: `document.querySelector('.floating-root').className` | ✅ 含 floating-root folded | (填) |
| 8 | 折叠态根 div 不挂 data-tauri-drag-region attribute | DevTools Console: `document.querySelector('.floating-root').getAttribute('data-tauri-drag-region')` | null | (填) |
| 9 | 折叠态根 div 含 class `flex items-center gap-2 px-2 py-1` (Tailwind utility 生效) | DevTools Console: `document.querySelector('.floating-root').classList.contains('flex')` | true | (填) |
| 10 | 玻璃背景 backdrop-filter 生效 | DevTools Console: `getComputedStyle(document.querySelector('.floating-root')).backdropFilter` | `blur(20px) saturate(180%)` (或 -webkit 前缀) | (填) |
| 11 | 折叠态文字 task_title 暗色可读 (#3 修复验证) | DevTools Console: `getComputedStyle(document.querySelector('.floating-root span')).color` | `rgb(29, 41, 57)` (= `#1D2939` text-text-1) | (填) |
| 12 | db 文件存在 `%APPDATA%\com.mindtap.desktop\projects.db` | `Test-Path "$env:APPDATA\com.mindtap.desktop\projects.db"` | True | (填) |

### 6.2 static grep 反模式 9 防御 (agent 自测)

| grep 项 | 命令 | 预期 | 实测 |
|---|---|---|---|
| `.floating-root` 类挂载 (折叠 + 展开) | `grep -rnE "floating-root (folded\|expanded)" src/floating/` | ≥ 2 处 | (填) |
| 内 button `data-no-expand` 短路 | `grep -rn "data-no-expand" src/floating/components/` | ≥ 4 处 (ContextMenu + ExpandedPanel + ControlRow × 3) | (填) |
| App.tsx 自实现 handleMouseDown + 4px threshold | `grep -nE "handleMouseDown\|DRAG_THRESHOLD_PX\|dragRef" src/floating/App.tsx` | ≥ 3 处 | (填) |
| App.tsx 长按 300ms + startDragging() IPC | `grep -nE "LONG_PRESS_MS\|startDragging" src/floating/App.tsx` | ≥ 2 处 | (填) |
| App.tsx useWindowPosition (saved 持久化 + clamp) | `grep -nE "POS_KEY\|availableMonitors\|clampInsideMonitor" src/floating/App.tsx` | ≥ 3 处 | (填) |
| lib.rs 不含重复 set_focusable 启动期 | `grep -n "set_focusable" src-tauri/src/lib.rs` | 0 处 (setFocusable 全在 App.tsx) | (填) |
| floating.css 无 `color-scheme: dark` | `grep -n "color-scheme" src/floating/styles/floating.css` | 0 处 | (填) |
| floating.css body 含 `color: var(--color-text-1)` | `grep -n "color: var(--color-text-1)" src/floating/styles/floating.css` | 1 处 | (填) |
| 文字色用 token (text-text-1/2/3, 无 text-white) | `grep -rn "text-white" src/floating/` | 0 处 (或仅 ContextMenu hover state) | (填) |
| vitest mock @tauri-apps/api/core (src/test/setup.ts) | `grep -n "vi.mock" src/test/setup.ts` | ≥ 1 处 | (填) |

---

## 七、GUI 18 条验收 (Step 5 — 用户实测反馈, agent 不能测的标 ❓)

> **agent 物理限制**: OpenCode bash 环境无 GUI 交互能力, 不能替用户点 / 拖 / 输入。
> agent 不能测项标 ❓, 等用户实测反馈后填入。

| # | 项 | spec §六 验收 | 实测 |
|---|---|---|---|
| 1 | 启动应用, 主窗 + 浮窗都显示 | 都显示 | (👤 用户实测: 填) |
| 2 | Ctrl+Shift+Space toggle 浮窗 | toggle 显隐 | (👤 用户实测: 填) |
| 3 | 浮窗折叠态显 task_title + focus_ms (1 秒刷新) | 文字暗色可读 + 时间每秒 +1 | (👤 用户实测: 填) |
| 4 | 浮窗不抢焦 (IDE 焦点保持) | 不抢焦 | (👤 用户实测: 填) |
| **5** | **折叠→展开** (#5 P0-1 修复验证) | 短按折叠态 = 展开 360×280 | (👤 用户实测: 填) |
| 6 | 输入 + Enter → active + 自动折叠 | Enter = 开始 | (👤 用户实测: 填) |
| 7 | Esc → 折叠不创建 session | Esc = 取消 | (👤 用户实测: 填) |
| 8 | 失焦 → 自动折叠 | 失焦折叠 | (👤 用户实测: 填) |
| 9 | active 展开按钮 (暂停/恢复/完成) | 看到 3 按钮 | (👤 用户实测: 填) |
| 10 | 暂停 | focus_ms 冻结 | (👤 用户实测: 填) |
| 11 | 恢复 | focus_ms 继续 | (👤 用户实测: 填) |
| 12 | 完成 | 自动折叠 | (👤 用户实测: 填) |
| 13 | 关主窗 = 隐藏 (不退出) | 关主窗浮窗继续 | (👤 用户实测: 填) |
| 14 | 浮窗右键 → 退出 | 退出应用 | (👤 用户实测: 填) |
| 15 | .floating-root 类 (折叠) | 静态测 (Step 6.2 #7 已含) | ✅ (沿用) |
| 16 | 玻璃背景 (blur + saturate) | 静态测 (Step 6.2 #10 已含) | ✅ (沿用) |
| **17** | **浮窗拖动** (#17 P0-2 修复验证) | **长按 300ms = OS 拖拽; 实际拖动窗口看位置变化** | (👤 用户实测: 填, 注意长按超 300ms 才触发, 不是短按) |
| 18 | 主窗 drag 5 区 (V0.1.6 carryover) | 主窗顶部 5 区可拖 | (👤 用户实测: 填) |

### Step 5 用户实测提示 (照搬给用户):

> **Windows 端 agent 转发给用户**:
>
> 请在 `npm run tauri dev` 启动后, 按以下顺序实测:
>
> 1. **确认浮窗文字可读** (V3 ❌ → V4 ✅): 看折叠态 task_title + 计时器是不是暗色清晰
> 2. **测试拖动** (V3 ❌ → V4 ✅, 注意是长按): 折叠态鼠标**按住不放 300ms 以上**, 窗口应跟随鼠标拖动;**短按**才是展开
> 3. **测试展开**: 折叠态短按 (点击 + 立即松开), 应展开 360×280 浮窗
> 4. **测试位置**: 浮窗应出现在主屏右下角 -100px (避 Win 11 任务栏 48px)
> 5. **测试不抢焦** (V3 ❓ → V4 ✅): IDE 焦点保持, 浮窗不抢
> 6. **测试输入**: 展开态输入任务标题 + Enter → 折叠回折叠态 + active 计时
> 7. **测试取消**: 输入文字 + Esc → 折叠不创建
> 8. **测试失焦折叠**: 展开态点别处 → 折叠
> 9. **测试暂停/恢复/完成**: 折叠态点开浮窗 → 点暂停/恢复/完成
> 10. **测试关主窗**: 点主窗关闭按钮, 主窗应隐藏, 浮窗继续, 应用不退出
> 11. **测试右键退出**: 浮窗右键 → "退出", 应用退出
> 12. **测试主窗拖动**: 主窗顶部 5 区可拖 (V0.1.6 carryover)

---

## 八、V3 报告 5 P0 修复针对性验证 (Step 6 — 核心)

> 每 P0 一个明确 ✅/❌/❓ (❓ = agent 物理限制 + 用户未实测)

| P0 | V3 ❌ → V4 验证项 | spec § | 实测 (用户) |
|---|---|---|---|
| **P0-3 视觉文字** | 折叠态 task_title 文字暗色可读 (实测 #3) | §三 3.1 + §九 9.8 | (👤 填 ✅/❌) |
| **P0-5 展开 resize** | 短按 = 展开 360×280 (实测 #5) | §三 3.2 + §九 9.3 | (👤 填 ✅/❌) |
| **新 P0-A 拖动** | 长按 300ms = OS 拖拽 (实测 #17) | §三 3.1 拖拽行 | (👤 填 ✅/❌, 注意长按 vs 短按) |
| **新 P0-B 设计语言** | 玻璃背景可视 + 文字暗色对比度 ≥ 4.5:1 (实测 #3 + #16) | glassic-ui-spec §八 #4 | (👤 填 ✅/❌) |
| **不抢焦清理** | lib.rs 不再重复设 set_focusable;展开态 input 可聚焦 (实测 #4 + #6) | §九 9.5 | (👤 填 ✅/❌) |

---

## 九、新 build 产物 (Step 7)

```bash
npm run tauri build
```

预期产物:

| 产物 | 路径 | 大小 | MD5 |
|---|---|---|---|
| 裸 exe | `D:\workspace\mindtap\src-tauri\target\release\mindtap.exe` | ~10 MB | (新 MD5, 应不同于 v3 报告 `8aba7a0a91bc3ad01e52be4f4d9b9063`) |
| NSIS installer | `D:\workspace\mindtap\src-tauri\target\release\bundle\nsis\mindtap_0.2.1_x64-setup.exe` | ~2.6 MB | (新 MD5) |

```bash
certutil -hashfile src-tauri\target\release\mindtap.exe MD5
certutil -hashfile src-tauri\target\release\bundle\nsis\mindtap_0.2.1_x64-setup.exe MD5
```

**实测填写**:

| 产物 | MD5 | 大小 |
|---|---|---|
| mindtap.exe | (填) | (填 MB) |
| mindtap_0.2.1_x64-setup.exe | (填) | (填 MB) |

---

## 十、V0.2.0 release gate (Step 8 — 全部 18 条 + 5 P0 验证后填)

| 项 | 状态 |
|---|---|
| 启动应用, 主窗 + 浮窗都显示 | ✅ / ❌ |
| Ctrl+Shift+Space toggle | ✅ / ❌ |
| 浮窗折叠态显 task_title + focus_ms (1 秒刷新) | ✅ / ❌ (V3 ❌ → V4 验证) |
| 浮窗不抢焦 (IDE 焦点保持) | ✅ / ❌ / ❓ |
| 折叠→展开 (短按) | ✅ / ❌ (V3 ❌ → V4 验证) |
| 输入 + Enter → active + 自动折叠 | ✅ / ❌ / ❓ |
| Esc → 折叠不创建 session | ✅ / ❌ / ❓ |
| 失焦 → 自动折叠 | ✅ / ❌ / ❓ |
| 暂停 / 恢复 / 完成 | ✅ / ❌ / ❓ |
| 完成 → 自动折叠 | ✅ / ❌ / ❓ |
| 关主窗 = 隐藏 (不退出) | ✅ / ❌ / ❓ |
| 浮窗右键 → 退出 | ✅ / ❌ / ❓ |
| a11y: aria-label + 对比度 4.5:1 | ✅ / ❌ (V3 ❌ → V4 验证) |
| **新**: 浮窗可拖动 (长按 300ms) | ✅ / ❌ (V3 ❌ → V4 验证) |
| **新**: 浅色玻璃设计语言符合 | ✅ / ❌ (V3 ❌ → V4 验证) |
| **新**: 折叠态位置 = 主屏右下角 -100px | ✅ / ❌ |
| **新**: 启动期 useWindowPosition 持久化 | ✅ / ❌ / ❓ |
| **新**: lib.rs 不再重复 set_focusable | ✅ / ❌ (静态 grep 验证) |

---

## 十一、严禁行为 (本任务全程遵守 — V3 反思 D1-D6)

- ❌ 未 amend 任何 commit (v4 = 新 commit, 不动前 7 commit 73749be / c008c3c / f089d26 / b99667b / ad1e14b / 70bcdbf)
- ❌ 未修改任何源码 (本报告只测不修)
- ❌ 未跳过静态 6 关 (Step 2 任一 ❌ = 停止, 通知用户)
- ❌ 未 commit "待验证 TODO" / "应 PASS" 字样 (V0.1.6 retro lesson 6)
- ❌ 未 "我假设可以工作" (实测什么就填什么, 不推断)
- ❌ 未擅自 kill 任何用户可能还在用的进程 (V3 反思 D1 — V3 agent 错 1)
- ❌ 未在测试期间 commit 任何东西 (V3 反思 D2 — V3 agent 错 2)
- ❌ 未 rebase / amend / force push (V3 反思 D3 — V3 agent 错 3)
- ❌ 未在报告加 agent 自由发挥内容 (V3 反思 D4 — V3 agent 错 4)
- ❌ 未用 cargo check 替代 runtime 实测 (V3 反思 D5 — V3 agent 错 5)
- ❌ 未把 agent 子任务完成 = 整个任务完成 (V3 反思 D6)

---

## 十二、commit v4 报告 (Step 9)

**前提**: 全部 18 条 GUI 实测 + 5 P0 验证齐了才 commit。

```bash
git add docs/reports/2026-07-12-v0.2.1-windows-qa-v4.md
git commit -m "docs(report): V0.2.1 Windows 端 QA 实测报告 v4 (基于 70bcdbf 5 P0 修复后)

- WSL 端 6 commits 修了 V3 报告 5 P0 一次到位, 沿用 .archive/src/ 已 work 实施路径
  - 73749be fix(floating): 折叠态端到端修复 (mousedown/drag/position + 文字暗色)
  - c008c3c fix(bridge): 不抢焦清理 (lib.rs 重复 set_focusable)
  - f089d26 test(floating): drag/click 测试用例
  - b99667b docs(spec): V0.2 spec v1.2 — 纠 4 处偏离
  - ad1e14b chore(release): V0.2.0 → V0.2.1 patch
  - 70bcdbf docs(retro): V0.2.0 final fix retrospective
- 静态 6 关全过 (实测填写)
- 用户实测 18 条 GUI + 5 P0 验证收口 (实测填写)
- 新 build NSIS MD5 (新 MD5, 不同 V3 报告 8aba7a0a91bc3ad01e52be4f4d9b9063)
- V0.2.1 release gate 全部 ✅

严禁行为全程遵守 (V3 反思 D1-D6):
- 未 amend 任何 commit
- 未修改任何源码
- 未跳过静态 6 关
- 未 commit 待验证 TODO
- 未 \"我假设可以工作\"
- 未擅自 kill 用户进程
- 测试期间仅 commit v4 报告 1 个 commit
- 未 rebase / amend / force push
- 报告内容严格按模板填, 无 agent 自由发挥

反模式 9 防御 (archive-reference-only.mdc): 沿用 .archive/src/ 实装路径全部 grep 验证 ✅
不引用 .archive/ 代码作为依据 (只作事件清单)"
```

---

## 十三、关联文档

- **前置报告**:
  - V3: `docs/reports/2026-07-11-v0.2.0-windows-qa-v3.md` (5 P0 ❌ 项起点)
  - V3 agent errors: `docs/reports/2026-07-11-v3-qa-agent-errors.md` (9 个错 + D1-D6 防御)
  - V3 runtime issues: `docs/reports/2026-07-11-v3-qa-runtime-issues.md` (用户实测 3 问题清单)
- **关联 fix**:
  - V0.2.1 retro: `docs/reports/v0.2.0-final-fix-retrospective.md` (5 P0 修复 commit 列表 + .archive 来源)
  - V0.2 spec v1.2: `docs/specs/2026-07-11-v0.2.0-floating-window-design.md` (§三 + §六 + §九)
  - V0.2.0 retro: `docs/reports/v0.2.0-retrospective.md` (反模式 9)
  - V0.1.6 retro: `docs/reports/v0.1.6-retrospective.md` (5 轮 drag 修复教训)
- **7 层 Checklist**: `docs/architecture/v0.2.0-floating-visibility-checklist.md`
- **设计语言**: `docs/design/glassic-ui-spec.md` (§八 #4 WCAG AA 4.5:1 强制项)

---

## 十四、报告自身信息

- **报告路径**: `docs/reports/2026-07-12-v0.2.1-windows-qa-v4.md`
- **填写状态**: ⏳ 待 agent 实测 + 用户 GUI 实测反馈后填入
- **commit 状态**: ⏳ 待全部 18 条 GUI + 5 P0 验证齐了才 commit
- **写于**: 2026-07-12
- **由**: WSL agent 制定模板 (此次会话), Windows 端 agent 填写实测数据 + commit

---

# ✅ V0.2.0 全部问题一次性修复收口
**本次 V0.2.1 patch 完成 5 P0 修复 + spec v1.2 纠错 + version bump + retro, Windows 端 v4 QA 验收待跑**