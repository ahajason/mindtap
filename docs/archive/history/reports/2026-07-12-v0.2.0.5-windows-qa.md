# V0.2.0.5 (≡ V0.2.7) Windows 端 QA 实测计划 v8 (Windows 端直接填写, 沿用 v7 模板增量)

> **版本号说明**: 本文件原标题 `V0.2.7 Windows 端 QA 实测计划 v8`,按 `docs/governance/versioning-rule.md` §三 应归属为 V0.2.0.5 PATCH QA 报告(同一次 PATCH 历史档案,内容不变)。正文仍用 `V0.2.7` 是历史名称追溯需要。
> **创建**: 2026-07-12 (V0.2.7 patch 后)
> **关联**: V0.2.7 commit (本次新增) + V0.2.6 commit 48f4dbd + V0.2.5 df8c252 + V0.2.4 90ecc71 + V0.2.3 v5 模板 (bf28555)
> **沿用**: V0.2.5 v7 模板 + V0.2.6 33 commit 回查 + V0.2.5 反模式 13 + **V0.2.7 反模式 15 防御标准**

---

## 一、任务范围

Windows 端 agent 唯一任务: **对 V0.2.7 patch (本次新增 commit) 跑实测, 填写本模板, commit v8 实测报告**。

V0.2.7 patch 修了 5 个 P0/P1 bug (commit hash 待生成):
- **Bug 1**: `act()` 函数无 try/catch → 闪退真根因 (App.tsx:239-247)
- **Bug 2**: 缺 `win.startDragging()` IPC → 拖动完全失效 (App.tsx:147-184) — **反模式 15 谎改真根因**
- **Bug 3**: tauri.conf.json floating 段硬编码 x:1500 y:920 → 多屏/小屏/4K 缩放启动位置错位
- **Bug 4**: resize useEffect await 之间无 cancelled 守卫 → 串行 IPC race
- **Bug 5**: 展开 setPosition 偏移 -20px → 贴右边缘时右边被截 4px
- **Bug 6**: spec 漂移 6 处 (line 3/70/77/85/146/§六/§九) → 反模式 15 下游灾害

**绝对禁止** (V3 反思 D1-D6 + V0.2.5 反模式 13 + V0.2.6 反模式 14):
- ❌ 改任何源码 (本任务只测不修)
- ❌ amend 任何 commit (v8 = 新 commit, 不动 V0.2.7 之前)
- ❌ 推断 root cause / 给修复方案 (D4)
- ❌ 擅自 kill / stop / close 用户可能还在用的环境 (D1)
- ❌ 测试期间 commit 任何东西 (D2)
- ❌ rebase / amend / force push (D3)
- ❌ "我假设可以工作" / "应 PASS" (按实测数据填)
- ❌ 漏完整读之前 retro (V0.2.5 反模式 13)
- ❌ **断言"代码字面包含 X 模式"而不是"X 行为生效" (反模式 14 防御标准: App.test.tsx:115-122 拖动测试只断言"不展开" 漏断"调了 startDragging" — V0.2.5/V0.2.6 反复修都漏改的真根因之一)**

---

## 二、前置阅读 (必读, 完整 7 链)

1. 本文件 (v8 验收模板)
2. **V0.2.7 final fix retro** (本次新增): `docs/reports/v0.2.0.5-final-fix-retrospective.md` (待生成)
3. V0.2.6 final fix retro + 33 commit 回查: `docs/reports/v0.2.0.4-final-fix-retrospective.md` + `v0.2.0.4-33-commit-audit.md`
4. V0.2.5 final fix retro: `docs/reports/v0.2.0.3-final-fix-retrospective.md`
5. V0.2.4 (= V0.2.0.2) final fix retro: **未生成** — 见 V0.2.0 final fix retro 反模式 12 段
6. V0.2.3 (= V0.2.0.1) final fix retro: **未生成** — 见 V0.2.0 final fix retro 反模式 11 段
7. V0.2.0 final fix retro: `docs/reports/v0.2.0-final-fix-retrospective.md`
8. V0.2.0 retro: `docs/reports/v0.2.0-retrospective.md`
9. V0.1.6 retro: `docs/reports/v0.1.6-retrospective.md` (5 轮 drag 修复 Lesson 5)
10. V3 agent errors: `docs/reports/2026-07-11-v3-qa-agent-errors.md` (D1-D6 防御)
11. **V0.2.7 spec v1.6** (本次同步): `docs/specs/2026-07-11-v0.2.0-floating-window-design.md` (line 3 v1.6 + 6 处更新)
12. .archive V1.0 实装完整: `.archive/src/floating/{FloatShell.tsx, useDragLongPress.ts, useWindowPosition.ts, glass.css, focus-block.css}`

---

## 三、起点确认 (Step 1)

```bash
cd D:\workspace\mindtap
git pull origin develop
git rev-parse HEAD
git log --oneline -15
```

**预期** (V0.2.7 commit hash 待生成, 假设以 `fix(floating): V0.2.7 patch` 开头):
```
branch: develop
HEAD: <V0.2.7 commit hash> fix(floating): V0.2.7 patch — 5 bug 真根因 (act try/catch + startDragging + tauri.conf x/y + cancelled + 偏移)
前 14 commits 包含:
  - V0.2.7 patch (本次)
  - 820dee1 docs: V0.2.6 33 commit 完整 1 行 1 行逐行回查报告
  - 48f4dbd fix(floating): V0.2.6 final fix — 13 改 + spec v1.6 + 13 组测试 + 33 commit 逐行复盘
  - 6990327 fix(floating): P0-9 WebView2 transparent 浮窗 native context menu
  - 1b162eb fix(floating): V0.2.5 patch — 展开闪退真根因
  - (其他 V0.2.5/V0.2.4/V0.2.3 commits)
working tree: clean
```

如果 HEAD 不是 V0.2.7 patch → **停止**, 通知用户。

---

## 四、静态 6 关 (Step 2 — agent 自跑, 全过才能进 Step 3)

> 沿用 v7 模板, V0.2.7 新增 10 组 fix tests 预期在 vitest 阶段 PASS。

```bash
cargo check --manifest-path src-tauri/Cargo.toml
cargo test --manifest-path src-tauri/Cargo.toml --lib
cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets
cd src-tauri && cargo fmt --check && cd ..
npx tsc --noEmit
npx vitest run
```

**预期**:
- cargo check: `Finished dev profile` (允许 1 warning: dead_code in old handler)
- cargo test: `12 passed; 0 failed` (V0.2.7 未新增 Rust 单测, 沿用 v7)
- cargo clippy: `Finished dev profile` + **0 warnings**
- cargo fmt: clean
- tsc: **0 errors**
- vitest: `Test Files 24 passed (24)` + `Tests 113 passed (113)` (**比 v7 86 多 27: V0.2.6 +13 + V0.2.7 +10 + V0.2.5 后续 +4**)

**实测填写**:

| 验证项 | 结果 | 输出 |
|---|---|---|
| cargo check Finished | ✅ / ❌ | (贴最后 1 行) |
| cargo test 12 passed | ✅ / ❌ | (贴输出) |
| cargo clippy 0 warn | ✅ / ❌ | (贴输出) |
| cargo fmt clean | ✅ / ❌ | |
| tsc 0 err | ✅ / ❌ | |
| vitest 113 passed | ✅ / ❌ | (贴输出) |
| V0.2.7 新增 fix tests 10 PASS | ✅ / ❌ | (贴 `App.fix.test.tsx` 输出) |

---

## 五、V0.2.7 专项实测 (Step 3 — Windows D:\ 端必跑)

> **本节是 v8 新增, 验证 V0.2.7 5 个 bug 修复真生效。沿用 v7 的 18 GUI + 5 P0 项继续测。**

### 5.1 Bug 3 验证: 浮窗初始位置不在 1500,920

```bash
# 用 4K 缩放 150% 或副屏 1024×768 实测:
# - 应用启动后浮窗应在主屏右上 -16px 可见
# - 不应在 1500,920 那个绝对位置
```

| 验证项 | 结果 |
|---|---|
| 4K 缩放 150% 启动 → 浮窗可见 (右上 -16px) | ✅ / ❌ |
| 副屏 1024×768 启动 → 浮窗可见 (主屏右上) | ✅ / ❌ |
| 单屏 1920×1080 启动 → 浮窗可见 (右上 -16px, 1584,16) | ✅ / ❌ |
| 启动后 0.5s 内浮窗瞬移到 1500,920 | ❌ (应不发生) |

### 5.2 Bug 2 验证: 折叠态拖动 4px → OS 拖窗生效 (反模式 15 防御)

> **关键测试**: V0.2.5/V0.2.6 反复声称"沿用 useDragLongPress.ts:49 startDragging"但**代码完全没调 IPC**。这次必须**真拖动**验证 OS 拖窗生效。

```bash
# 折叠态浮窗 → 鼠标按下 + 拖动 5px → 松开
# 预期: 窗口跟随鼠标移动 + 抬起后保持移动后位置 + 不展开
```

| 验证项 | 结果 |
|---|---|
| 折叠态拖动 5px → 窗口跟随鼠标移动 | ✅ / ❌ |
| 拖动后松开 → 窗口保持新位置 | ✅ / ❌ |
| 拖动后松开 → 浮窗仍折叠 (不展开) | ✅ / ❌ |
| 拖动结束 1s 内 → 重启应用浮窗在拖动后的位置 (onMoved + localStorage) | ✅ / ❌ |
| **如果拖动 5px 窗口不动** → **V0.2.7 失败, 反模式 15 复发** → ❌ |

### 5.3 Bug 5 验证: 展开位置不偏移 -20px

```bash
# 把浮窗拖到主屏右上 1584,920 (1920×1080 屏)
# 点击折叠态 → 展开
# 预期: 浮窗展开后 x 仍 = 1584 (不偏移), 右边到 1944 (溢出 24px 但 OS 不报错)
# V0.2.6 行为: x = 1564 (-20 偏移), 右边到 1924 (溢出 4px)
```

| 验证项 | 结果 |
|---|---|
| 浮窗拖到右上角 → 点击展开 → 浮窗 x = 1584 (不偏移) | ✅ / ❌ |  <!-- [V0.2.0 误导] -->
| 浮窗 x = 1564 (-20 偏移) → V0.2.6 行为复发 | ❌ (应不发生) |

### 5.4 Bug 1 验证: pause/resume/complete IPC 失败时不闪退

> **难手动测**: 需要故意触发 IPC 失败。可选方式:
> A. 应用运行时手动 `%APPDATA%\com.mindtap.desktop\projects.db` 加锁 (用另一个进程持锁)
> B. 用 dev tools 模拟 invoke reject
> C. 短时间高频点击 pause/resume/complete 触发竞态

| 验证项 | 结果 |
|---|---|
| 触发 IPC 失败 (A/B/C 任一方式) → 浮窗不闪退 | ✅ / ❌ |
| 控制台 (Ctrl+Shift+I) 看到 `[act:pause] failed` 错误日志 | ✅ / ❌ |
| 浮窗 webview 仍能展开/折叠/输入 | ✅ / ❌ |

### 5.5 Bug 4 验证: resize useEffect cancelled 守卫

> **难手动测**: 需要快速切换展开/折叠触发 race。

```bash
# 反复快速点击折叠态根 div 触发展开+折叠 race
# 预期: 浮窗最终状态稳定, 不出现"已折叠但尺寸是 360×280"或反之
```

| 验证项 | 结果 |
|---|---|
| 反复快速点击 (1 秒内 5+ 次) → 浮窗最终状态稳定 | ✅ / ❌ |
| 浮窗最终尺寸与 expanded 状态一致 (折叠=320×36, 展开=360×280) | ✅ / ❌ |

---

## 六、沿用 v7 模板 (Step 4 — 18 GUI + 5 P0 项, 继续测)

> v7 模板的 §五 到 §十一 全沿用, 本文件不重复。仅当 V0.2.7 patch 影响了 v7 某项时**额外标注**:

### v7 §5.1 不抢焦实测 (V0.2.7 spec 已更新: V1.0 D15 路径)

```bash
# IDE (VSCode) 焦点 → 按 Ctrl+Shift+Space 显示浮窗
# 预期: IDE 输入框仍能输入 (焦点未丢)
```

| 验证项 | 结果 |
|---|---|
| IDE 焦点 → Ctrl+Shift+Space → IDE 仍可输入 | ✅ / ❌ |
| 浮窗输入框点击 → IDE 不抢焦 (反模式 13 防御) | ✅ / ❌ |

### v7 §5.2-5.18 (18 GUI 项) 沿用, 不重复

### v7 §5 P0 修复 (5 P0) — V0.2.7 新增 P0 修复 (5 项)

**新增必测**:
- ✅ Bug 3: tauri.conf x/y 硬编码删 (见 §5.1)
- ✅ Bug 2: startDragging IPC 实调 (见 §5.2)
- ✅ Bug 5: 展开不偏移 (见 §5.3)
- ✅ Bug 1: act try/catch (见 §5.4)
- ✅ Bug 4: resize cancelled 守卫 (见 §5.5)

**V0.2.5 沿用必测** (沿用 v7 §5):
- ✅ 不抢焦 (反模式 13 防御标准)
- ✅ 1px 黑边消除
- ✅ 呼吸灯 animate-pulse-dot
- ✅ setFocusable 严禁 (Win11 WebView2 panic)
- ✅ contextmenu 原生 listener (capture phase)

---

## 七、反模式 15 防御标准 (V0.2.7 新增验收要求)

> **核心**: V0.2.5/V0.2.6 反复修都没修好的真根因之一是 commit message 撒谎。本节强制 v8 实测要验证 commit message 与代码实际行为一致。

```bash
# 对每个 V0.2.7 commit claim, 验证代码真做了:
git log --oneline -5
# 然后 grep 代码确认:
grep -n "win\.startDragging" src/floating/App.tsx  # 应有调用
grep -n "x.*1500\|y.*920" src-tauri/tauri.conf.json  # 应 0 命中
grep -n "Math\.round.*FOLDED_W.*EXPANDED_W" src/floating/App.tsx  # 应 0 命中
grep -n "try" src/floating/App.tsx | grep -i act  # act 函数内应有 try
grep -n "cancelled" src/floating/App.tsx  # resize useEffect 应有 >= 4 处 cancelled check
```

| 验证项 | 结果 |
|---|---|
| `win.startDragging()` 在源码中存在且会被调 (见 §5.2) | ✅ / ❌ |
| tauri.conf.json 无 x:1500 y:920 硬编码 | ✅ / ❌ |
| Math.round((FOLDED_W-EXPANDED_W)/2) 公式已删 | ✅ / ❌ |
| act() 函数体内含 try/catch | ✅ / ❌ |
| resize useEffect 每个 await 后有 cancelled 检查 | ✅ / ❌ |

---

## 八、最终交付清单 (Step 5)

完成所有 §四-§七 后, agent 必须:

1. **填写本模板所有 ✅ / ❌**
2. **commit v8 实测报告** (不允许 amend, 不允许 rebase):
   ```bash
   git add docs/reports/2026-07-12-v0.2.0.5-windows-qa.md
   git commit -m "test(win-qa): V0.2.7 v8 实测报告 — 5 P0 修复验证"
   git push origin develop
   ```
3. **如有 ❌** → 立即停止, 通知用户, 不允许自行修

---

## 九、反模式沉淀 (V0.2.7 新增)

| # | 名称 | 出处 |
|---|---|---|
| 14 | 反复修最多次仍未修好 = 测试断言太弱 + 1.x 教训没完整沿用 + user 没重 build + 没一次到位完整修 | `v0.2.6-final-fix-retrospective.md:174` |
| 15 | commit message 撒谎 = 声称改但实际没改 | `v0.2.6-final-fix-retrospective.md:178-208` |
| **16 (V0.2.7 新增候选)** | 测试断言只覆盖字面包含而非行为生效 = V0.2.5/V0.2.6 反复修都漏改的真根因之一 | App.test.tsx:115-122 拖动测试只断言"不展开"漏断"调 startDragging" |

> v8 实测报告 commit 后, 反模式 16 应加入 `v0.2.0.5-final-fix-retrospective.md` 沉淀(2026-07-13 rename from V0.2.7 后)。