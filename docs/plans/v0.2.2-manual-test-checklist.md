# V0.2.2 手动实测检查清单

> 仅验证 V0.2.2 **新增**功能（P1–P6）。V0.2.1 及之前的基础功能（浮窗折叠/拖动、快捷键、空闲检测、失真检测等）不在本清单范围。
>
> 执行前确保 `npm run tauri dev` 正常运行，浮窗可见。

---

## 1. 主窗重构（P3）— 路由 + 侧边栏

- [ ] 启动后主窗显示侧边栏（图标导航：复盘 / 管理 / 设置）
- [ ] 侧边栏底部有「设计系统」入口（开发工具）
- [ ] 侧边栏导航切换正常：复盘 → 管理 → 设置
- [ ]  `/style-guide` 路由独立，不显示侧边栏
- [ ] 默认首页 `/` 显示复盘视图

## 2. 复盘视图（P2）— ReviewPage

- [ ] 复盘页面加载后显示四个面板：今日完成 / 进行中 / 待办分布 / 未覆盖时段
- [ ] 今日完成面板：列出已归档任务，显示每次 focus 时长
- [ ] 进行中面板：列出当前 active 任务
- [ ] 待办分布：显示各时段 focus 量（柱状或列表）
- [ ] 未覆盖时段：列出当天 gap（无记录的时间段）
- [ ] 点击 gap 的「关联」按钮 → 调用 `api.review.associateGap`，gap 消失
- [ ] 数据刷新：切换 tab 回来后数据正确

## 3. 管理页（P4）— ManagePage

- [ ] 管理页显示 tabs：进行中 / 待办 / 归档 / 回收站
- [ ] 搜索框：输入关键词实时过滤列表
- [ ] 状态筛选：切换 tab 只显示对应状态的条目
- [ ] 软删除：点击删除 → Toast 出现「撤销」按钮（5秒窗口）
- [ ] 撤销：5秒内点击撤销 → 任务恢复原状
- [ ] 硬删除：回收站中点击「彻底删除」→ 条目消失不可恢复
- [ ] 回收站：显示 `deleted_at IS NOT NULL` 的条目，可撤销或硬删除
- [ ] 编辑：点击条目标题进入编辑模式，保存后内容更新

## 4. 前台活动信号（P5）— foreground.rs + 浮窗强化

- [ ] 设置页开启「活动监听」开关
- [ ] 切换到其他应用（如记事本）→ 等待约 30s → 浮窗显示呼吸动画建议文案
- [ ] 文案正确：「看起来你在做「xxx」，要记一下吗？」
- [ ] 点击「记一笔」→ 创建任务并开始计时，浮窗恢复正常
- [ ] 点击「忽略」→ 建议文案消失，当前信号不再提示
- [ ] 切换另一个不同应用 → 新信号正常弹出（忽略不会永久阻塞）
- [ ] 关闭「活动监听」开关 → 不再弹出建议

## 5. 设置页（P6）— SettingsPage

- [ ] 设置页显示：活动监听开关、通知权限、时区偏移
- [ ] 活动监听开关切换：开启/关闭，重新启动后保持设置
- [ ] 时区偏移显示：显示当前时区 UTC 偏移（如 UTC+8）
- [ ] 通知权限：首次启动尝试请求通知权限（无弹窗时静默降级）

## 6. 回归验证（仅关键路径）

- [ ] `npm test`（vitest）：111 passed，24 files
- [ ] `cargo test`：66 passed
- [ ] `npx tsc --noEmit`：无错误
- [ ] `npm run build`（纯前端）：无错误