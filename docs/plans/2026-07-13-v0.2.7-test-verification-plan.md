# V0.2.7 浮窗 5 bug 测试验证计划

> **For agentic workers:** 本计划是「验证 (verification)」而非「实施 (implementation)」—— fix 已在 commit `04c89da` 完成, 计划目的是**穷举证明 fix 真生效**, 同时执行反模式 15/16 防御(commit claim grep + 行为断言, 不能只信字面包含)。

**Goal:** 用 3 层验证 (L1 vitest 自动化 + L2 源码 grep + L3 Windows D:\ 实测) 穷举确认 V0.2.7 5 bug fix 真根因已生效, 落地反模式 14/15/16 防御标准。

**Architecture:** 不重写 fix, 只验证。每 bug 平行 3 验证(自动化可重复执行那 1 层 / 源码静态检查 1 层 / Windows manual 1 层), 任一 PASS 不算修好——必须全 PASS。

**Tech Stack:** vitest 2.x + @testing-library/react 16.x + Tauri 2 IPC mock + WebView2 (Windows-only manual) + bash / PowerShell

---

## 〇、前置阅读 (必读, 6 件 artifact)

| # | 文档 | 角色 |
|---|---|---|
| 1 | 本文件 | 验证计划 |
| 2 | [`docs/specs/2026-07-11-v0.2.0-floating-window-design.md`](../specs/2026-07-11-v0.2.0-floating-window-design.md) | spec v1.6 (本次同步) |
| 3 | [`src/floating/App.fix.test.tsx`](../../src/floating/App.fix.test.tsx) | 10 组 V0.2.7 新增 fix 测试 (L1 主测试) |
| 4 | [`src/floating/App.test.tsx`](../../src/floating/App.test.tsx) | 27 反复犯回归测试 (L1 旧测试, 不应被本次 fix 打破) |
| 5 | [`docs/reports/2026-07-12-v0.2.7-windows-qa-v8.md`](../reports/2026-07-12-v0.2.7-windows-qa-v8.md) | Windows D:\ 实测模板 (L3 主模板) |
| 6 | [`docs/reports/2026-07-12-v0.2.7-33-commit-audit.md`](../reports/2026-07-12-v0.2.7-33-commit-audit.md) (待生成) | 33 commit 逐行回查 (反模式 14/15 防御复盘) |

---

## 一、L1 自动化验证 (WSL 单跑, 阻塞前置门禁)

> 这 5 步全部 PASS 才能进 L3 Windows D:\ 实测。任何 1 个 FAIL → **立即停止**, 走 §五失败 fallback。

### 1.0 L1 全局环境自检

**Files**: 不改文件, 只跑命令

- [ ] **Step 1: 确认当前分支与 HEAD**

```bash
cd /home/jason/workspace/mindtap
git status -sb && git log --oneline -1
```

**预期输出**:
```
## develop...origin/develop
15356cd merge: V0.2.7 floating 5 bug fix into develop (from worktree-fix-floating-v0.2.7-bugs)
```

(若不是 → 重新走 `mindtap-dual-worktree-sync` 的 stash+pull+unstash 流程)

- [ ] **Step 2: 工作树干净**

```bash
git status --porcelain
```

**预期**: 0 行输出(否则有未 commit, 不能保证 fix 真的在)

---

### 1.1 Bug 1 — act() try/catch 闪退真根因

**Files**: 
- Fix: `src/floating/App.tsx:239-247`
- Test: `src/floating/App.fix.test.tsx:15-32` (Bug 1 describe 块, 2 个 it)

- [ ] **Step 1: 跑 Bug 1 全 describe 块**

```bash
cd /home/jason/workspace/mindtap
npx vitest run src/floating/App.fix.test.tsx -t "Bug 1: act" --reporter=verbose
```

**预期输出**(2 个 PASS, 0 FAIL):
```
✓ V0.2.7 patch — Bug 1: act() 函数必须 try/catch 包裹 await (闪退真根因) > App.tsx act() 函数体内含 try { ... } catch (...) 块
✓ V0.2.7 patch — Bug 1: act() 函数必须 try/catch 包裹 await (闪退真根因) > App.tsx act() catch 块必须调 console.error (错误可见性, 反模式 13 防御标准)

Test Files  1 passed (1)
     Tests  2 passed (2)
```

- [ ] **Step 2: 失败判定 (反模式 16 防御)**

若 FAIL 但测试只是"正则没匹配" 而手动 grep `src/floating/App.tsx` 真有 try/catch → **测试断言写错了, 不是代码错了** → 停下改测试断言, 不动代码。

---

### 1.2 Bug 2 — `win.startDragging()` IPC 拖动真根因

**Files**:
- Fix: `src/floating/App.tsx:147-184` (handleMouseDown/onMouseMove)
- Test: `src/floating/App.fix.test.tsx:34-52`

- [ ] **Step 1: 跑 Bug 2 测试**

```bash
cd /home/jason/workspace/mindtap
npx vitest run src/floating/App.fix.test.tsx -t "Bug 2: onMouseMove" --reporter=verbose
```

**预期**:
```
✓ App.tsx 拖动阈值满足后必须调 win.startDragging() IPC
✓ App.tsx getCurrentWindow() 解构/调用必须含 startDragging (前端调 IPC 的前提)
Test Files  1 passed (1)
     Tests  2 passed (2)
```

⚠️ **关键 (反模式 15 防御)**:V0.2.5/V0.2.6 反复"声称修了拖动"但 App.tsx 没调 `startDragging()`。测试只是 grep 源码匹配 `win\.startDragging\s*\(\s*\)` —— **如果 fail 但实际源码 grep 命中 → 测试断言写错; 如果源码 grep 也不命中 → 代码真没修 → 重写 App.tsx**。两者必居其一, 不接受"测试改松一点"。

---

### 1.3 Bug 3 — tauri.conf.json 删硬编码 x/y

**Files**:
- Fix: `src-tauri/tauri.conf.json` (floating 段)
- Test: `src/floating/App.fix.test.tsx:54-68`

- [ ] **Step 1: 跑 Bug 3 测试**

```bash
cd /home/jason/workspace/mindtap
npx vitest run src/floating/App.fix.test.tsx -t "Bug 3: tauri.conf" --reporter=verbose
```

**预期**:
```
✓ tauri.conf.json floating 段不含硬编码 x 字段 (V0.2.7 修 V0.2.6 final fix 谎改)
✓ tauri.conf.json floating 段不含硬编码 y 字段 (V0.2.7 修 V0.2.6 final fix 谎改)
Test Files  1 passed (1)
     Tests  2 passed (2)
```

- [ ] **Step 2: L2 增强 grep(独立交叉验证)**

```bash
cd /home/jason/workspace/mindtap
grep -nE '^\s*"(x|y)":\s*-?[0-9]' src-tauri/tauri.conf.json
```

**预期**: 0 行输出(允许 main 段有 x/y 但 floating 段必须没有)

---

### 1.4 Bug 4 — resize useEffect cancelled 检查

**Files**:
- Fix: `src/floating/App.tsx:47-72`
- Test: `src/floating/App.fix.test.tsx:71-98`

- [ ] **Step 1: 跑 Bug 4 测试**

```bash
cd /home/jason/workspace/mindtap
npx vitest run src/floating/App.fix.test.tsx -t "Bug 4: resize useEffect" --reporter=verbose
```

**预期**:
```
✓ App.tsx resize useEffect [expanded] 内 cancelled flag 检查次数 >= 4 (开头 1 + 3 个 await 后)
✓ App.tsx resize useEffect 每个 await 后立即跟 cancelled 检查 (无空隙)
Test Files  1 passed (1)
     Tests  2 passed (2)
```

⚠️ **判定**:cancelled flag 必须 ≥ 4 处(开头 1 + 至少 3 个 await win.setSize/setPosition 后)。V0.2.5/V0.2.6 只在 useEffect 开头 1 处,cancelled 拉到 await 之后才补好。

---

### 1.5 Bug 5 — 展开 setPosition x 不偏移

**Files**:
- Fix: `src/floating/App.tsx`(展开分支 setPosition)
- Test: `src/floating/App.fix.test.tsx:100-114`

- [ ] **Step 1: 跑 Bug 5 测试**

```bash
cd /home/jason/workspace/mindtap
npx vitest run src/floating/App.fix.test.tsx -t "Bug 5: 展开" --reporter=verbose
```

**预期**:
```
✓ App.tsx 展开 setPosition 不再使用 Math.round((FOLDED_W - EXPANDED_W) / 2) 偏移公式
✓ App.tsx 展开 setPosition x 直接用 pos.x (不偏移)
Test Files  1 passed (1)
     Tests  2 passed (2)
```

- [ ] **Step 2: L2 grep 残留检查**

```bash
cd /home/jason/workspace/mindtap
grep -nE 'Math\.round\s*\(\s*\(\s*FOLDED_W\s*-' src/floating/App.tsx
```

**预期**: 0 行

- [ ] **Step 3: 验证展开分支 setPosition 调用形态**

```bash
cd /home/jason/workspace/mindtap
grep -n -A2 'await win\.setPosition' src/floating/App.tsx | head -20
```

**预期**:命中 `new PhysicalPosition(pos.x, pos.y)`(无 Math.round 偏移)

---

### 1.6 全套测试组合跑(L1 总门禁)

- [ ] **Step 1: 跑 vitest 全部 suite**

```bash
cd /home/jason/workspace/mindtap
npx vitest run --reporter=default 2>&1 | tail -30
```

**预期**:
```
Test Files  24 passed (24)
     Tests  113 passed (113)
   Duration  ~X.Xs
```

- [ ] **Step 2: 跑 tsc**

```bash
cd /home/jason/workspace/mindtap
npx tsc --noEmit
```

**预期**: 0 errors(允许 warning)

- [ ] **Step 3: 跑 cargo check**

```bash
cd /home/jason/workspace/mindtap/src-tauri
cargo check 2>&1 | tail -5
```

**预期**:`Finished dev profile [unoptimized + debuginfo] target(s) in X.Xs`

- [ ] **Step 4: 跑 cargo clippy (反模式 15 防御扩展)**

```bash
cd /home/jason/workspace/mindtap/src-tauri
cargo clippy --all-targets 2>&1 | tail -10
```

**预期**:`Finished` + 0 warning(允许 1 个已存 dead_code)

> ⚠️ L1 全部 PASS 才进 L2。**任 1 FAIL → 立即停止, 不许"先 commit 再修"**。

---

## 二、L2 源码反模式 15 防御 (静态 grep, WSL 单跑)

> 这一层的目的是穷举证明 commit message 没撒谎。V0.2.5/V0.2.6 反复修都漏改的真根因之一就是 commit claim grep 不到代码。

### 2.0 反模式 15 全量 grep

**Files**: 不改, 只跑 grep

- [ ] **Step 1: V0.2.7 5 claim 全量 grep**

```bash
cd /home/jason/workspace/mindtap

echo "=== Claim 1: act() try/catch ==="
grep -nE 'async function act\(.*\)\s*\{' src/floating/App.tsx
grep -nE 'try\s*\{' src/floating/App.tsx | head -5
grep -nE 'catch\s*\(' src/floating/App.tsx | head -5

echo "=== Claim 2: win.startDragging() IPC ==="
grep -nE 'win\.startDragging\s*\(' src/floating/App.tsx

echo "=== Claim 3: tauri.conf 无硬编码 x/y ==="
grep -nE '^\s*"(x|y)":\s*-?[0-9]+' src-tauri/tauri.conf.json

echo "=== Claim 4: cancelled flag >= 4 处 ==="
grep -cE 'if\s*\(\s*cancelled\s*\)\s*return' src/floating/App.tsx

echo "=== Claim 5: 展开 setPosition 不偏移 ==="
grep -nE 'Math\.round\s*\(\s*\(\s*FOLDED_W' src/floating/App.tsx && echo "FAIL: 公式还在" || echo "OK: 公式已删"
grep -nE 'new\s+PhysicalPosition\s*\(\s*pos\.x\s*,\s*pos\.y\s*\)' src/floating/App.tsx
```

**预期**:
- Claim 1: 命中 `async function act`, 至少 1 个 `try {`, 至少 1 个 `catch (`
- Claim 2: 命中 `win.startDragging(...)` 至少 1 行
- Claim 3: 0 命中(floating 段无 x/y)
- Claim 4: `if (cancelled) return` 计数 `>= 4`
- Claim 5: 第一条 grep 输出 `FAIL: 公式还在`, 第二条命中 1 行 `new PhysicalPosition(pos.x, pos.y)`

> 任 1 不符合预期 → commit message 与代码不一致, **该 claim 没真修**, 回 Phase 4 重修。

- [ ] **Step 2: 反模式 16 防御——重新审 App.test.tsx:115-122**

```bash
cd /home/jason/workspace/mindtap
sed -n '115,122p' src/floating/App.test.tsx
```

**预期**:
```typescript
  it("折叠态拖动超 4px 不触发展开 (dragRef dragStarted=true 后 mouseup 保持折叠)", async () => {
    render(<FloatingApp />);
    const foldedRoot = await screen.findByTestId("floating-root-folded");
    fireEvent.mouseDown(foldedRoot, { clientX: 10, clientY: 10 });
    fireEvent.mouseMove(foldedRoot, { clientX: 20, clientY: 15 });
    fireEvent.mouseUp(foldedRoot, { clientX: 20, clientY: 15 });
    expect(document.querySelector(".floating-root.expanded")).toBeNull();
  });
```

> ⚠️ **如果这测仍在用字面断言而非 startDragging 调用断言** → 视为反模式 16 复发, **必须**升级为行为断言:
> ```typescript
> expect(win.startDragging).toHaveBeenCalledOnce();  // 行为断言
> ```
> V0.2.5/V0.2.6 反复修都漏改的真根因之一——光测"不展开"是不够的。

---

## 三、L3 Windows D:\ 真机实测 (用户亲跑, 不可代理)

> 这一层是反模式 13(Win 11 WebView2 panic / 透明窗口 / OS 拖窗 / 多屏) 的唯一真验证。playwright + webdriver 在 WSL 跑不了 Tauri 窗口 API, **必须 D:\ 端用户亲跑**。

### 3.0 Windows 端前置 (Step 1, 单源确认)

- [ ] **Step 1: 确认 D:\ 已 sync 到 15356cd**

```powershell
cd D:\workspace\mindtap
git log --oneline -1
```

**预期**:`15356cd merge: V0.2.7 floating 5 bug fix into develop`

- [ ] **Step 2: 启动 Tauri dev**

```powershell
cd D:\workspace\mindtap
.\scripts\dev.bat
```

预期:
- dev.ps1 拒绝在 WSL 跑(在本机 Windows PowerShell 不触发)
- 命中项目根
- cargo tauri 优先, 清 1420/1421 端口
- WebView2 窗口弹出主窗 + 浮窗

- [ ] **Step 3: 触发浮窗**

按 Ctrl+Shift+Space(或右键主窗托盘) 显示浮窗, 进入"折叠态"

---

### 3.1 Bug 3 真实测 — 浮窗初始位置不硬编码

- [ ] **Step 1: 单屏 1920×1080 启动 → 测浮窗初始位置**

预期:
- 浮窗出现在屏幕**右上角 -16px**(实际坐标约 `1584, 16`)
- 不在 `1500, 920`(那是 V0.2.6 漏删的硬编码, 应不出现)

- [ ] **Step 2: 副屏 1024×768 接上 → 重启应用**

预期: 浮窗出现在**主屏**右上 -16px(不丢到副屏坐标)

- [ ] **Step 3: 4K 屏缩放 150% → 重启应用**

预期: 浮窗可见, 不消失

**Pass 判定**: 3 个屏幕环境都看得见浮窗。**任 1 看不见 → Bug 3 没修好, 回 Phase 4**。

### 3.2 Bug 2 真实测 — 折叠态拖动 4px

- [ ] **Step 1: 折叠态 → 按住浮窗 → 拖动 ≥ 5px → 松开**

预期:
- 拖动过程中窗口**跟随鼠标**(OS 拖窗生效)
- 松开后**保持新位置**(不弹回原位)
- 浮窗**保持折叠**(不展开)

- [ ] **Step 2: 重启应用 → 浮窗在拖动后的位置(onMoved + localStorage)**

- [ ] **Step 3: 反模式 15 复发检测**

若拖动 5px 窗口**不动** → V0.2.7 Bug 2 修复**失败, 反模式 15 复发** → **❌ STOP, 看 §五失败 fallback**

### 3.3 Bug 5 真实测 — 展开位置不偏移

- [ ] **Step 1: 把浮窗拖到主屏右上角 `~1584, ~80`**

- [ ] **Step 2: 点击浮窗 → 展开**

预期:
- 浮窗展开后 x 仍 ≈ `1584`(V0.2.7 修)
- ❌ V0.2.6 行为复发 = x ≈ `1564`(偏移 -20px), 右边溢出 4px → **判定为 Bug 5 未修**

### 3.4 Bug 1 真实测 — IPC 失败不闪退 (难手动测, 选一方式)

> 需故意触发 invoke reject。3 选 1:

- [ ] **方式 A: 高频点击竞态**

反复快速点击折叠态触发 pause/resume/complete 30 次/秒 × 3 秒

预期: 浮窗**不闪退**, 控制台(右键 → Inspect Element)可见 `[act:pause] failed` 类错误日志

- [ ] **方式 B: 锁定数据库**

另起 PowerShell:
```powershell
$file = "$env:APPDATA\com.mindtap.desktop\projects.db"
icacls $file /deny "$env:USERNAME:(R)"  # 加读锁
```
然后在浮窗内触发 pause/resume, 完成后再 `icacls $file /reset`

预期: 浮窗不闪退, 显示错误(IPC reject 后 catch 住)

- [ ] **方式 C: DevTools 模拟**

按 F12 / 右键 Inspect, 在 Console 注入:
```javascript
window.__TAURI_INTERNALS__.invoke = () => Promise.reject(new Error("simulated"));
```
然后触发 pause

预期: 浮窗不闪退, WebView 仍可展开/折叠/输入

**Pass 判定**: 3 方式任 1 浮窗仍存活 + 有控制台报错日志 = Bug 1 修好

### 3.5 Bug 4 真实测 — resize race 取消

- [ ] **Step 1: 反复快速点击(1 秒内 ≥ 5 次)折叠态根 div**

预期:
- 浮窗**最终状态稳定**(不卡在"已折叠但尺寸是 360×280"或反之)
- 最终尺寸 = 当前 expanded 状态对应的尺寸(折叠 320×36 / 展开 360×280)

---

### 3.6 v8 QA 全套补测 (沿用历史)

> 5 个新 bug 之外的旧 18 GUI + 5 P0 项继续测, 见 [`docs/reports/2026-07-12-v0.2.7-windows-qa-v8.md` §六](../reports/2026-07-12-v0.2.7-windows-qa-v8.md#六沿用-v7-模板-step-4--18-gui--5-p0-项-继续测) 沿用 v7 模板部分

- [ ] 不抢焦实测(IDE 焦点 → Ctrl+Shift+Space → IDE 仍可输入)
- [ ] 1px 黑边消除
- [ ] 呼吸灯 animate-pulse-dot
- [ ] setFocusable 严禁(Win11 WebView2 panic 防御)
- [ ] contextmenu 原生 listener(capture phase)
- [ ] 其他 13 个 GUI 项按 v8 §六 checklist 跑

---

## 四、L3 实测数据回填 (Step 6, 用户亲填)

完成 §三 全部检查后, 用户把数据写回 [`docs/reports/2026-07-12-v0.2.7-windows-qa-v8.md` §八 最终交付清单](../reports/2026-07-12-v0.2.7-windows-qa-v8.md#八最终交付清单-step-5):

- [ ] **填写所有 ✅ / ❌** (3.1 - 3.5 + 3.6 每一项)
- [ ] **commit 实测报告**(D:\ 端, 不允许 amend V0.2.7 任何 commit):

```powershell
cd D:\workspace\mindtap
git add docs/reports/2026-07-12-v0.2.7-windows-qa-v8.md
git commit -m "test(win-qa): V0.2.7 v8 实测报告 — 5 P0 修复验证 (L3 数据)"
git push origin develop
```

- [ ] **同步回 WSL**:

```bash
git fetch origin
git -C /home/jason/workspace/mindtap reset --hard origin/develop
```

---

## 五、失败 Fallback (任一层 FAIL 时必读)

### 5.1 L1 (vitest / tsc / cargo) FAIL

**症状**: 测试 FAIL / tsc 出 error / cargo 出 error

**禁止操作**:
- ❌ "改测试让其通过"
- ❌ "skip 这个 bug 先 commit 其他的"
- ❌ "先 commit 跑的测试, 回头修"

**正确流程**:
1. 看 error message 找根因
2. 若 fix 写错 → 回 Phase 4 重写 fix
3. 若测试断言写错(很常见, 反模式 16) → 修测试断言但保留行为意图
4. 重跑直到 PASS
5. amend 本次 V0.2.7 fix commit 或新加 fix commit(看 commit-style rule)

### 5.2 L2 (反模式 15 grep) FAIL

**症状**: grep 命中了 V0.2.7 不该有 / 不该不命中 的代码

**判定标准**:
- Claim 2 (startDragging) 漏 grep → **代码真没修, 是 commit message 谎改** → 回 Phase 4 重写 App.tsx:147-184
- Claim 4 (cancelled 检查数 < 4) → 代码漏检查一处 → 补全
- Claim 5 (setPosition 公式仍在) → 代码漏改 → 删公式

**禁止**:
- ❌ 删测试断言
- ❌ "先 commit 再修"

### 5.3 L3 (Windows D:\ 实测) FAIL

**症状**: §3.1-3.5 任一 ❌

**判定**:
- Bug 2 (拖动窗口不动) FAIL → 反模式 15 复发, V0.2.7 fix 未生效 → 回 Phase 4 重查 startDragging IPC 调用链(可能 mock 没生效, 可能 App.tsx 仍漏掉, 可能 getCurrentWindow() 取错)
- Bug 1 (闪退) FAIL → try/catch 没生效, 检查控制台 catch 块实际是否触发
- Bug 3 (多屏错位) FAIL → tauri.conf.json x/y 仍在, 重 grep
- Bug 4 (race) FAIL → useEffect cancelled 检查数仍不够
- Bug 5 (偏移) FAIL → setPosition 公式仍在, 重 grep

**禁止**:
- ❌ 改 D:\ 端源码做"本机 hotfix"(违反 CLAUDE.md 单源真值 = origin/develop)
- ❌ amend 任何 V0.2.7 commit

**正确流程**: 停所有工作 → 在 WSL 工作树修代码 → 走 §六补 commit → push → D:\ pull 重测

---

## 六、收尾 (PASS 后)

- [ ] **补 commit(如需)**: 如有 §五失败 fallback 触发的重 fix, 用 1 个新 commit:

```bash
cd /home/jason/workspace/mindtap
# 视情况 add 受影响文件 (以下为最常见情形):
git add src/floating/App.tsx
git add src/floating/App.fix.test.tsx  # 若改了测试断言
git add docs/reports/2026-07-12-v0.2.7-windows-qa-v8.md  # 若 L3 回填了新数据
git commit -m "fix(floating): V0.2.7 patch re-verify — {具体修哪个, e.g. 'Bug 4 cancelled check 补一处'}"
git push origin develop
```

- [ ] **更新 v0.2.7-final-fix-retrospective.md**(若尚未生成):

参考反模式沉淀:
- 反模式 14: 反复修最多次仍未修好 (cancelled check / try/catch)
- 反模式 15: commit message 谎改 (拖动 claim 必须 grep 命中)
- 反模式 16: 测试断言只覆盖字面 (升级 startDragging 为行为断言)

写到 `docs/reports/v0.2.7-final-fix-retrospective.md`

- [ ] **draft PR**: 通过 web 手动建 → https://github.com/ahajason/mindtap/compare/develop...worktree-fix-floating-v0.2.7-bugs

PR title: `fix(floating): V0.2.7 patch — 5 bug 真根因 (act try/catch + startDragging + tauri.conf x/y + cancelled + 偏移)`

PR body 引用本计划文件 path:`docs/plans/2026-07-13-v0.2.7-test-verification-plan.md`

- [ ] **memory 更新**: V0.2.7 final fix 完成, 加新 memory `mindtap-v0.2.7-final-fix-status.md`(若尚未加), 链接到 [[mindtap-v0.2.7-anti-patterns]]

---

## 七、与 V5 / 33 commit 的位置关系

本计划**只验证 V0.2.7 5 bug**(commit `04c89da` + `17a257c` + `50c9329`)。V0.2.6 之前的回归见:

| 版本 | plan / retro | 状态 |
|---|---|---|
| V0.2.3-v0.2.6 反复修 | `docs/reports/v0.2.6-final-fix-retrospective.md` + `v0.2.6-33-commit-line-by-line-audit.md` | 已沉淀 8 反模式 |
| V0.2.7 (本次) 5 bug | **本计划** | 走完 PASS 后再写 V0.2.7-final-fix-retro |

---

## 八、相关 memory

- [[mindtap-dual-worktree-sync]] — D:\ 同步流程
- [[mindtap-v0.2.7-anti-patterns]] — 反模式 14/15/16 沉淀
- [[vite-tauri-multi-entry-path-gotcha]] — dev 时多入口路径坑
