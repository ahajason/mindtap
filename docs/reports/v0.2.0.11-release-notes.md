# V0.2.0.11 PATCH — 浮窗 4 issue 收口 (A ContextMenu + B StatusDot + C transparent + D 移除 transition)

> 发版日期: 2026-07-13
> 任务档: [`docs/tasks/v0.2.0.11-fix-floating-4-issues/task.md`](../tasks/v0.2.0.11-fix-floating-4-issues/task.md)

## 用户视角摘要(5 行)

1. **右键折叠态能弹 ContextMenu**(显示主窗 / 退出);之前 `top:-84` 菜单跑到 viewport 外看不见
2. **呼吸灯(状态点)在浮窗右上角内**,不再穿透到 viewport 角落错位 4px
3. **黑边(focus 后也)消失**:`tauri.conf.json` floating 段开启 `transparent: true` + 显式 `backgroundColor: "#00000000"`,WebView2 半透明合成不再出灰色描边
4. **浮窗不再有 transition 误匹配**:`prefers-reduced-motion` 媒体查询移除,即使系统偏好 reduce-motion 也不会误关(本来就没动画)
5. D:\ 端需 Ctrl+C 重启 `scripts\dev.bat` 才能看到 C transparent 效果(WebView2 透明需重启)

## 技术摘要(开发者)

### A — ContextMenu 定位 + button guard + 展开态渲染

- `ContextMenu.tsx`:加 `clampToViewport()` 函数,`MENU_W=144 / MENU_H=80 / VIEWPORT_MARGIN=4` 常数,做 viewport 双向 clamp(`Math.max(VIEWPORT_MARGIN, Math.min(x, maxLeft))` 同理 top)
- `App.tsx`:`onContextMenuCapture` 加 `if (e.button !== 2) return;` 守卫(左/中键不弹菜单)
- `App.tsx`:ContextMenu 渲染从折叠态 JSX 移到外层 Fragment(折叠 + 展开态共用)

### B — `.floating-root` 加 `position: relative`

- `floating.css` `.floating-root` 块加 `position: relative;` 作为首条声明,让 StatusDot `absolute top-1 right-1` 找根 div 当祖先
- `StatusDot.tsx` 注释纠错:V0.2.0.7 改 `top-1 right-1` 是必要非充分,真正 fix 是根 div 加 relative

### C — tauri.conf.json floating transparent + backgroundColor

- `tauri.conf.json` floating 段:`transparent: false` → `true` + 加 `backgroundColor: "#00000000"`
- `App.test.tsx` V0.2.6 旧反向 lock-in describe(`transparent:false`)替换为 V0.2.0.11 锁住(`transparent:true` + `backgroundColor` + `resizable:true` 保留)

### D — 移除 transition + prefers-reduced-motion

- `floating.css`:删 `.floating-root` 块 `transition: width 200ms ease-out, ...` 行 + 整个 `@media (prefers-reduced-motion: reduce) { .floating-root { transition: none } }` 块
- `App.fix.test.tsx`:删 V0.2.0.9 Bug 5 cosmetic describe(4 it)+ 加 V0.2.0.11 D 锁住(3 it)

## 修复路径

- V0.2.0.6 ~ V0.2.0.10 五个 PATCH 都涉及浮窗,user L3 重测发现 4 issue 都没真修(commit 谎改 + 反复修错地方)
- V0.2.0.11 穷举所有真根因 + 一次收口:
  - A: ContextMenu 单向 clamp → 双向 clamp + button guard + 展开态 JSX
  - B: StatusDot 类名 → 根 div 加 relative(JSX tree 上一层)
  - C: `.floating-root` CSS → tauri.conf.json transparent + backgroundColor(Rust / 配置层)
  - D: 加 transition → 整体移除(不在 scope + @media 误匹配)
- 反模式 14(反复修)+ 15(commit 谎改)+ 16(测试字面断言)+ 18(CSS regex @media 嵌套)全部触发并防御

## 跨 task 依赖

- **阻塞 V0.2.0.6 ~ V0.2.0.10 retro 收口**: V0.2.0.11 是这五个 PATCH 漏掉真根因的统一收口
- **影响 V0.2.0-retrospective §二 Bug 表**: A/B/C 行从"未完全修好"更新为"V0.2.0.11 修复";D 行从"待重测"更新为"V0.2.0.11 不在 scope 已移除"

## 关联

- [`docs/governance/versioning-rule.md`](../governance/versioning-rule.md) §三 + §四 + §六
- V0.2.0.6 A: [`../tasks/v0.2.0.6-fix-contextmenu-right-click/task.md`](../tasks/v0.2.0.6-fix-contextmenu-right-click/task.md)
- V0.2.0.7 B: [`../tasks/v0.2.0.7-fix-statusdot-displacement/task.md`](../tasks/v0.2.0.7-fix-statusdot-displacement/task.md)
- V0.2.0.8 C: [`../tasks/v0.2.0.8-fix-black-border/task.md`](../tasks/v0.2.0.8-fix-black-border/task.md)
- V0.2.0.9 D: [`../tasks/v0.2.0.9-fix-expanded-asymmetry/task.md`](../tasks/v0.2.0.9-fix-expanded-asymmetry/task.md)
- V0.2.0.10 C 二次根因: [`../tasks/v0.2.0.10-fix-foldedbar-inline-shadow/task.md`](../tasks/v0.2.0.10-fix-foldedbar-inline-shadow/task.md)
- V0.2.0.11 任务档: [`../tasks/v0.2.0.11-fix-floating-4-issues/task.md`](../tasks/v0.2.0.11-fix-floating-4-issues/task.md)
- L3 findings 来源: [`2026-07-13-v0.2.7-l3-findings.md`](2026-07-13-v0.2.7-l3-findings.md)
- V0.2.5 commit 谎改审计: [`v0.2.6-33-commit-line-by-line-audit.md`](v0.2.6-33-commit-line-by-line-audit.md)