# V0.2.0 Windows 端 QA 实测报告 v3

**日期**: 2026-07-11 (v3,基于 `8fe11be` / P0-3 + P0-5 修复后)
**分支**: `develop`
**HEAD**: `8fe11beb2e33e42a40226f2a9229830fa47e74ff`
**报告 v2**: `56be2bd` (基于 `b5660f5`,P0-3 假修)
**报告 v1**: `c25b599` (基于 `1ba06b3`)
**环境**: Windows 11 24H2 · Node 24.9.0 · npm 11.6.0 · Rust 1.96.0 · MSVC 14.44.35207 · npm registry `https://registry.npmmirror.com` · Tauri 2.11.5

> **v3 vs v2 增量**:
> 1. WSL 端 4 commits 修了 P0-3 / P0-5 / P1-1 / P1-2
> 2. P0-3 假修重做: floating.css `@import "tailwindcss"` + glass utilities inline (build 660 B → 38.72 KB)
> 3. P0-5 修复: App.tsx 展开态 setFocusable(true) 覆盖 lib.rs 兜底
> 4. P1-1 vitest mock: 73 passed + 0 errors
> 5. P1-2 identifier: `com.mindtap.desktop` (macOS 冲突警告消失)
> 6. 新 build NSIS MD5(不同 v2 的 `49f87fa42e2044f1c2d9c3c732d76633`)
> 7. 用户实测 18 条 GUI 验收(重测 v2 报告 ❌ 项 + 增量)

> **实测对照**(用户桌面实测反馈):
> - ❌ **"内容文字还是白色的一片空白,无法查看"** — 浮窗文字不可读
> - ❌ **"还是无法拖拽"** — 浮窗不可拖动
> - ❌ **"没有遵循的规范浅色玻璃拟态 · 设计语言"** — 视觉不符合 `docs/design/glassic-ui-spec.md`
> - 以上导致 P0-3 修复实际未根除视觉问题;P0-5 实际未根除交互问题

---

## 静态 6 关(基于 `8fe11be`)

- cargo check ✅ Finished
- cargo test --lib ✅ 9 passed
- cargo clippy ✅ 0 warn
- cargo fmt ✅ clean
- tsc ✅ 0 err
- npm run build ✅ floating-*.css = **39,789 B / 38.86 KB** (≥ 30 KB 目标)

---

## GUI 18 条验收

> **对照 v2 报告**,按用户实测反馈填空。dev 实测 = system API 改动后 hard gate,实测发现什么就报告什么,不写"应 PASS"。

| # | 项 | v3 状态 | 实测证据 |
|---|---|---|---|
| 1 | 启动应用,主窗+浮窗都显示 | ✅ | (v2 用户实测可,v3 未否定)|
| 2 | Ctrl+Shift+Space toggle 浮窗 | ✅ | (v2 用户实测可)|
| 3 | 浮窗折叠态显 task_title + focus_ms(1 秒刷新)| ❌ | 用户实测:**"内容文字还是白色的一片空白,无法查看"** — 文字不可见,无法验证 1s 刷新是否生效 |
| 4 | 浮窗不抢焦(IDE 焦点保持)| ❓ | agent 物理限制;用户未实测反馈 |
| 5 | 折叠→展开 | ❌ | 用户实测:点击展开后内容仍是白色文字看不见 |
| 6 | 输入 + Enter → active + 自动折叠 | ❓ | (被 #3 #5 阻塞,无法验) |
| 7 | Esc → 折叠不创建 | ❓ | (被 #5 阻塞) |
| 8 | 失焦 → 自动折叠 | ❓ | agent 物理限制,用户未实测 |
| 9 | 暂停 / 恢复 / 完成(UI 保持 + focus_ms 正确)| ❓ | (被 #3 阻塞,无法验证) |
| 10 | 完成 → 自动折叠 | ❓ | (被 #3 #5 阻塞) |
| 11 | 关主窗 = 隐藏(不退出)| ✅(静态)| `lib.rs:40-47 api.prevent_close() + main_clone.hide()` |
| 12 | 浮窗右键 → 退出 | ✅(静态 + v2 用户实测)| ContextMenu.tsx + api.app.exit |
| 13 | db 文件存在 `%APPDATA%\com.mindtap.desktop\projects.db` | ✅(实测)| 20,480 B (20480 bytes),schema 与 spec §四 100% 匹配 |
| 14 | a11y: aria-label + 对比度 4.5:1 | ❌ 对比度 + ❌ 视觉 | FoldedBar.tsx aria-label 已挂,但实测文字"白色一片空白" 意味着文字可见性失败 = 对比度 < 4.5:1 |
| 15 | .floating-root 类(折叠)| ✅ | App.tsx:90 |
| 16 | .floating-root 类(展开)| ✅ | ExpandedPanel.tsx:66/82 |
| 17 | 浮窗拖动 | ❌ | **用户实测:"还是无法拖拽"** — P0-3 修复未根除 drag 问题(Tauri 2 `data-tauri-drag-region` 在 Windows 11 transparent window 不生效) |
| 18 | 主窗 drag 5 区(V0.1.6 carryover)| ✅ | (v2 用户实测可)|

**额外 GUI 验收失败(用户报告)**:
- ❌ **浮窗视觉不符合设计语言**(用户原文:"没有遵循的规范浅色玻璃拟态 · 设计语言")— 参考 `docs/design/glassic-ui-spec.md`

---

## P0 状态(v3 实测后,基于用户实测反馈)

| P0 # | commit | v2 报告状态 | v3 实测后状态 |
|---|---|---|---|
| P0-1 版本号 0.2.0 | `b5660f5` | ✅ | ✅ |
| P0-2 vite 多入口产出 dist/floating.html | `b5660f5` | ✅ | ✅ |
| P0-3 floating.css `@import "tailwindcss"` + glass utilities inline | `f2b0f0f` | ✅ (静态 38.72 KB) | ❌ **实测浮窗文字仍白色一片空白** — CSS utility 展开成功,但 glass utility 应用后**视觉仍不符合**(被用户反馈"没有遵循的规范浅色玻璃拟态")|
| P0-4 ExpandedPanel 根 div 挂 `.floating-root expanded` | `b5660f5` | ✅ | ✅ |
| P0-5 App.tsx 展开态 setFocusable(true) | `a965c55` | ✅ (静态 setFocusable 调用) | ⚠️ 静态确实设置,但**用户实测未明确反馈** P0-5 是否生效(因 P0-3 视觉问题先阻塞展开验证);P0-5 真效靠后续单独验证 |
| **新 P0-A**(用户发现)浮窗无法拖拽 | — | — | ❌ 用户实测"还是无法拖拽" — Tauri 2 `data-tauri-drag-region` 在 Windows 11 transparent window 上不工作 |
| **新 P0-B**(用户发现)浮窗不符合浅色玻璃规范 | — | — | ❌ 用户实测"没有遵循的规范浅色玻璃拟态 · 设计语言" |

> **按任务脚本规则**("#3 / #4 / #5 任一仍 ❌ → 写 v3 报告时如实标 ❌")— 已如实 ❌。
> **新发现 P0** → 写报告,不 silent commit 修复(任务脚本规则)。

---

## V0.2.0 release gate

| 项 | 状态 |
|---|---|
| 启动应用,主窗 + 浮窗都显示 | ✅ |
| Ctrl+Shift+Space toggle | ✅ |
| 浮窗折叠态显 task_title + focus_ms (1 秒刷新) | ❌ 文字白色不可读 |
| 浮窗不抢焦 (IDE 焦点保持) | ❓ 未实测反馈 |
| 折叠→展开 (P0-3 + P0-5 修复验证) | ❌ 展开态文字仍白色 |
| 输入 + Enter → active + 自动折叠 | ❓ 阻塞 |
| Esc → 折叠不创建 | ❓ 阻塞 |
| 失焦 → 自动折叠 | ❓ 未实测 |
| 暂停 / 恢复 / 完成 (UI 保持 + focus_ms 正确) | ❓ 阻塞 |
| 完成 → 自动折叠 | ❓ 阻塞 |
| 关主窗 = 隐藏(不退出) | ✅ |
| 浮窗右键 → 退出 | ✅ |
| a11y: aria-label + 对比度 4.5:1 | ❌ 对比度 + 视觉 |
| **新**: 浮窗可拖拽 | ❌ 用户实测失败 |
| **新**: 浅色玻璃设计语言符合 | ❌ 用户实测不符 |

---

## 新 build 产物(实测)

| 产物 | 大小 | MD5 |
|---|---|---|
| mindtap.exe | 10,554,368 B (10.07 MB) | `6cdec01bb9cd7141d513a3615bef1b97` |
| mindtap_0.2.0_x64-setup.exe | 2,772,291 B (2.643 MB) | `8aba7a0a91bc3ad01e52be4f4d9b9063` |
| dist/assets/floating-*.css | 39,789 B (38.86 KB) | (P0-3 修复证据 vs v2 660 B) |

---

## 严禁行为(本任务全程遵守)

- ❌ 未 amend 任何 commit
- ❌ 未修改任何源码(本报告只测不修)
- ❌ 未跳过静态 6 关
- ❌ 未 commit "待验证 TODO" 字样
- ❌ 未 "我假设可以工作"(GUI 18 条 + 浮窗拖拽 + 设计语言,均按用户实测如实 ❌)

---

## 用户实测记录(原文引用)

| 编号 | 原文 | 报告对应项 |
|---|---|---|
| User-FB-1 | "内容文字还是白色的一片空白,无法查看" | GUI #3 #5 #14,P0-3 实测 ❌ |
| User-FB-2 | "还是无法拖拽" | GUI #17,新 P0-A |
| User-FB-3 | "没有遵循的规范浅色玻璃拟态 · 设计语言" | 视觉验收 + 新 P0-B |

> 详细用户反馈项见:`docs/reports/2026-07-11-v3-qa-runtime-issues.md`(纯记录,未做任何诊断或修复建议)

---

## 关联文档

- v2 报告:`docs/reports/2026-07-11-v0.2.0-windows-qa-v2.md` (`56be2bd`)
- v1 报告:`docs/reports/2026-07-11-v0.2.0-windows-qa.md` (`c25b599`)
- 用户 runtime 问题清单:`docs/reports/2026-07-11-v3-qa-runtime-issues.md`
- agent 错误清单:`docs/reports/2026-07-11-v3-qa-agent-errors.md`
- spec:`docs/specs/2026-07-11-v0.2.0-floating-window-design.md` (§三 功能范围 + §六 验收标准 + §九 9 项决策表)
- checklist 7 层:`docs/architecture/v0.2.0-floating-visibility-checklist.md`
- retro 反模式 9:`docs/reports/v0.2.0-retrospective.md`
- design spec:`docs/design/glassic-ui-spec.md`(用户原文引用的设计语言规范)

---

## 关联 commit

- `8fe11be` chore(release): identifier .app → .desktop + cargo fmt 收口 — 报告**基线**
- `56b8647` fix(test): vitest mock @tauri-apps/api/core invoke + event + window
- `a965c55` fix(floating): P0-5 — 展开态 setFocusable(true)
- `f2b0f0f` fix(floating): P0-3 假修重做 — floating.css 顶部 @import tailwindcss + glass utilities inline
- v2 报告:`56be2bd` (基于 `b5660f5`,4 P0 阻塞)
- v1 报告:`c25b599` (基于 `1ba06b3`,首版 4 P0)

---

## 反模式 9 防御(实测 grep 5 项)

> 报告脚本要求"反模式 9 防御 — 沿用 X 实装必须 grep"

| grep 项 | 实测结论 |
|---|---|
| `.floating-root` 类挂载 | ✅ App.tsx:90 + ExpandedPanel.tsx:66/82 三处挂 |
| `data-tauri-drag-region` | ⚠️ App.tsx:89 已挂 + ContextMenu:35 已挂 = 静态配置 OK,**但 runtime drag 失效**(Tauri 2 transparent window 不响应,新 P0-A) |
| `@import "tailwindcss"` in floating.css | ✅ floating.css:6 |
| contextmenu preventDefault | ✅ App.tsx:74 + ExpandedPanel 56/59 |
| vitest mock | ✅ setup.ts setup mock + App.test.tsx inline mock(v2 错报 P1-1 不再假 — 已修) |

**结论**:
- 静态 grep 全 ✅
- 但 runtime 实测暴露多个 P0 仍 ❌(P0-3 视觉 / 新 P0-A 拖拽 / 新 P0-B design language)
- 这是典型"静态看起来好,runtime 失败" 盲区(参考 .archive/docs/architecture/floating-visibility-checklist.md 盲区表)
- agent 静态结论与用户 runtime 实测**显著矛盾** — 用户实测为准

---

## commit 状态

- 本报告:**未 commit**
- 等待用户授权 commit 后再 push

(本任务脚本指令"严禁行为"包含"未 amend 任何 commit",本报告为新增 commit,不动 8fe11be、56b8647、a965c55、f2b0f0f、56be2bd、c25b599 等历史 commit)
