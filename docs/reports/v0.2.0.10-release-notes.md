# V0.2.0.10 PATCH — 黑边真根因修复(部分根因,**已被 V0.2.0.12 PATCH 替代**)

> ⚠️ **本 PATCH 仅修黑边的局部根因(FoldedBar inline box-shadow),非黑边最终 fix**。V0.2.0.11 + V0.2.0.12 PATCH 才补完黑边全部根因(`shadow: false` + `transparent: true` + `backgroundColor: "#00000000"` + `resizable: false`)。完整黑边修复链见 [`docs/reports/v0.2.0.12-release-notes.md`](./v0.2.0.12-release-notes.md)。
> 发版日期: 2026-07-13
> 任务档: [`docs/tasks/v0.2.0.10-fix-foldedbar-inline-shadow/task.md`](../tasks/v0.2.0.10-fix-foldedbar-inline-shadow/task.md)

## 用户视角摘要(5 行)

1. **折叠态浮窗边缘不再有灰色描边**(原 V0.2.0.8 黑边修复的真根因)
2. FoldedBar 组件不再用 inline style 写 box-shadow(优先级最高会覆盖 CSS)
3. 视觉契约保持:背景半透明 + 顶部圆角 + drop shadow,无顶部白色高光带
4. 修复后 132/132 vitest PASS(含 4 个新增锁住测试防回归)
5. D:\ 端需 Ctrl+C 重启 `scripts\dev.bat` 才能看到效果(Vite hot-reload 可能没刷到 WebView2)

## 技术摘要(开发者)

- `FoldedBar.tsx`:删 inline `boxShadow`,加 `folded-bar-inner` className
- `floating.css`:加 `.folded-bar-inner` 块,box-shadow 仅保留 drop shadow(`0 4px 16px rgba(0, 30, 80, 0.06)`)
- `App.fix.test.tsx`:+50 行,4 个新测试锁住"无 inline style box-shadow + .folded-bar-inner 块无 inset"

## 修复路径

- V0.2.0.8(原 V0.2.8 Issue C)反复改 `.floating-root` 的 CSS,但真根因在 FoldedBar 的 inline style
- inline style 优先级 1000 > CSS class 任何选择器,`.floating-root` 怎么改都被覆盖
- 本次穷举所有 box-shadow 来源(CSS class + inline style),定位到 FoldedBar inline style 后修复

## 不修复项(留作后续 PATCH)

- V0.2.0.6 右键修复:**待 L3 重测**(user 报"右键无反应",可能是 hot-reload 没生效,需 Ctrl+C 重启 dev.bat 后再测)
- V0.2.0.7 呼吸灯错位:**待 L3 重测**(user inspect 路径可能是 FoldedBar inner div 而非根 div,需重测 StatusDot 是否在 `.floating-root folded` 第一个子节点)
- V0.2.0.9 折叠展开 width/height/border-radius 过渡:**待 L3 重测**(需 user 在 D:\ 端实测看 200ms ease-out 是否可见)

## 跨 task 依赖

- V0.2.0.6~0.9 retro 收口需补"V0.2.0.10 才真修好 Issue C"一行
- `docs/reports/v0.2.0-retrospective.md` §二 Bug 表 C 行需更新

## 关联

- [`docs/governance/versioning-rule.md`](../governance/versioning-rule.md) §三 + §四 + §六
- [`docs/tasks/v0.2.0.8-fix-black-border/task.md`](../tasks/v0.2.0.8-fix-black-border/task.md)(漏修的真根因)
- [`docs/reports/2026-07-13-v0.2.7-l3-findings.md`](2026-07-13-v0.2.7-l3-findings.md) §二 B + §三 C 段