# Mindtap 领域术语

## 浮窗

常驻桌面的紧凑窗口，用于显示当前任务、累计计时和必要操作。V0.2.1 只改变浮窗表面的材质能力，不改变主窗口、其他弹窗、任务流程或浮窗四状态。

### 平台原生动态材质

由目标操作系统提供、能让浮窗表面呈现可感知后方环境层次的半透明材质。它不等同于任何其他平台的视觉名称，也不以单纯透明或模糊作为完成标准。

### 候选方案

开发期间接受同一实机验收矩阵比较的一种材质实现。候选必须逐个验证；未通过淘汰门槛的候选不能进入生产。

### 候选淘汰门槛

候选一旦破坏既有功能、边缘完整性、状态与显示缩放稳定性、交互响应，或仍无法呈现可感知的后方环境层次，即判定不通过。

### CSS fallback

动态材质不可用时使用的轻量网页表面。它不要求呈现动态环境层次，但必须保持浮窗内容可读、边缘完整和交互稳定。

### 材质运行期选择

应用启动时为本次运行确定一个生产材质或 CSS fallback，之后不因折叠、展开、拖动或焦点切换重新选择。非原生方案失败时只允许单向降级到 CSS fallback，并保持到退出。

### 平台基线

Windows 11 是 V0.2.1 完整动态材质的支持与验收目标。其他系统版本或缺少相应能力的环境只要求 CSS fallback 基础可用。

### 验收矩阵

在浅色、深色和高对比或纹理背景下，组合检查聚焦与非聚焦、折叠与展开、100% / 125% / 150% 显示缩放，并观察可读性、边缘、稳定性、拖动和窗口大小变化。
