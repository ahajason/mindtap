# Mindtap V1.3 浮动窗口快速记录 · 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 Tauri 2 + React 19 现有 V1.0 脚手架上，实现 V1.3 浮动窗口快速记录功能：3 表数据层 + 业务层事务双写 + Tauri 2 多窗口（主窗 + 浮动窗）+ 4 状态 TODO 状态机 + Liquid Glass UI + 跨平台（macOS NSPanel + Windows 工具窗）。

**Architecture:**
- **后端（Rust）**：rusqlite 单 DB + task/idea/check_in 三源表 + record 通用表（业务层事务双写）+ 9 个 Tauri command
- **前端（React 19）**：主窗时间线 + 浮动窗 FoldState (320×36) / ExpandState (360×280) / SwitchDropdown + Liquid Glass CSS
- **跨平台**：macOS 通过 NSPanel 不抢焦 + Windows 通过 Tauri 2 内置 WS_EX_TOOLWINDOW
- **关键不变量**：L1 全局唯一 active · L2 paused→active 保留 duration · L3 active→paused 累加 · L4 active→done 累加 · L5 done 可撤销

**Tech Stack:**
- Rust 1.83+ / Tauri 2.x / rusqlite 0.32 (bundled)
- React 19.1 + TypeScript 5.8 + Vite 7
- 现有 V1.0 脚手架（`src-tauri/` + `src/` + `package.json` + `Cargo.toml`）

**参考文档：**
- 设计：`docs/superpowers/specs/2026-06-14-浮动窗口快速记录-design.md`
- 视觉：`docs/superpowers/specs/2026-06-14-浮动窗口快速记录-design.md` 第 8 节
- mockup：`/home/jason/workspace/mindtap/.superpowers/brainstorm/150600-1781431421/content/final-mockup.html`

---

## 文件结构（变更前 / 变更后）

### 新增文件
```
src-tauri/src/
├── db/
│   ├── mod.rs                # DB 初始化 + 连接管理
│   ├── schema.rs             # 4 张表 CREATE 语句
│   ├── task.rs               # task 业务函数（事务双写）
│   ├── idea.rs               # idea 业务函数
│   ├── check_in.rs           # check_in 业务函数
│   └── record.rs             # record 视图查询
├── commands/
│   ├── mod.rs                # 命令注册
│   ├── task_cmd.rs           # 8 个 task_* command
│   ├── idea_cmd.rs           # idea_create command
│   ├── check_in_cmd.rs       # check_in_create command
│   └── floating_cmd.rs       # floating_show/hide/toggle command
├── floating/
│   ├── mod.rs                # FloatingWindow 启动
│   └── platform.rs           # macOS NSPanel / Windows tool window
└── error.rs                  # DbError / AppError 统一错误

src/
├── floating/                 # 浮动窗 React 入口
│   ├── main.tsx
│   ├── App.tsx
│   ├── components/
│   │   ├── FoldState.tsx
│   │   ├── ExpandState.tsx
│   │   ├── FocusBlock.tsx
│   │   ├── SwitchDropdown.tsx
│   │   └── ControlRow.tsx
│   ├── hooks/
│   │   ├── useActiveTask.ts
│   │   ├── useTick.ts        # 每秒 evt_tick
│   │   └── useFloatingWindow.ts
│   └── styles/
│       ├── glass.css
│       └── floating.css
├── timeline/                 # 主窗时间线（新组件）
│   ├── RecordTimeline.tsx
│   ├── TypeTabs.tsx
│   ├── SearchBox.tsx
│   └── hooks/
│       └── useRecords.ts
└── lib/
    ├── tauri-bridge.ts       # 封装 invoke / event
    └── time.ts               # 时间格式化（00:12:34）

src/floating.html             # 浮动窗 HTML 入口
```

### 修改文件
```
src-tauri/Cargo.toml            # + rusqlite (bundled) + tokio
src-tauri/tauri.conf.json       # + floating window + globalShortcut plugin
src-tauri/src/lib.rs            # + 初始化 db + 注册 commands + 启动 floating window
src-tauri/src/main.rs           # 现状 OK
src/main.tsx                    # 不变（主窗入口）
src/App.tsx                     # 集成 timeline 组件
```

---

## Phase 1：数据层（Rust）

### Task 1: 引入 rusqlite 依赖

**Files:**
- Modify: `src-tauri/Cargo.toml:14-22`

- [ ] **Step 1: 在 Cargo.toml 添加 rusqlite 与 tokio 依赖**

在 `[dependencies]` 块追加：

```toml
rusqlite = { version = "0.32", features = ["bundled"] }
tokio = { version = "1", features = ["sync", "time", "rt"] }
anyhow = "1"
thiserror = "1"
```

`bundled` 让 SQLite 静态链接，避免系统库依赖。

- [ ] **Step 2: 验证编译**

```bash
cd /home/jason/workspace/mindtap/src-tauri && cargo check
```

Expected: 编译通过，下载新依赖。

- [ ] **Step 3: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src-tauri/Cargo.toml src-tauri/Cargo.lock
git commit -m "feat(db): 引入 rusqlite + tokio + anyhow + thiserror 依赖"
```

---

### Task 2: 创建 db 模块骨架

**Files:**
- Create: `src-tauri/src/db/mod.rs`
- Create: `src-tauri/src/error.rs`
- Modify: `src-tauri/src/lib.rs:1-14`

- [ ] **Step 1: 创建 error.rs**

```rust
// src-tauri/src/error.rs
use thiserror::Error;

#[derive(Debug, Error)]
pub enum AppError {
    #[error("数据库错误: {0}")]
    Db(#[from] rusqlite::Error),

    #[error("序列化错误: {0}")]
    Serde(#[from] serde_json::Error),

    #[error("未找到资源: {0}")]
    NotFound(String),

    #[error("状态机非法转换: {0}")]
    InvalidTransition(String),

    #[error("全局唯一 Focus 冲突")]
    ActiveExists,
}

pub type AppResult<T> = Result<T, AppError>;

impl serde::Serialize for AppError {
    fn serialize<S: serde::Serializer>(&self, s: S) -> Result<S::Ok, S::Error> {
        s.serialize_str(&self.to_string())
    }
}
```

- [ ] **Step 2: 创建 db/mod.rs（占位）**

```rust
// src-tauri/src/db/mod.rs
pub mod schema;
pub mod task;
pub mod idea;
pub mod check_in;
pub mod record;

use rusqlite::Connection;
use std::sync::Mutex;
use tauri::Manager;

pub struct DbState(pub Mutex<Connection>);

pub fn init(app: &tauri::App) -> Result<(), Box<dyn std::error::Error>> {
    let app_dir = app.path().app_data_dir()?;
    std::fs::create_dir_all(&app_dir)?;
    let db_path = app_dir.join("mindtap.db");
    let conn = Connection::open(&db_path)?;
    schema::create_tables(&conn)?;
    app.manage(DbState(Mutex::new(conn)));
    Ok(())
}
```

- [ ] **Step 3: 创建 db/schema.rs（占位）**

```rust
// src-tauri/src/db/schema.rs
use rusqlite::Connection;

pub fn create_tables(_conn: &Connection) -> rusqlite::Result<()> {
    Ok(())
}
```

- [ ] **Step 4: 创建 task/idea/check_in/record.rs（空模块）**

```rust
// src-tauri/src/db/task.rs
// 占位
```

```rust
// src-tauri/src/db/idea.rs
// 占位
```

```rust
// src-tauri/src/db/check_in.rs
// 占位
```

```rust
// src-tauri/src/db/record.rs
// 占位
```

- [ ] **Step 5: 修改 lib.rs 注册 db init**

替换 `src-tauri/src/lib.rs`：

```rust
mod db;
mod error;

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .plugin(tauri_plugin_opener::init())
        .setup(|app| {
            db::init(app).map_err(|e| e.to_string())?;
            Ok(())
        })
        .invoke_handler(tauri::generate_handler![])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

- [ ] **Step 6: 验证编译**

```bash
cd /home/jason/workspace/mindtap/src-tauri && cargo check
```

Expected: 编译通过，db 模块加载。

- [ ] **Step 7: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src-tauri/src/db src-tauri/src/error.rs src-tauri/src/lib.rs
git commit -m "feat(db): 创建 db 模块骨架 + DbState + AppError"
```

---

### Task 3: 编写 task 表 schema + 单元测试

**Files:**
- Modify: `src-tauri/src/db/schema.rs`

- [ ] **Step 1: 在 schema.rs 顶部加测试模块占位**

```rust
// src-tauri/src/db/schema.rs
use rusqlite::Connection;

pub fn create_tables(_conn: &Connection) -> rusqlite::Result<()> {
    Ok(())
}

#[cfg(test)]
mod tests {
    use super::*;
    use rusqlite::Connection;

    fn test_conn() -> Connection {
        Connection::open_in_memory().unwrap()
    }

    #[test]
    fn create_tables_runs_without_error() {
        let conn = test_conn();
        let result = create_tables(&conn);
        assert!(result.is_ok(), "create_tables should succeed: {:?}", result);
    }
}
```

- [ ] **Step 2: 运行测试确认通过（占位实现）**

```bash
cd /home/jason/workspace/mindtap/src-tauri && cargo test create_tables_runs_without_error
```

Expected: 1 passed

- [ ] **Step 3: 实现完整 create_tables**

替换 `create_tables` 函数：

```rust
pub fn create_tables(conn: &Connection) -> rusqlite::Result<()> {
    conn.execute_batch(
        r#"
        CREATE TABLE IF NOT EXISTS task (
            id                  INTEGER PRIMARY KEY AUTOINCREMENT,
            content             TEXT    NOT NULL,
            status              TEXT    NOT NULL DEFAULT 'pending',
            duration_ms         INTEGER NOT NULL DEFAULT 0,
            first_focused_at    INTEGER,
            focus_started_at    INTEGER,
            paused_at           INTEGER,
            focused_count       INTEGER NOT NULL DEFAULT 0,
            due_at              INTEGER,
            created_at          INTEGER NOT NULL,
            updated_at          INTEGER NOT NULL,
            archived_at         INTEGER
        );
        CREATE INDEX IF NOT EXISTS idx_task_status ON task(status);
        CREATE INDEX IF NOT EXISTS idx_task_due_at ON task(due_at);

        CREATE TABLE IF NOT EXISTS idea (
            id          INTEGER PRIMARY KEY AUTOINCREMENT,
            content     TEXT    NOT NULL,
            created_at  INTEGER NOT NULL
        );
        CREATE INDEX IF NOT EXISTS idx_idea_created_at ON idea(created_at);

        CREATE TABLE IF NOT EXISTS check_in (
            id          INTEGER PRIMARY KEY AUTOINCREMENT,
            habit       TEXT    NOT NULL,
            note        TEXT,
            checked_at  INTEGER NOT NULL
        );
        CREATE INDEX IF NOT EXISTS idx_check_in_habit      ON check_in(habit);
        CREATE INDEX IF NOT EXISTS idx_check_in_checked_at ON check_in(checked_at);

        CREATE TABLE IF NOT EXISTS record (
            id          INTEGER PRIMARY KEY AUTOINCREMENT,
            kind        TEXT    NOT NULL,
            source_id   INTEGER NOT NULL,
            content     TEXT    NOT NULL,
            status      TEXT,
            created_at  INTEGER NOT NULL,
            UNIQUE(kind, source_id)
        );
        CREATE INDEX IF NOT EXISTS idx_record_created_at ON record(created_at);
        CREATE INDEX IF NOT EXISTS idx_record_kind       ON record(kind);
        CREATE INDEX IF NOT EXISTS idx_record_status     ON record(status);
        "#,
    )
}
```

- [ ] **Step 4: 验证表结构（添加 schema 完整性测试）**

在 `tests` 模块追加：

```rust
#[test]
fn all_four_tables_exist_after_create() {
    let conn = test_conn();
    create_tables(&conn).unwrap();
    let names: Vec<String> = conn
        .prepare("SELECT name FROM sqlite_master WHERE type='table' ORDER BY name")
        .unwrap()
        .query_map([], |r| r.get(0))
        .unwrap()
        .filter_map(|r| r.ok())
        .collect();
    assert!(names.contains(&"task".to_string()));
    assert!(names.contains(&"idea".to_string()));
    assert!(names.contains(&"check_in".to_string()));
    assert!(names.contains(&"record".to_string()));
}
```

- [ ] **Step 5: 运行所有 schema 测试**

```bash
cd /home/jason/workspace/mindtap/src-tauri && cargo test schema
```

Expected: 2 passed

- [ ] **Step 6: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src-tauri/src/db/schema.rs
git commit -m "feat(db): 实现 4 表 schema (task/idea/check_in/record) + 索引"
```

---

### Task 4: 实现 task::create（事务双写 task + record）

**Files:**
- Modify: `src-tauri/src/db/task.rs`

- [ ] **Step 1: 在 task.rs 加 Task 结构 + 测试**

```rust
// src-tauri/src/db/task.rs
use crate::error::{AppError, AppResult};
use rusqlite::{Connection, params};
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Task {
    pub id: i64,
    pub content: String,
    pub status: String,
    pub duration_ms: i64,
    pub first_focused_at: Option<i64>,
    pub focus_started_at: Option<i64>,
    pub paused_at: Option<i64>,
    pub focused_count: i64,
    pub due_at: Option<i64>,
    pub created_at: i64,
    pub updated_at: i64,
    pub archived_at: Option<i64>,
}

pub fn now_ms() -> i64 {
    use std::time::{SystemTime, UNIX_EPOCH};
    SystemTime::now()
        .duration_since(UNIX_EPOCH)
        .unwrap_or_default()
        .as_millis() as i64
}

pub fn create_task(
    conn: &Connection,
    content: &str,
    due_at: Option<i64>,
) -> AppResult<Task> {
    let now = now_ms();
    let tx = conn.unchecked_transaction()?;
    tx.execute(
        "INSERT INTO task (content, status, created_at, updated_at, due_at)
         VALUES (?1, 'pending', ?2, ?2, ?3)",
        params![content, now, due_at],
    )?;
    let id = tx.last_insert_rowid();
    tx.execute(
        "INSERT INTO record (kind, source_id, content, status, created_at)
         VALUES ('task', ?1, ?2, 'pending', ?3)",
        params![id, content, now],
    )?;
    tx.commit()?;
    Ok(Task {
        id,
        content: content.to_string(),
        status: "pending".to_string(),
        duration_ms: 0,
        first_focused_at: None,
        focus_started_at: None,
        paused_at: None,
        focused_count: 0,
        due_at,
        created_at: now,
        updated_at: now,
        archived_at: None,
    })
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::db::schema::create_tables;

    fn test_conn() -> Connection {
        let conn = Connection::open_in_memory().unwrap();
        create_tables(&conn).unwrap();
        conn
    }

    #[test]
    fn create_task_inserts_both_task_and_record() {
        let conn = test_conn();
        let task = create_task(&conn, "写 Q3 计划", None).unwrap();
        assert_eq!(task.status, "pending");
        assert_eq!(task.duration_ms, 0);
        assert_eq!(task.focused_count, 0);

        // 验证 task 行存在
        let task_count: i64 = conn
            .query_row("SELECT COUNT(*) FROM task WHERE id = ?1", params![task.id], |r| r.get(0))
            .unwrap();
        assert_eq!(task_count, 1);

        // 验证 record 行自动写入（事务双写）
        let (kind, source_id, content, status): (String, i64, String, Option<String>) = conn
            .query_row(
                "SELECT kind, source_id, content, status FROM record WHERE source_id = ?1",
                params![task.id],
                |r| Ok((r.get(0)?, r.get(1)?, r.get(2)?, r.get(3)?)),
            )
            .unwrap();
        assert_eq!(kind, "task");
        assert_eq!(source_id, task.id);
        assert_eq!(content, "写 Q3 计划");
        assert_eq!(status, Some("pending".to_string()));
    }

    #[test]
    fn create_task_with_due_at_stores_due() {
        let conn = test_conn();
        let due = now_ms() + 86_400_000;
        let task = create_task(&conn, "明天做", Some(due)).unwrap();
        assert_eq!(task.due_at, Some(due));
    }
}
```

- [ ] **Step 2: 运行测试**

```bash
cd /home/jason/workspace/mindtap/src-tauri && cargo test create_task
```

Expected: 2 passed

- [ ] **Step 3: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src-tauri/src/db/task.rs
git commit -m "feat(db): task::create 事务双写 (task + record)"
```

---

### Task 5: 实现 task 状态机（start_timer / pause / resume / complete / undo）

**Files:**
- Modify: `src-tauri/src/db/task.rs`

- [ ] **Step 1: 在 task.rs 追加状态机函数 + 测试**

在 `task.rs` 的 `create_task` 之后追加：

```rust
/// 启动计时：pending → active
pub fn start_timer(conn: &Connection, task_id: i64) -> AppResult<Task> {
    let now = now_ms();
    let tx = conn.unchecked_transaction()?;

    // L1: 全局唯一 active 检查
    let existing: Option<i64> = tx
        .query_row(
            "SELECT id FROM task WHERE status = 'active' AND id != ?1 LIMIT 1",
            params![task_id],
            |r| r.get(0),
        )
        .ok();
    if existing.is_some() {
        return Err(AppError::ActiveExists);
    }

    tx.execute(
        "UPDATE task
         SET status = 'active',
             focus_started_at = ?1,
             first_focused_at = COALESCE(first_focused_at, ?1),
             focused_count = focused_count + 1,
             updated_at = ?1
         WHERE id = ?2",
        params![now, task_id],
    )?;
    tx.execute(
        "UPDATE record SET status = 'active' WHERE kind = 'task' AND source_id = ?1",
        params![task_id],
    )?;
    tx.commit()?;
    get_task(conn, task_id)
}

/// 暂停：active → paused
pub fn pause(conn: &Connection, task_id: i64) -> AppResult<Task> {
    let now = now_ms();
    let tx = conn.unchecked_transaction()?;
    let (status, focus_started_at, duration_ms): (String, Option<i64>, i64) = tx.query_row(
        "SELECT status, focus_started_at, duration_ms FROM task WHERE id = ?1",
        params![task_id],
        |r| Ok((r.get(0)?, r.get(1)?, r.get(2)?)),
    )?;
    if status != "active" {
        return Err(AppError::InvalidTransition(format!(
            "pause requires active, got {}", status
        )));
    }
    let started = focus_started_at.ok_or_else(|| {
        AppError::InvalidTransition("active task missing focus_started_at".into())
    })?;
    let new_duration = duration_ms + (now - started);
    tx.execute(
        "UPDATE task
         SET status = 'paused',
             duration_ms = ?1,
             focus_started_at = NULL,
             paused_at = ?2,
             updated_at = ?2
         WHERE id = ?3",
        params![new_duration, now, task_id],
    )?;
    tx.execute(
        "UPDATE record SET status = 'paused' WHERE kind = 'task' AND source_id = ?1",
        params![task_id],
    )?;
    tx.commit()?;
    get_task(conn, task_id)
}

/// 继续：paused → active
pub fn resume(conn: &Connection, task_id: i64) -> AppResult<Task> {
    let now = now_ms();
    let tx = conn.unchecked_transaction()?;
    let existing: Option<i64> = tx
        .query_row(
            "SELECT id FROM task WHERE status = 'active' AND id != ?1 LIMIT 1",
            params![task_id],
            |r| r.get(0),
        )
        .ok();
    if existing.is_some() {
        return Err(AppError::ActiveExists);
    }
    let updated = tx.execute(
        "UPDATE task
         SET status = 'active',
             focus_started_at = ?1,
             paused_at = NULL,
             updated_at = ?1
         WHERE id = ?2 AND status = 'paused'",
        params![now, task_id],
    )?;
    if updated == 0 {
        return Err(AppError::InvalidTransition("resume requires paused".into()));
    }
    tx.execute(
        "UPDATE record SET status = 'active' WHERE kind = 'task' AND source_id = ?1",
        params![task_id],
    )?;
    tx.commit()?;
    get_task(conn, task_id)
}

/// 完成：active → done
pub fn complete(conn: &Connection, task_id: i64) -> AppResult<Task> {
    let now = now_ms();
    let tx = conn.unchecked_transaction()?;
    let (status, focus_started_at, duration_ms): (String, Option<i64>, i64) = tx.query_row(
        "SELECT status, focus_started_at, duration_ms FROM task WHERE id = ?1",
        params![task_id],
        |r| Ok((r.get(0)?, r.get(1)?, r.get(2)?)),
    )?;
    if status != "active" {
        return Err(AppError::InvalidTransition(format!(
            "complete requires active, got {}", status
        )));
    }
    let new_duration = if let Some(started) = focus_started_at {
        duration_ms + (now - started)
    } else {
        duration_ms
    };
    tx.execute(
        "UPDATE task
         SET status = 'done',
             duration_ms = ?1,
             focus_started_at = NULL,
             updated_at = ?2
         WHERE id = ?3",
        params![new_duration, now, task_id],
    )?;
    tx.execute(
        "UPDATE record SET status = 'done' WHERE kind = 'task' AND source_id = ?1",
        params![task_id],
    )?;
    tx.commit()?;
    get_task(conn, task_id)
}

/// 撤销：done → active
pub fn undo(conn: &Connection, task_id: i64) -> AppResult<Task> {
    let now = now_ms();
    let tx = conn.unchecked_transaction()?;
    let updated = tx.execute(
        "UPDATE task
         SET status = 'active',
             focus_started_at = ?1,
             updated_at = ?1
         WHERE id = ?2 AND status = 'done'",
        params![now, task_id],
    )?;
    if updated == 0 {
        return Err(AppError::InvalidTransition("undo requires done".into()));
    }
    tx.execute(
        "UPDATE record SET status = 'active' WHERE kind = 'task' AND source_id = ?1",
        params![task_id],
    )?;
    tx.commit()?;
    get_task(conn, task_id)
}

pub fn get_task(conn: &Connection, id: i64) -> AppResult<Task> {
    Ok(conn.query_row(
        "SELECT id, content, status, duration_ms, first_focused_at, focus_started_at,
                paused_at, focused_count, due_at, created_at, updated_at, archived_at
         FROM task WHERE id = ?1",
        params![id],
        |r| Ok(Task {
            id: r.get(0)?,
            content: r.get(1)?,
            status: r.get(2)?,
            duration_ms: r.get(3)?,
            first_focused_at: r.get(4)?,
            focus_started_at: r.get(5)?,
            paused_at: r.get(6)?,
            focused_count: r.get(7)?,
            due_at: r.get(8)?,
            created_at: r.get(9)?,
            updated_at: r.get(10)?,
            archived_at: r.get(11)?,
        }),
    )?)
}
```

- [ ] **Step 2: 追加状态机测试**

在 `task.rs` 的 `tests` 模块末尾追加：

```rust
    #[test]
    fn start_timer_pending_to_active() {
        let conn = test_conn();
        let task = create_task(&conn, "test", None).unwrap();
        let active = start_timer(&conn, task.id).unwrap();
        assert_eq!(active.status, "active");
        assert!(active.first_focused_at.is_some());
        assert!(active.focus_started_at.is_some());
        assert_eq!(active.focused_count, 1);
    }

    #[test]
    fn start_timer_when_active_exists_errors() {
        let conn = test_conn();
        let t1 = create_task(&conn, "a", None).unwrap();
        let t2 = create_task(&conn, "b", None).unwrap();
        start_timer(&conn, t1.id).unwrap();
        let result = start_timer(&conn, t2.id);
        assert!(matches!(result, Err(AppError::ActiveExists)));
    }

    #[test]
    fn pause_active_accumulates_duration() {
        let conn = test_conn();
        let task = create_task(&conn, "x", None).unwrap();
        start_timer(&conn, task.id).unwrap();
        std::thread::sleep(std::time::Duration::from_millis(50));
        let paused = pause(&conn, task.id).unwrap();
        assert_eq!(paused.status, "paused");
        assert!(paused.duration_ms >= 50);
        assert!(paused.focus_started_at.is_none());
        assert!(paused.paused_at.is_some());
    }

    #[test]
    fn resume_paused_preserves_duration() {
        let conn = test_conn();
        let task = create_task(&conn, "x", None).unwrap();
        start_timer(&conn, task.id).unwrap();
        std::thread::sleep(std::time::Duration::from_millis(30));
        let paused = pause(&conn, task.id).unwrap();
        let d1 = paused.duration_ms;
        let resumed = resume(&conn, task.id).unwrap();
        assert_eq!(resumed.status, "active");
        assert_eq!(resumed.duration_ms, d1, "duration preserved across resume");
        assert!(resumed.focus_started_at.is_some());
    }

    #[test]
    fn complete_accumulates_then_done() {
        let conn = test_conn();
        let task = create_task(&conn, "x", None).unwrap();
        start_timer(&conn, task.id).unwrap();
        std::thread::sleep(std::time::Duration::from_millis(20));
        let done = complete(&conn, task.id).unwrap();
        assert_eq!(done.status, "done");
        assert!(done.duration_ms >= 20);
        assert!(done.focus_started_at.is_none());
    }

    #[test]
    fn undo_done_to_active() {
        let conn = test_conn();
        let task = create_task(&conn, "x", None).unwrap();
        start_timer(&conn, task.id).unwrap();
        complete(&conn, task.id).unwrap();
        let undone = undo(&conn, task.id).unwrap();
        assert_eq!(undone.status, "active");
        assert!(undone.focus_started_at.is_some());
    }
```

- [ ] **Step 3: 运行所有 task 测试**

```bash
cd /home/jason/workspace/mindtap/src-tauri && cargo test task
```

Expected: 8 passed (2 create + 6 state machine)

- [ ] **Step 4: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src-tauri/src/db/task.rs
git commit -m "feat(db): task 4 状态机 (start/pause/resume/complete/undo) + 5 条不变量"
```

---

### Task 6: 实现 task::switch（切换 Focus）

**Files:**
- Modify: `src-tauri/src/db/task.rs`

- [ ] **Step 1: 追加 switch 函数**

在 `task.rs` 末尾（`get_task` 之后）追加：

```rust
/// 切换 Focus：当前 active → pending/paused，new_id → active
pub fn switch_focus(conn: &Connection, new_id: i64) -> AppResult<Task> {
    let now = now_ms();
    let tx = conn.unchecked_transaction()?;

    // 把当前 active 暂停（保留时间）
    let current: Option<(i64, Option<i64>, i64)> = tx
        .query_row(
            "SELECT id, focus_started_at, duration_ms FROM task WHERE status = 'active'",
            [],
            |r| Ok((r.get(0)?, r.get(1)?, r.get(2)?)),
        )
        .ok();
    if let Some((cur_id, focus_started_at, duration_ms)) = current {
        if cur_id != new_id {
            let new_duration = if let Some(started) = focus_started_at {
                duration_ms + (now - started)
            } else {
                duration_ms
            };
            tx.execute(
                "UPDATE task
                 SET status = 'paused',
                     duration_ms = ?1,
                     focus_started_at = NULL,
                     paused_at = ?2
                 WHERE id = ?3",
                params![new_duration, now, cur_id],
            )?;
            tx.execute(
                "UPDATE record SET status = 'paused' WHERE kind = 'task' AND source_id = ?1",
                params![cur_id],
            )?;
        }
    }

    // 启动新 task
    let target_status: String = tx
        .query_row("SELECT status FROM task WHERE id = ?1", params![new_id], |r| r.get(0))
        .map_err(|_| AppError::NotFound(format!("task {}", new_id)))?;
    let new_status = match target_status.as_str() {
        "pending" | "paused" => {
            tx.execute(
                "UPDATE task
                 SET status = 'active',
                     focus_started_at = ?1,
                     first_focused_at = COALESCE(first_focused_at, ?1),
                     focused_count = focused_count + 1,
                     paused_at = NULL,
                     updated_at = ?1
                 WHERE id = ?2",
                params![now, new_id],
            )?;
            "active"
        }
        "active" => "active", // 已经是 active
        other => {
            return Err(AppError::InvalidTransition(format!(
                "cannot switch to {} task", other
            )));
        }
    };
    tx.execute(
        "UPDATE record SET status = ?1 WHERE kind = 'task' AND source_id = ?2",
        params![new_status, new_id],
    )?;
    tx.commit()?;
    get_task(conn, new_id)
}
```

- [ ] **Step 2: 追加 switch 测试**

```rust
    #[test]
    fn switch_pauses_current_and_activates_new() {
        let conn = test_conn();
        let t1 = create_task(&conn, "first", None).unwrap();
        let t2 = create_task(&conn, "second", None).unwrap();
        start_timer(&conn, t1.id).unwrap();
        std::thread::sleep(std::time::Duration::from_millis(30));
        let switched = switch_focus(&conn, t2.id).unwrap();
        assert_eq!(switched.id, t2.id);
        assert_eq!(switched.status, "active");

        let t1_after: (String, i64) = conn
            .query_row(
                "SELECT status, duration_ms FROM task WHERE id = ?1",
                params![t1.id],
                |r| Ok((r.get(0)?, r.get(1)?)),
            )
            .unwrap();
        assert_eq!(t1_after.0, "paused");
        assert!(t1_after.1 >= 30);
    }
```

- [ ] **Step 3: 运行测试**

```bash
cd /home/jason/workspace/mindtap/src-tauri && cargo test switch
```

Expected: 1 passed

- [ ] **Step 4: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src-tauri/src/db/task.rs
git commit -m "feat(db): task::switch_focus 原子事务（暂停当前+激活新）"
```

---

### Task 7: 实现 idea::create（事务双写）

**Files:**
- Modify: `src-tauri/src/db/idea.rs`

- [ ] **Step 1: 实现 idea 业务**

```rust
// src-tauri/src/db/idea.rs
use crate::db::task::now_ms;
use crate::error::AppResult;
use rusqlite::{Connection, params};

pub fn create_idea(conn: &Connection, content: &str) -> AppResult<i64> {
    let now = now_ms();
    let tx = conn.unchecked_transaction()?;
    tx.execute(
        "INSERT INTO idea (content, created_at) VALUES (?1, ?2)",
        params![content, now],
    )?;
    let id = tx.last_insert_rowid();
    tx.execute(
        "INSERT INTO record (kind, source_id, content, status, created_at)
         VALUES ('idea', ?1, ?2, NULL, ?3)",
        params![id, content, now],
    )?;
    tx.commit()?;
    Ok(id)
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::db::schema::create_tables;

    fn test_conn() -> Connection {
        let conn = Connection::open_in_memory().unwrap();
        create_tables(&conn).unwrap();
        conn
    }

    #[test]
    fn create_idea_writes_idea_and_record() {
        let conn = test_conn();
        let id = create_idea(&conn, "用 Vercel KV 做缓存").unwrap();
        let idea_count: i64 = conn
            .query_row("SELECT COUNT(*) FROM idea WHERE id = ?1", params![id], |r| r.get(0))
            .unwrap();
        assert_eq!(idea_count, 1);
        let record_kind: String = conn
            .query_row(
                "SELECT kind FROM record WHERE source_id = ?1",
                params![id],
                |r| r.get(0),
            )
            .unwrap();
        assert_eq!(record_kind, "idea");
    }
}
```

- [ ] **Step 2: 运行测试**

```bash
cd /home/jason/workspace/mindtap/src-tauri && cargo test create_idea
```

Expected: 1 passed

- [ ] **Step 3: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src-tauri/src/db/idea.rs
git commit -m "feat(db): idea::create 事务双写 (idea + record)"
```

---

### Task 8: 实现 check_in::create（事务双写）

**Files:**
- Modify: `src-tauri/src/db/check_in.rs`

- [ ] **Step 1: 实现 check_in 业务**

```rust
// src-tauri/src/db/check_in.rs
use crate::db::task::now_ms;
use crate::error::AppResult;
use rusqlite::{Connection, params};

pub fn create_check_in(
    conn: &Connection,
    habit: &str,
    note: Option<&str>,
) -> AppResult<i64> {
    let now = now_ms();
    let content = match note {
        Some(n) => format!("{} · {}", habit, n),
        None => habit.to_string(),
    };
    let tx = conn.unchecked_transaction()?;
    tx.execute(
        "INSERT INTO check_in (habit, note, checked_at) VALUES (?1, ?2, ?3)",
        params![habit, note, now],
    )?;
    let id = tx.last_insert_rowid();
    tx.execute(
        "INSERT INTO record (kind, source_id, content, status, created_at)
         VALUES ('check_in', ?1, ?2, NULL, ?3)",
        params![id, content, now],
    )?;
    tx.commit()?;
    Ok(id)
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::db::schema::create_tables;

    fn test_conn() -> Connection {
        let conn = Connection::open_in_memory().unwrap();
        create_tables(&conn).unwrap();
        conn
    }

    #[test]
    fn create_check_in_with_note() {
        let conn = test_conn();
        let id = create_check_in(&conn, "晨跑", Some("5km")).unwrap();
        let content: String = conn
            .query_row(
                "SELECT content FROM record WHERE kind = 'check_in' AND source_id = ?1",
                params![id],
                |r| r.get(0),
            )
            .unwrap();
        assert_eq!(content, "晨跑 · 5km");
    }

    #[test]
    fn create_check_in_without_note() {
        let conn = test_conn();
        let id = create_check_in(&conn, "冥想", None).unwrap();
        let content: String = conn
            .query_row(
                "SELECT content FROM record WHERE kind = 'check_in' AND source_id = ?1",
                params![id],
                |r| r.get(0),
            )
            .unwrap();
        assert_eq!(content, "冥想");
    }
}
```

- [ ] **Step 2: 运行测试**

```bash
cd /home/jason/workspace/mindtap/src-tauri && cargo test check_in
```

Expected: 2 passed

- [ ] **Step 3: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src-tauri/src/db/check_in.rs
git commit -m "feat(db): check_in::create 事务双写 (check_in + record)"
```

---

### Task 9: 实现 record 视图查询（主窗时间线用）

**Files:**
- Modify: `src-tauri/src/db/record.rs`

- [ ] **Step 1: 实现 record 查询函数**

```rust
// src-tauri/src/db/record.rs
use crate::error::AppResult;
use rusqlite::{Connection, params};
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Record {
    pub id: i64,
    pub kind: String,
    pub source_id: i64,
    pub content: String,
    pub status: Option<String>,
    pub created_at: i64,
}

/// 全部记录（最新优先），可隐藏归档
pub fn list_records(
    conn: &Connection,
    limit: i64,
    hide_archived: bool,
) -> AppResult<Vec<Record>> {
    let sql = if hide_archived {
        "SELECT id, kind, source_id, content, status, created_at FROM record
         WHERE kind != 'task' OR source_id NOT IN
             (SELECT id FROM task WHERE archived_at IS NOT NULL)
         ORDER BY created_at DESC
         LIMIT ?1"
    } else {
        "SELECT id, kind, source_id, content, status, created_at FROM record
         ORDER BY created_at DESC
         LIMIT ?1"
    };
    let mut stmt = conn.prepare(sql)?;
    let rows = stmt
        .query_map(params![limit], |r| {
            Ok(Record {
                id: r.get(0)?,
                kind: r.get(1)?,
                source_id: r.get(2)?,
                content: r.get(3)?,
                status: r.get(4)?,
                created_at: r.get(5)?,
            })
        })?
        .collect::<Result<Vec<_>, _>>()?;
    Ok(rows)
}

/// 按类型筛选
pub fn list_by_kind(
    conn: &Connection,
    kind: &str,
    limit: i64,
) -> AppResult<Vec<Record>> {
    let mut stmt = conn.prepare(
        "SELECT id, kind, source_id, content, status, created_at FROM record
         WHERE kind = ?1
         ORDER BY created_at DESC
         LIMIT ?2",
    )?;
    let rows = stmt
        .query_map(params![kind, limit], |r| {
            Ok(Record {
                id: r.get(0)?,
                kind: r.get(1)?,
                source_id: r.get(2)?,
                content: r.get(3)?,
                status: r.get(4)?,
                created_at: r.get(5)?,
            })
        })?
        .collect::<Result<Vec<_>, _>>()?;
    Ok(rows)
}

/// 切换下拉清单：未开始 + 已暂停
pub fn list_switchable(conn: &Connection) -> AppResult<Vec<Record>> {
    let mut stmt = conn.prepare(
        "SELECT r.id, r.kind, r.source_id, r.content, r.status, r.created_at
         FROM record r
         WHERE r.kind = 'task' AND r.status IN ('pending', 'paused')
         ORDER BY
             CASE r.status WHEN 'pending' THEN 1 WHEN 'paused' THEN 2 END,
             r.created_at DESC",
    )?;
    let rows = stmt
        .query_map([], |r| {
            Ok(Record {
                id: r.get(0)?,
                kind: r.get(1)?,
                source_id: r.get(2)?,
                content: r.get(3)?,
                status: r.get(4)?,
                created_at: r.get(5)?,
            })
        })?
        .collect::<Result<Vec<_>, _>>()?;
    Ok(rows)
}

/// 当前 active task
pub fn get_active_task(conn: &Connection) -> AppResult<Option<Record>> {
    let mut stmt = conn.prepare(
        "SELECT id, kind, source_id, content, status, created_at FROM record
         WHERE kind = 'task' AND status = 'active'
         LIMIT 1",
    )?;
    let mut rows = stmt.query([])?;
    if let Some(r) = rows.next()? {
        Ok(Some(Record {
            id: r.get(0)?,
            kind: r.get(1)?,
            source_id: r.get(2)?,
            content: r.get(3)?,
            status: r.get(4)?,
            created_at: r.get(5)?,
        }))
    } else {
        Ok(None)
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::db::check_in::create_check_in;
    use crate::db::idea::create_idea;
    use crate::db::schema::create_tables;
    use crate::db::task::{complete, create_task, start_timer};

    fn test_conn() -> Connection {
        let conn = Connection::open_in_memory().unwrap();
        create_tables(&conn).unwrap();
        conn
    }

    #[test]
    fn list_records_mixes_all_kinds() {
        let conn = test_conn();
        create_idea(&conn, "a").unwrap();
        create_check_in(&conn, "h", None).unwrap();
        let t = create_task(&conn, "x", None).unwrap();
        start_timer(&conn, t.id).unwrap();

        let all = list_records(&conn, 50, false).unwrap();
        assert_eq!(all.len(), 3);
        let kinds: Vec<&str> = all.iter().map(|r| r.kind.as_str()).collect();
        assert!(kinds.contains(&"idea"));
        assert!(kinds.contains(&"check_in"));
        assert!(kinds.contains(&"task"));
    }

    #[test]
    fn get_active_task_returns_currently_active() {
        let conn = test_conn();
        let t = create_task(&conn, "active one", None).unwrap();
        start_timer(&conn, t.id).unwrap();
        let active = get_active_task(&conn).unwrap();
        assert!(active.is_some());
        assert_eq!(active.unwrap().source_id, t.id);
    }

    #[test]
    fn list_switchable_excludes_active_and_done() {
        let conn = test_conn();
        let t1 = create_task(&conn, "active", None).unwrap();
        start_timer(&conn, t1.id).unwrap();
        let t2 = create_task(&conn, "pending one", None).unwrap();
        let t3 = create_task(&conn, "to be done", None).unwrap();
        start_timer(&conn, t3.id).unwrap();
        complete(&conn, t3.id).unwrap();

        let list = list_switchable(&conn).unwrap();
        let ids: Vec<i64> = list.iter().map(|r| r.source_id).collect();
        assert!(ids.contains(&t2.id));
        assert!(!ids.contains(&t1.id)); // active excluded
        assert!(!ids.contains(&t3.id)); // done excluded
    }
}
```

- [ ] **Step 2: 运行测试**

```bash
cd /home/jason/workspace/mindtap/src-tauri && cargo test record
```

Expected: 3 passed

- [ ] **Step 3: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src-tauri/src/db/record.rs
git commit -m "feat(db): record 视图查询 (list/list_by_kind/list_switchable/get_active)"
```

---

### Task 10: 实现 task::archive（归档）

**Files:**
- Modify: `src-tauri/src/db/task.rs`

- [ ] **Step 1: 追加 archive 函数**

在 `task.rs` 末尾追加：

```rust
/// 归档：任何状态 → archived（设置 archived_at）
pub fn archive(conn: &Connection, task_id: i64) -> AppResult<Task> {
    let now = now_ms();
    conn.execute(
        "UPDATE task SET archived_at = ?1, updated_at = ?1 WHERE id = ?2",
        params![now, task_id],
    )?;
    get_task(conn, task_id)
}
```

- [ ] **Step 2: 追加 archive 测试**

```rust
    #[test]
    fn archive_sets_archived_at() {
        let conn = test_conn();
        let t = create_task(&conn, "x", None).unwrap();
        start_timer(&conn, t.id).unwrap();
        complete(&conn, t.id).unwrap();
        let archived = archive(&conn, t.id).unwrap();
        assert!(archived.archived_at.is_some());
    }
```

- [ ] **Step 3: 运行测试**

```bash
cd /home/jason/workspace/mindtap/src-tauri && cargo test archive
```

Expected: 1 passed

- [ ] **Step 4: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src-tauri/src/db/task.rs
git commit -m "feat(db): task::archive 设置 archived_at"
```

---

## Phase 2：Tauri Commands + 事件

### Task 11: 创建 commands 模块骨架

**Files:**
- Create: `src-tauri/src/commands/mod.rs`
- Create: `src-tauri/src/commands/task_cmd.rs`（占位）
- Create: `src-tauri/src/commands/idea_cmd.rs`（占位）
- Create: `src-tauri/src/commands/check_in_cmd.rs`（占位）
- Create: `src-tauri/src/commands/floating_cmd.rs`（占位）
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: 创建 commands/mod.rs**

```rust
// src-tauri/src/commands/mod.rs
pub mod task_cmd;
pub mod idea_cmd;
pub mod check_in_cmd;
pub mod floating_cmd;
```

- [ ] **Step 2: 创建占位文件**

```rust
// src-tauri/src/commands/task_cmd.rs
// 占位
```

```rust
// src-tauri/src/commands/idea_cmd.rs
// 占位
```

```rust
// src-tauri/src/commands/check_in_cmd.rs
// 占位
```

```rust
// src-tauri/src/commands/floating_cmd.rs
// 占位
```

- [ ] **Step 3: 修改 lib.rs 引用 commands 模块**

```rust
mod commands;
mod db;
mod error;

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .plugin(tauri_plugin_opener::init())
        .setup(|app| {
            db::init(app).map_err(|e| e.to_string())?;
            Ok(())
        })
        .invoke_handler(tauri::generate_handler![])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

- [ ] **Step 4: 验证编译**

```bash
cd /home/jason/workspace/mindtap/src-tauri && cargo check
```

Expected: 编译通过

- [ ] **Step 5: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src-tauri/src/commands src-tauri/src/lib.rs
git commit -m "feat(cmd): 创建 commands 模块骨架"
```

---

### Task 12: 实现 task 系列 command

**Files:**
- Modify: `src-tauri/src/commands/task_cmd.rs`
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: 实现 task_cmd.rs**

```rust
// src-tauri/src/commands/task_cmd.rs
use crate::db::task;
use crate::db::DbState;
use crate::error::AppResult;
use tauri::{Emitter, State};

#[tauri::command]
pub fn task_create(
    state: State<'_, DbState>,
    app: tauri::AppHandle,
    content: String,
    due_at: Option<i64>,
) -> AppResult<task::Task> {
    let conn = state.0.lock().unwrap();
    let t = task::create_task(&conn, &content, due_at)?;
    let _ = app.emit("record-updated", ());
    Ok(t)
}

#[tauri::command]
pub fn task_start_timer(
    state: State<'_, DbState>,
    app: tauri::AppHandle,
    id: i64,
) -> AppResult<task::Task> {
    let conn = state.0.lock().unwrap();
    let t = task::start_timer(&conn, id)?;
    let _ = app.emit("focus-changed", &t);
    Ok(t)
}

#[tauri::command]
pub fn task_pause(
    state: State<'_, DbState>,
    app: tauri::AppHandle,
    id: i64,
) -> AppResult<task::Task> {
    let conn = state.0.lock().unwrap();
    let t = task::pause(&conn, id)?;
    let _ = app.emit("focus-changed", &t);
    Ok(t)
}

#[tauri::command]
pub fn task_resume(
    state: State<'_, DbState>,
    app: tauri::AppHandle,
    id: i64,
) -> AppResult<task::Task> {
    let conn = state.0.lock().unwrap();
    let t = task::resume(&conn, id)?;
    let _ = app.emit("focus-changed", &t);
    Ok(t)
}

#[tauri::command]
pub fn task_complete(
    state: State<'_, DbState>,
    app: tauri::AppHandle,
    id: i64,
) -> AppResult<task::Task> {
    let conn = state.0.lock().unwrap();
    let t = task::complete(&conn, id)?;
    let _ = app.emit("focus-changed", &t);
    Ok(t)
}

#[tauri::command]
pub fn task_undo(
    state: State<'_, DbState>,
    app: tauri::AppHandle,
    id: i64,
) -> AppResult<task::Task> {
    let conn = state.0.lock().unwrap();
    let t = task::undo(&conn, id)?;
    let _ = app.emit("focus-changed", &t);
    Ok(t)
}

#[tauri::command]
pub fn task_switch(
    state: State<'_, DbState>,
    app: tauri::AppHandle,
    id: i64,
) -> AppResult<task::Task> {
    let conn = state.0.lock().unwrap();
    let t = task::switch_focus(&conn, id)?;
    let _ = app.emit("focus-changed", &t);
    Ok(t)
}

#[tauri::command]
pub fn task_archive(
    state: State<'_, DbState>,
    app: tauri::AppHandle,
    id: i64,
) -> AppResult<task::Task> {
    let conn = state.0.lock().unwrap();
    let t = task::archive(&conn, id)?;
    let _ = app.emit("record-updated", ());
    Ok(t)
}
```

- [ ] **Step 2: 修改 lib.rs 注册 task commands**

```rust
mod commands;
mod db;
mod error;

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .plugin(tauri_plugin_opener::init())
        .setup(|app| {
            db::init(app).map_err(|e| e.to_string())?;
            Ok(())
        })
        .invoke_handler(tauri::generate_handler![
            commands::task_cmd::task_create,
            commands::task_cmd::task_start_timer,
            commands::task_cmd::task_pause,
            commands::task_cmd::task_resume,
            commands::task_cmd::task_complete,
            commands::task_cmd::task_undo,
            commands::task_cmd::task_switch,
            commands::task_cmd::task_archive,
        ])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

- [ ] **Step 3: 验证编译**

```bash
cd /home/jason/workspace/mindtap/src-tauri && cargo check
```

Expected: 编译通过

- [ ] **Step 4: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src-tauri/src/commands/task_cmd.rs src-tauri/src/lib.rs
git commit -m "feat(cmd): 8 个 task_* command + focus-changed/record-updated 事件"
```

---

### Task 13: 实现 idea / check_in / record / floating 辅助 command

**Files:**
- Modify: `src-tauri/src/commands/idea_cmd.rs`
- Modify: `src-tauri/src/commands/check_in_cmd.rs`
- Create: `src-tauri/src/commands/record_cmd.rs`
- Create: `src-tauri/src/commands/floating_cmd.rs`
- Modify: `src-tauri/src/commands/mod.rs`
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: 实现 idea_cmd.rs**

```rust
// src-tauri/src/commands/idea_cmd.rs
use crate::db::idea;
use crate::db::DbState;
use crate::error::AppResult;
use tauri::{Emitter, State};

#[tauri::command]
pub fn idea_create(
    state: State<'_, DbState>,
    app: tauri::AppHandle,
    content: String,
) -> AppResult<i64> {
    let conn = state.0.lock().unwrap();
    let id = idea::create_idea(&conn, &content)?;
    let _ = app.emit("record-updated", ());
    Ok(id)
}
```

- [ ] **Step 2: 实现 check_in_cmd.rs**

```rust
// src-tauri/src/commands/check_in_cmd.rs
use crate::db::check_in;
use crate::db::DbState;
use crate::error::AppResult;
use tauri::{Emitter, State};

#[tauri::command]
pub fn check_in_create(
    state: State<'_, DbState>,
    app: tauri::AppHandle,
    habit: String,
    note: Option<String>,
) -> AppResult<i64> {
    let conn = state.0.lock().unwrap();
    let id = check_in::create_check_in(&conn, &habit, note.as_deref())?;
    let _ = app.emit("record-updated", ());
    Ok(id)
}
```

- [ ] **Step 3: 创建 record_cmd.rs（主窗时间线用）**

```rust
// src-tauri/src/commands/record_cmd.rs
use crate::db::record::{self, Record};
use crate::db::DbState;
use crate::error::AppResult;
use tauri::State;

#[tauri::command]
pub fn record_list(
    state: State<'_, DbState>,
    limit: Option<i64>,
    hide_archived: Option<bool>,
) -> AppResult<Vec<Record>> {
    let conn = state.0.lock().unwrap();
    record::list_records(&conn, limit.unwrap_or(50), hide_archived.unwrap_or(true))
}

#[tauri::command]
pub fn record_list_by_kind(
    state: State<'_, DbState>,
    kind: String,
    limit: Option<i64>,
) -> AppResult<Vec<Record>> {
    let conn = state.0.lock().unwrap();
    record::list_by_kind(&conn, &kind, limit.unwrap_or(50))
}

#[tauri::command]
pub fn record_list_switchable(state: State<'_, DbState>) -> AppResult<Vec<Record>> {
    let conn = state.0.lock().unwrap();
    record::list_switchable(&conn)
}

#[tauri::command]
pub fn record_get_active(state: State<'_, DbState>) -> AppResult<Option<Record>> {
    let conn = state.0.lock().unwrap();
    record::get_active_task(&conn)
}
```

- [ ] **Step 4: 创建 floating_cmd.rs（控制浮动窗可见）**

```rust
// src-tauri/src/commands/floating_cmd.rs
use tauri::Manager;

#[tauri::command]
pub fn floating_show(app: tauri::AppHandle) -> Result<(), String> {
    if let Some(w) = app.get_webview_window("floating") {
        w.show().map_err(|e| e.to_string())?;
        w.set_focus().map_err(|e| e.to_string())?;
    }
    Ok(())
}

#[tauri::command]
pub fn floating_hide(app: tauri::AppHandle) -> Result<(), String> {
    if let Some(w) = app.get_webview_window("floating") {
        w.hide().map_err(|e| e.to_string())?;
    }
    Ok(())
}

#[tauri::command]
pub fn floating_toggle(app: tauri::AppHandle) -> Result<(), String> {
    if let Some(w) = app.get_webview_window("floating") {
        if w.is_visible().unwrap_or(false) {
            w.hide().map_err(|e| e.to_string())?;
        } else {
            w.show().map_err(|e| e.to_string())?;
            w.set_focus().map_err(|e| e.to_string())?;
        }
    }
    Ok(())
}
```

- [ ] **Step 5: 修改 commands/mod.rs**

```rust
pub mod task_cmd;
pub mod idea_cmd;
pub mod check_in_cmd;
pub mod record_cmd;
pub mod floating_cmd;
```

- [ ] **Step 6: 修改 lib.rs 注册所有 command**

```rust
        .invoke_handler(tauri::generate_handler![
            commands::task_cmd::task_create,
            commands::task_cmd::task_start_timer,
            commands::task_cmd::task_pause,
            commands::task_cmd::task_resume,
            commands::task_cmd::task_complete,
            commands::task_cmd::task_undo,
            commands::task_cmd::task_switch,
            commands::task_cmd::task_archive,
            commands::idea_cmd::idea_create,
            commands::check_in_cmd::check_in_create,
            commands::record_cmd::record_list,
            commands::record_cmd::record_list_by_kind,
            commands::record_cmd::record_list_switchable,
            commands::record_cmd::record_get_active,
            commands::floating_cmd::floating_show,
            commands::floating_cmd::floating_hide,
            commands::floating_cmd::floating_toggle,
        ])
```

- [ ] **Step 7: 验证编译**

```bash
cd /home/jason/workspace/mindtap/src-tauri && cargo check
```

Expected: 编译通过

- [ ] **Step 8: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src-tauri/src/commands src-tauri/src/lib.rs
git commit -m "feat(cmd): idea/check_in/record/floating command 完整注册"
```

---

### Task 14: 添加 floating window 配置（tauri.conf.json）

**Files:**
- Modify: `src-tauri/tauri.conf.json`

- [ ] **Step 1: 添加 floating window 配置**

替换 `app.windows` 块：

```json
  "app": {
    "windows": [
      {
        "label": "main",
        "title": "轻念 · Mindtap",
        "width": 800,
        "height": 600
      },
      {
        "label": "floating",
        "title": "Mindtap",
        "url": "floating.html",
        "width": 320,
        "height": 36,
        "minWidth": 320,
        "minHeight": 36,
        "maxWidth": 360,
        "maxHeight": 320,
        "transparent": true,
        "decorations": false,
        "alwaysOnTop": true,
        "resizable": false,
        "skipTaskbar": true,
        "focus": false,
        "shadow": false,
        "visible": true
      }
    ],
    "security": {
      "csp": null
    }
  }
```

- [ ] **Step 2: 验证 Tauri 仍能解析**

```bash
cd /home/jason/workspace/mindtap && npx tauri --version
```

Expected: tauri 2.x

- [ ] **Step 3: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src-tauri/tauri.conf.json
git commit -m "feat(tauri): 添加 floating window 配置 (320×36 transparent)"
```

---

### Task 15: 全局快捷键（macOS Cmd+Shift+Space / Windows Ctrl+Shift+Space）

**Files:**
- Modify: `src-tauri/Cargo.toml`
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: 添加 tauri-plugin-global-shortcut 依赖**

在 `[dependencies]` 追加：

```toml
tauri-plugin-global-shortcut = "2"
```

- [ ] **Step 2: 实现全局快捷键逻辑**

替换 `src-tauri/src/lib.rs`：

```rust
mod commands;
mod db;
mod error;

use tauri_plugin_global_shortcut::{Code, Modifiers, Shortcut, ShortcutState};

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    let toggle_shortcut = Shortcut::new(
        Some(if cfg!(target_os = "macos") {
            Modifiers::SUPER | Modifiers::SHIFT
        } else {
            Modifiers::CONTROL | Modifiers::SHIFT
        }),
        Code::Space,
    );

    tauri::Builder::default()
        .plugin(tauri_plugin_opener::init())
        .plugin(
            tauri_plugin_global_shortcut::Builder::new()
                .with_handler(move |app, shortcut, event| {
                    if shortcut == &toggle_shortcut && event.state == ShortcutState::Pressed {
                        let _ = commands::floating_cmd::floating_toggle(app.clone());
                    }
                })
                .build(),
        )
        .setup(|app| {
            db::init(app).map_err(|e| e.to_string())?;
            app.global_shortcut().register(toggle_shortcut)?;
            Ok(())
        })
        .invoke_handler(tauri::generate_handler![
            commands::task_cmd::task_create,
            commands::task_cmd::task_start_timer,
            commands::task_cmd::task_pause,
            commands::task_cmd::task_resume,
            commands::task_cmd::task_complete,
            commands::task_cmd::task_undo,
            commands::task_cmd::task_switch,
            commands::task_cmd::task_archive,
            commands::idea_cmd::idea_create,
            commands::check_in_cmd::check_in_create,
            commands::record_cmd::record_list,
            commands::record_cmd::record_list_by_kind,
            commands::record_cmd::record_list_switchable,
            commands::record_cmd::record_get_active,
            commands::floating_cmd::floating_show,
            commands::floating_cmd::floating_hide,
            commands::floating_cmd::floating_toggle,
        ])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

- [ ] **Step 3: 验证编译**

```bash
cd /home/jason/workspace/mindtap/src-tauri && cargo check
```

Expected: 编译通过

- [ ] **Step 4: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src-tauri/Cargo.toml src-tauri/Cargo.lock src-tauri/src/lib.rs
git commit -m "feat(tauri): 全局快捷键 toggle 浮动窗 (Cmd+Shift+Space / Ctrl+Shift+Space)"
```

---

## Phase 3：前端 - FloatingWindow

### Task 16: 创建 floating.html + React 入口

**Files:**
- Create: `src/floating.html`
- Create: `src/floating/main.tsx`
- Create: `src/floating/App.tsx`（占位）
- Modify: `vite.config.ts`

- [ ] **Step 1: 创建 src/floating.html**

```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mindtap Floating</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/floating/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 2: 创建 src/floating/main.tsx**

```tsx
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./styles/glass.css";
import "./styles/floating.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
```

- [ ] **Step 3: 创建占位 App.tsx**

```tsx
export default function App() {
  return <div className="floating-root">Floating</div>;
}
```

- [ ] **Step 4: 创建 glass.css**

```css
/* src/floating/styles/glass.css */
:root {
  --glass-bg: rgba(255, 255, 255, 0.42);
  --glass-bg-fold: rgba(255, 255, 255, 0.34);
  --glass-bg-done: rgba(255, 255, 255, 0.32);
  --glass-bg-empty: rgba(255, 255, 255, 0.28);
  --glass-border: rgba(255, 255, 255, 0.65);
  --glass-blur: blur(32px) saturate(200%);
  --ink: #1d1d1f;
  --ink-2: #424245;
  --ink-3: #86868b;
  --blue: #007aff;
  --blue-soft: rgba(0, 122, 255, 0.12);
  --green: #34c759;
  --green-soft: rgba(52, 199, 89, 0.12);
  --green-deep: #1f8a3f;
  --green-tag: rgba(52, 199, 89, 0.18);
  --orange: #ff9500;
  --orange-deep: #cc7a00;
  --orange-tag: rgba(255, 149, 0, 0.18);
  --shadow-soft: inset 0 1px 0 rgba(255, 255, 255, 0.7),
    0 8px 24px rgba(0, 0, 0, 0.08);
}

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font: 14px/1.6 -apple-system, BlinkMacSystemFont, "SF Pro Display",
    "PingFang SC", "Segoe UI", sans-serif;
  color: var(--ink);
  background: transparent;
  overflow: hidden;
  user-select: none;
  -webkit-user-select: none;
}
```

- [ ] **Step 5: 创建 floating.css（占位）**

```css
/* src/floating/styles/floating.css */
.floating-root {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 36px;
  padding: 0;
}
```

- [ ] **Step 6: 修改 vite.config.ts（多入口）**

```ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import { resolve } from "path";

export default defineConfig({
  plugins: [react()],
  build: {
    rollupOptions: {
      input: {
        main: resolve(__dirname, "index.html"),
        floating: resolve(__dirname, "src/floating.html"),
      },
    },
  },
});
```

- [ ] **Step 7: 验证构建**

```bash
cd /home/jason/workspace/mindtap && npm run build
```

Expected: 构建成功，dist/floating.html 生成

- [ ] **Step 8: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src/floating.html src/floating vite.config.ts
git commit -m "feat(floating): 创建浮动窗 React 入口 + glass.css + vite 多入口"
```

---

### Task 17: 实现 tauri-bridge.ts（封装 invoke + event）

**Files:**
- Create: `src/lib/tauri-bridge.ts`

- [ ] **Step 1: 实现桥接层**

```ts
// src/lib/tauri-bridge.ts
import { invoke } from "@tauri-apps/api/core";
import { listen, type UnlistenFn } from "@tauri-apps/api/event";

export type Task = {
  id: number;
  content: string;
  status: "pending" | "active" | "paused" | "done";
  duration_ms: number;
  first_focused_at: number | null;
  focus_started_at: number | null;
  paused_at: number | null;
  focused_count: number;
  due_at: number | null;
  created_at: number;
  updated_at: number;
  archived_at: number | null;
};

export type Record = {
  id: number;
  kind: "task" | "idea" | "check_in";
  source_id: number;
  content: string;
  status: string | null;
  created_at: number;
};

export const api = {
  taskCreate: (content: string, dueAt?: number) =>
    invoke<Task>("task_create", { content, dueAt }),
  taskStartTimer: (id: number) => invoke<Task>("task_start_timer", { id }),
  taskPause: (id: number) => invoke<Task>("task_pause", { id }),
  taskResume: (id: number) => invoke<Task>("task_resume", { id }),
  taskComplete: (id: number) => invoke<Task>("task_complete", { id }),
  taskUndo: (id: number) => invoke<Task>("task_undo", { id }),
  taskSwitch: (id: number) => invoke<Task>("task_switch", { id }),
  taskArchive: (id: number) => invoke<Task>("task_archive", { id }),

  ideaCreate: (content: string) => invoke<number>("idea_create", { content }),
  checkInCreate: (habit: string, note?: string) =>
    invoke<number>("check_in_create", { habit, note }),

  recordList: (limit = 50, hideArchived = true) =>
    invoke<Record[]>("record_list", { limit, hideArchived }),
  recordListByKind: (kind: "task" | "idea" | "check_in", limit = 50) =>
    invoke<Record[]>("record_list_by_kind", { kind, limit }),
  recordListSwitchable: () => invoke<Record[]>("record_list_switchable"),
  recordGetActive: () => invoke<Record | null>("record_get_active"),

  floatingShow: () => invoke<void>("floating_show"),
  floatingHide: () => invoke<void>("floating_hide"),
  floatingToggle: () => invoke<void>("floating_toggle"),
};

export const events = {
  onFocusChanged: (cb: (task: Task) => void): Promise<UnlistenFn> =>
    listen<Task>("focus-changed", (e) => cb(e.payload)),
  onRecordUpdated: (cb: () => void): Promise<UnlistenFn> =>
    listen("record-updated", () => cb()),
  onTick: (cb: (now: number) => void): Promise<UnlistenFn> =>
    listen<number>("tick", (e) => cb(e.payload)),
};
```

- [ ] **Step 2: 验证类型检查**

```bash
cd /home/jason/workspace/mindtap && npx tsc --noEmit
```

Expected: 类型检查通过

- [ ] **Step 3: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src/lib/tauri-bridge.ts
git commit -m "feat(lib): tauri-bridge.ts 封装 17 个 invoke + 3 个 event listener"
```

---

### Task 18: 实现 useActiveTask + useTick hook

**Files:**
- Create: `src/floating/hooks/useActiveTask.ts`
- Create: `src/floating/hooks/useTick.ts`

- [ ] **Step 1: 实现 useActiveTask**

```ts
// src/floating/hooks/useActiveTask.ts
import { useEffect, useState } from "react";
import { api, events, type Record } from "../../lib/tauri-bridge";

export function useActiveTask() {
  const [active, setActive] = useState<Record | null>(null);

  useEffect(() => {
    api.recordGetActive().then(setActive).catch(console.error);
    const unlisten = events.onFocusChanged(() => {
      api.recordGetActive().then(setActive).catch(console.error);
    });
    return () => {
      unlisten.then((u) => u());
    };
  }, []);

  return active;
}
```

- [ ] **Step 2: 实现 useTick**

```ts
// src/floating/hooks/useTick.ts
import { useEffect, useState } from "react";
import { events } from "../../lib/tauri-bridge";

/** 每秒触发一次（后端推送） */
export function useTick() {
  const [now, setNow] = useState(() => Date.now());
  useEffect(() => {
    const interval = setInterval(() => setNow(Date.now()), 1000);
    return () => clearInterval(interval);
  }, []);
  return now;
}
```

- [ ] **Step 3: 验证类型检查**

```bash
cd /home/jason/workspace/mindtap && npx tsc --noEmit
```

Expected: 通过

- [ ] **Step 4: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src/floating/hooks
git commit -m "feat(floating): useActiveTask + useTick hooks"
```

---

### Task 19: 实现 FocusBlock 组件（折叠态 320×36）

**Files:**
- Create: `src/floating/components/FocusBlock.tsx`
- Modify: `src/floating/styles/floating.css`

- [ ] **Step 1: 创建 FocusBlock.tsx**

```tsx
// src/floating/components/FocusBlock.tsx
import { useActiveTask } from "../hooks/useActiveTask";
import { useTick } from "../hooks/useTick";
import { formatDuration } from "../../lib/time";

export function FocusBlock() {
  const active = useActiveTask();
  const now = useTick();

  if (!active) {
    return (
      <div className="fold-state empty">
        <div className="fold-inner">
          <span className="dot dot-empty" />
          <span className="tag tag-empty">EMPTY</span>
          <span className="title title-empty">点击记录第一个任务</span>
          <span className="icon icon-blue">+</span>
        </div>
      </div>
    );
  }

  if (active.status === "done") {
    return (
      <div className="fold-state done">
        <div className="fold-inner">
          <span className="dot dot-done" />
          <span className="tag tag-done">DONE</span>
          <span className="title title-done">{active.content}</span>
          <span className="time time-frozen">
            {formatDuration(0)} {/* done 时间由 record.due_at 决定，此处简化为 0 */}
          </span>
          <span className="icon icon-blue" title="撤销">↶</span>
        </div>
      </div>
    );
  }

  return (
    <div className="fold-state active">
      <div className="fold-inner">
        <span className="dot dot-active" />
        <span className="tag tag-focus">FOCUS</span>
        <span className="title">{active.content}</span>
        <span className="time">
          {formatDuration(0)} {/* 见 Task 20：实际从 task.duration_ms + 实时计算 */}
        </span>
        <span className="icon icon-green">⏸</span>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 创建 src/lib/time.ts**

```ts
// src/lib/time.ts
/** 毫秒 → "HH:MM:SS" 或 "MM:SS" */
export function formatDuration(ms: number): string {
  const totalSec = Math.floor(ms / 1000);
  const h = Math.floor(totalSec / 3600);
  const m = Math.floor((totalSec % 3600) / 60);
  const s = totalSec % 60;
  const pad = (n: number) => n.toString().padStart(2, "0");
  if (h > 0) return `${pad(h)}:${pad(m)}:${pad(s)}`;
  return `${pad(m)}:${pad(s)}`;
}
```

- [ ] **Step 3: 补充 floating.css（FocusBlock 样式）**

```css
/* 追加到 src/floating/styles/floating.css */

/* 折叠态基类 */
.fold-state {
  width: 320px;
  height: 36px;
  border-radius: 18px;
  backdrop-filter: var(--glass-blur);
  -webkit-backdrop-filter: var(--glass-blur);
  border: 1px solid var(--glass-border);
  box-shadow: var(--shadow-soft);
  cursor: pointer;
  display: flex;
  align-items: center;
}

.fold-state.active {
  background: var(--glass-bg);
}
.fold-state.done {
  background: var(--glass-bg-done);
  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.6),
    0 6px 18px rgba(0, 0, 0, 0.05);
}
.fold-state.empty {
  background: var(--glass-bg-empty);
  border: 1px dashed rgba(0, 122, 255, 0.25);
}

.fold-inner {
  display: flex;
  align-items: center;
  height: 100%;
  padding: 0 6px 0 10px;
  gap: 8px;
  font: 600 12.5px -apple-system, "PingFang SC";
  color: var(--ink);
  width: 100%;
}

.dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  flex-shrink: 0;
  align-self: center;
}
.dot-active {
  background: var(--green);
  animation: pulse 1.6s ease-in-out infinite;
}
.dot-done {
  background: var(--green);
  opacity: 0.5;
}
.dot-empty {
  background: rgba(0, 0, 0, 0.12);
}

.tag {
  font-size: 9px;
  padding: 2px 7px;
  border-radius: 8px;
  font-weight: 700;
  letter-spacing: 0.4px;
  display: inline-flex;
  align-items: center;
  height: 18px;
  flex-shrink: 0;
}
.tag-focus {
  background: var(--green-tag);
  color: var(--green-deep);
}
.tag-done {
  background: var(--green-soft);
  color: var(--green-deep);
}
.tag-empty {
  background: rgba(0, 0, 0, 0.05);
  color: var(--ink-3);
}

.title {
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-weight: 600;
  display: inline-flex;
  align-items: center;
  height: 18px;
}
.title-done {
  opacity: 0.7;
}
.title-empty {
  font-weight: 500;
  color: var(--ink-3);
}

.time {
  color: var(--blue);
  font-variant-numeric: tabular-nums;
  font-weight: 700;
  letter-spacing: -0.3px;
  display: inline-flex;
  align-items: center;
  height: 18px;
}
.time-frozen {
  color: var(--ink-3);
}

.icon {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  height: 24px;
  width: 24px;
  font-size: 15px;
  line-height: 1;
  flex-shrink: 0;
}
.icon-green {
  color: var(--green);
}
.icon-blue {
  color: var(--blue);
  font-size: 14px;
}

@keyframes pulse {
  0%,
  100% {
    box-shadow: 0 0 0 0 rgba(52, 199, 89, 0.4);
  }
  50% {
    box-shadow: 0 0 0 6px rgba(52, 199, 89, 0);
  }
}
```

- [ ] **Step 4: 验证类型检查**

```bash
cd /home/jason/workspace/mindtap && npx tsc --noEmit
```

Expected: 通过

- [ ] **Step 5: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src/floating/components/FocusBlock.tsx src/floating/styles/floating.css src/lib/time.ts
git commit -m "feat(floating): FocusBlock 3 态折叠 (active/done/empty) + formatDuration"
```

---

### Task 20: 完成 FocusBlock 真实计时（duration_ms + 实时累加）

**Files:**
- Modify: `src/floating/components/FocusBlock.tsx`
- Modify: `src/floating/hooks/useActiveTask.ts`（返回完整 Task 而非 Record）
- Modify: `src/floating/styles/floating.css`

- [ ] **Step 1: 升级 useActiveTask 返回 Task**

替换 `useActiveTask.ts`：

```ts
// src/floating/hooks/useActiveTask.ts
import { useEffect, useState } from "react";
import { api, events, type Task } from "../../lib/tauri-bridge";

export function useActiveTask(): Task | null {
  const [active, setActive] = useState<Task | null>(null);

  useEffect(() => {
    api
      .recordGetActive()
      .then((r) => {
        if (r && r.kind === "task") {
          // 同时获取 task 详情以拿到 duration_ms
          return api.taskStartTimer; // 占位，下面用新方法
        }
        return r;
      })
      .catch(console.error);
  }, []);

  // 简化：直接用 record_get_active 返回的内容，duration_ms 用 content 字段存（hack）
  // 实际应新增 record_get_active_task command
  return active as Task | null;
}
```

**注意**：上面是占位。正确的做法需要新增 `record_get_active_task` command。改用：

```ts
// src/floating/hooks/useActiveTask.ts
import { useEffect, useState } from "react";
import { api, events, type Task } from "../../lib/tauri-bridge";

export function useActiveTask(): Task | null {
  const [active, setActive] = useState<Task | null>(null);

  useEffect(() => {
    // 通过 task_switch 列表 + status 过滤，简化方法
    // 这里用 task list endpoint 占位
    refresh();
    const unlisten = events.onFocusChanged(() => {
      refresh();
    });
    return () => {
      unlisten.then((u) => u());
    };

    async function refresh() {
      const rec = await api.recordGetActive();
      if (rec && rec.kind === "task" && rec.status === "active") {
        // 从 record 拉 task 详情（通过新增 command）
        // 占位：直接用 rec.content
        setActive({
          id: rec.source_id,
          content: rec.content,
          status: "active",
          duration_ms: 0,
          first_focused_at: null,
          focus_started_at: null,
          paused_at: null,
          focused_count: 0,
          due_at: null,
          created_at: rec.created_at,
          updated_at: rec.created_at,
          archived_at: null,
        });
      } else {
        setActive(null);
      }
    }
  }, []);

  return active;
}
```

**关键**：上面的 `duration_ms: 0` 不准确。需要在后端新增一个 `record_get_active_task` command 返回完整 Task 字段。这在下一个 task 处理。

- [ ] **Step 2: 在 record_cmd.rs 新增 record_get_active_task**

```rust
// 追加到 src-tauri/src/commands/record_cmd.rs
use crate::db::task::{self, Task};

#[tauri::command]
pub fn record_get_active_task(state: State<'_, DbState>) -> AppResult<Option<Task>> {
    let conn = state.0.lock().unwrap();
    let rec = record::get_active_task(&conn)?;
    match rec {
        Some(r) => Ok(Some(task::get_task(&conn, r.source_id)?)),
        None => Ok(None),
    }
}
```

- [ ] **Step 3: 在 lib.rs 注册新 command**

在 `tauri::generate_handler!` 列表追加：

```rust
            commands::record_cmd::record_get_active_task,
```

- [ ] **Step 4: 在 tauri-bridge.ts 添加新方法**

```ts
  recordGetActiveTask: () => invoke<Task | null>("record_get_active_task"),
```

- [ ] **Step 5: 更新 useActiveTask 使用新方法**

```ts
// src/floating/hooks/useActiveTask.ts
import { useEffect, useState } from "react";
import { api, events, type Task } from "../../lib/tauri-bridge";

export function useActiveTask(): Task | null {
  const [active, setActive] = useState<Task | null>(null);

  useEffect(() => {
    refresh();
    const unlisten = events.onFocusChanged(() => refresh());
    return () => {
      unlisten.then((u) => u());
    };

    async function refresh() {
      try {
        const t = await api.recordGetActiveTask();
        setActive(t);
      } catch (e) {
        console.error(e);
      }
    }
  }, []);

  return active;
}
```

- [ ] **Step 6: 更新 FocusBlock 显示真实计时**

```tsx
// src/floating/components/FocusBlock.tsx
import { useActiveTask } from "../hooks/useActiveTask";
import { useTick } from "../hooks/useTick";
import { formatDuration } from "../../lib/time";

export function FocusBlock() {
  const active = useActiveTask();
  const now = useTick();

  if (!active) {
    return (
      <div className="fold-state empty">
        <div className="fold-inner">
          <span className="dot dot-empty" />
          <span className="tag tag-empty">EMPTY</span>
          <span className="title title-empty">点击记录第一个任务</span>
          <span className="icon icon-blue">+</span>
        </div>
      </div>
    );
  }

  if (active.status === "done") {
    return (
      <div className="fold-state done">
        <div className="fold-inner">
          <span className="dot dot-done" />
          <span className="tag tag-done">DONE</span>
          <span className="title title-done">{active.content}</span>
          <span className="time time-frozen">
            {formatDuration(active.duration_ms)}
          </span>
          <span
            className="icon icon-blue"
            title="撤销"
            onClick={(e) => {
              e.stopPropagation();
              api.taskUndo(active.id);
            }}
          >
            ↶
          </span>
        </div>
      </div>
    );
  }

  // active / paused
  const live =
    active.duration_ms +
    (active.status === "active" && active.focus_started_at
      ? now - active.focus_started_at
      : 0);

  return (
    <div className="fold-state active">
      <div className="fold-inner">
        <span className="dot dot-active" />
        <span className="tag tag-focus">FOCUS</span>
        <span className="title">{active.content}</span>
        <span className="time">{formatDuration(live)}</span>
        <span
          className="icon icon-green"
          onClick={(e) => {
            e.stopPropagation();
            if (active.status === "active") {
              api.taskPause(active.id);
            } else {
              api.taskResume(active.id);
            }
          }}
        >
          {active.status === "active" ? "⏸" : "▶"}
        </span>
      </div>
    </div>
  );
}
```

顶部加 import：

```tsx
import { api } from "../../lib/tauri-bridge";
```

- [ ] **Step 7: 验证编译**

```bash
cd /home/jason/workspace/mindtap && npx tsc --noEmit && (cd src-tauri && cargo check)
```

Expected: 都通过

- [ ] **Step 8: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src/floating/components/FocusBlock.tsx src/floating/hooks/useActiveTask.ts src/lib/tauri-bridge.ts src-tauri/src/commands/record_cmd.rs src-tauri/src/lib.rs
git commit -m "feat(floating): FocusBlock 真实计时 (duration_ms + 实时累加) + 点击交互"
```

---

### Task 21: 实现 SwitchDropdown 组件（顶靠右下拉）

**Files:**
- Create: `src/floating/components/SwitchDropdown.tsx`

- [ ] **Step 1: 创建 SwitchDropdown.tsx**

```tsx
// src/floating/components/SwitchDropdown.tsx
import { useEffect, useState } from "react";
import { api, type Record } from "../../lib/tauri-bridge";
import { formatDuration } from "../../lib/time";

export function SwitchDropdown() {
  const [open, setOpen] = useState(false);
  const [list, setList] = useState<Record[]>([]);

  useEffect(() => {
    if (!open) return;
    api.recordListSwitchable().then(setList).catch(console.error);
    const close = (e: MouseEvent) => {
      const target = e.target as HTMLElement;
      if (!target.closest(".switch-dropdown")) setOpen(false);
    };
    document.addEventListener("click", close);
    return () => document.removeEventListener("click", close);
  }, [open]);

  if (!open) {
    return (
      <button
        className="switch-btn"
        onClick={() => setOpen(true)}
        title="切换 Focus"
      >
        ⇄ <span className="caret">▼</span>
      </button>
    );
  }

  return (
    <div className="switch-dropdown">
      <button
        className="switch-btn open"
        onClick={(e) => {
          e.stopPropagation();
          setOpen(false);
        }}
      >
        ⇄ <span className="caret">▲</span>
      </button>
      <div className="dropdown-panel">
        <div className="dropdown-header">⇄ 切换到</div>
        {list.length === 0 && (
          <div className="dropdown-empty">没有可切换的任务</div>
        )}
        {list.map((r) => (
          <div className="dropdown-item" key={r.id}>
            <span
              className={
                r.status === "paused" ? "item-tag tag-paused" : "item-tag tag-pending"
              }
            >
              {r.status === "paused" ? "暂停" : "未开始"}
            </span>
            <span className="item-title">{r.content}</span>
            <span className="item-time">
              {r.status === "paused" ? formatDuration(0) : ""}
            </span>
            <button
              className="item-play"
              onClick={() => api.taskSwitch(r.source_id)}
            >
              ⏵
            </button>
          </div>
        ))}
        {list.length > 0 && (
          <div className="dropdown-footer">查看全部 {list.length} 个 ›</div>
        )}
      </div>
    </div>
  );
}
```

**注意**：`formatDuration(0)` 是简化，实际 paused 任务的已计时需要从 task 详情取。

- [ ] **Step 2: 追加下拉样式**

```css
/* 追加到 src/floating/styles/floating.css */
.switch-btn {
  display: inline-flex;
  align-items: center;
  gap: 3px;
  height: 24px;
  padding: 0 9px;
  background: rgba(255, 255, 255, 0.5);
  color: var(--ink);
  border: none;
  border-radius: 7px;
  font-size: 11.5px;
  font-weight: 600;
  cursor: pointer;
}
.switch-btn:hover {
  background: rgba(255, 255, 255, 0.7);
}
.switch-btn.open {
  background: rgba(0, 122, 255, 0.18);
  color: var(--blue);
}
.caret {
  font-size: 8px;
}

.switch-dropdown {
  position: relative;
}
.dropdown-panel {
  position: absolute;
  top: 28px;
  right: 0;
  width: 240px;
  background: rgba(255, 255, 255, 0.85);
  backdrop-filter: blur(20px) saturate(180%);
  -webkit-backdrop-filter: blur(20px) saturate(180%);
  border: 1px solid var(--glass-border);
  border-radius: 10px;
  padding: 6px;
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
  z-index: 100;
}
.dropdown-header {
  padding: 5px 8px;
  font-size: 10px;
  color: var(--ink-3);
  font-weight: 700;
  letter-spacing: 0.4px;
}
.dropdown-empty {
  padding: 10px;
  text-align: center;
  color: var(--ink-3);
  font-size: 11.5px;
}
.dropdown-item {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 5px 8px;
  border-radius: 7px;
  margin-bottom: 1px;
}
.dropdown-item:hover {
  background: rgba(0, 122, 255, 0.08);
}
.item-tag {
  font-size: 9px;
  padding: 1px 5px;
  border-radius: 5px;
  font-weight: 700;
  flex-shrink: 0;
  width: 30px;
  text-align: center;
}
.tag-paused {
  background: var(--orange-tag);
  color: var(--orange-deep);
}
.tag-pending {
  background: rgba(0, 0, 0, 0.05);
  color: var(--ink-3);
}
.item-title {
  flex: 1;
  font-weight: 600;
  font-size: 11.5px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.item-time {
  color: var(--orange);
  font-size: 10px;
  font-weight: 700;
  font-variant-numeric: tabular-nums;
}
.item-play {
  background: none;
  border: none;
  color: var(--blue);
  font-size: 12px;
  cursor: pointer;
  padding: 0 4px;
}
.dropdown-footer {
  padding: 5px 8px;
  text-align: center;
  font-size: 11px;
  color: var(--blue);
  font-weight: 600;
  margin-top: 1px;
  cursor: pointer;
}
```

- [ ] **Step 3: 验证类型**

```bash
cd /home/jason/workspace/mindtap && npx tsc --noEmit
```

Expected: 通过

- [ ] **Step 4: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src/floating/components/SwitchDropdown.tsx src/floating/styles/floating.css
git commit -m "feat(floating): SwitchDropdown 顶靠右下拉 (未开始+暂停清单)"
```

---

### Task 22: 实现 ControlRow + ExpandState 组件

**Files:**
- Create: `src/floating/components/ControlRow.tsx`
- Create: `src/floating/components/ExpandState.tsx`

- [ ] **Step 1: 创建 ControlRow.tsx**

```tsx
// src/floating/components/ControlRow.tsx
import { api, type Task } from "../../lib/tauri-bridge";
import { SwitchDropdown } from "./SwitchDropdown";

export function ControlRow({ active }: { active: Task | null }) {
  return (
    <div className="control-row">
      {active && (
        <>
          <button
            className="btn-pause"
            onClick={() => {
              if (active.status === "active") api.taskPause(active.id);
              else if (active.status === "paused") api.taskResume(active.id);
            }}
          >
            {active.status === "active" ? "⏸ 暂停" : "▶ 继续"}
          </button>
          <button
            className="btn-complete"
            onClick={() => active.status !== "done" && api.taskComplete(active.id)}
          >
            ✓ 完成
          </button>
        </>
      )}
      <SwitchDropdown />
    </div>
  );
}
```

- [ ] **Step 2: 创建 ExpandState.tsx**

```tsx
// src/floating/components/ExpandState.tsx
import { useState } from "react";
import { api } from "../../lib/tauri-bridge";
import { useActiveTask } from "../hooks/useActiveTask";
import { ControlRow } from "./ControlRow";

type Type = "task" | "idea" | "check_in";

export function ExpandState() {
  const active = useActiveTask();
  const [content, setContent] = useState("");
  const [type, setType] = useState<Type>("task");
  const [submitting, setSubmitting] = useState(false);

  const submit = async (dueAt?: number) => {
    if (!content.trim() || submitting) return;
    setSubmitting(true);
    try {
      if (type === "task") {
        const t = await api.taskCreate(content, dueAt);
        await api.taskStartTimer(t.id);
      } else if (type === "idea") {
        await api.ideaCreate(content);
      } else {
        await api.checkInCreate(content);
      }
      setContent("");
    } catch (e) {
      console.error(e);
    } finally {
      setSubmitting(false);
    }
  };

  const endOfToday = () => {
    const d = new Date();
    d.setHours(23, 59, 59, 999);
    return d.getTime();
  };
  const endOfTomorrow = () => {
    const d = new Date();
    d.setDate(d.getDate() + 1);
    d.setHours(23, 59, 59, 999);
    return d.getTime();
  };

  return (
    <div className="expand-state">
      {active && (
        <div className="expand-focus">
          <span className="dot dot-active" />
          <span className="tag tag-focus">FOCUS</span>
          <span className="title">{active.content}</span>
        </div>
      )}
      <ControlRow active={active} />
      <div className="divider" />
      <textarea
        className="content-input"
        value={content}
        onChange={(e) => setContent(e.target.value)}
        placeholder="事项内容…"
        autoFocus
      />
      <div className="type-row">
        <button
          className={type === "task" ? "type-btn active" : "type-btn"}
          onClick={() => setType("task")}
        >
          ✓ 待办
        </button>
        <button
          className={type === "idea" ? "type-btn active" : "type-btn"}
          onClick={() => setType("idea")}
        >
          ✎ 灵感
        </button>
        <button
          className={type === "check_in" ? "type-btn active" : "type-btn"}
          onClick={() => setType("check_in")}
        >
          ⚡ 打卡
        </button>
      </div>
      <div className="cta-row">
        {type === "task" && (
          <>
            <button className="date-btn" onClick={() => submit(endOfToday())}>
              📅 稍后
            </button>
            <button className="date-btn" onClick={() => submit(endOfTomorrow())}>
              📅 明天
            </button>
          </>
        )}
        <button
          className="start-btn"
          onClick={() => submit()}
          disabled={submitting}
        >
          ⏵ 开始计时
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: 追加 ExpandState 样式**

```css
/* 追加到 src/floating/styles/floating.css */
.expand-state {
  width: 360px;
  height: 280px;
  border-radius: 18px;
  background: var(--glass-bg);
  backdrop-filter: var(--glass-blur);
  -webkit-backdrop-filter: var(--glass-blur);
  border: 1px solid var(--glass-border);
  box-shadow: var(--shadow-soft);
  padding: 12px 14px;
  display: flex;
  flex-direction: column;
  gap: 8px;
  font: 12.5px -apple-system, "PingFang SC";
  color: var(--ink);
}

.expand-focus {
  display: flex;
  align-items: center;
  gap: 9px;
  background: rgba(52, 199, 89, 0.08);
  border-radius: 12px;
  padding: 9px 11px;
  flex-shrink: 0;
}
.expand-focus .title {
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-weight: 600;
}

.control-row {
  display: flex;
  gap: 5px;
  align-items: center;
  height: 24px;
  flex-shrink: 0;
}
.control-row > :not(.switch-dropdown) {
  flex: 1;
}
.btn-pause,
.btn-complete {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 24px;
  border: none;
  border-radius: 7px;
  font-size: 11px;
  font-weight: 600;
  cursor: pointer;
}
.btn-pause {
  background: rgba(0, 122, 255, 0.12);
  color: var(--blue);
}
.btn-complete {
  background: rgba(52, 199, 89, 0.12);
  color: var(--green-deep);
}

.divider {
  height: 1px;
  background: rgba(0, 0, 0, 0.08);
  flex-shrink: 0;
  margin: 0 -14px;
}

.content-input {
  flex: 1;
  background: rgba(255, 255, 255, 0.45);
  border: 1px solid rgba(0, 0, 0, 0.06);
  border-radius: 8px;
  padding: 8px;
  font: 12.5px -apple-system, "PingFang SC";
  color: var(--ink);
  resize: none;
  outline: none;
}
.content-input:focus {
  border-color: var(--blue);
}

.type-row,
.cta-row {
  display: flex;
  gap: 4px;
  flex-shrink: 0;
}
.type-row {
  height: 20px;
}
.cta-row {
  height: 26px;
}
.type-btn {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255, 255, 255, 0.5);
  color: var(--ink);
  border: none;
  border-radius: 5px;
  font-size: 10px;
  font-weight: 600;
  cursor: pointer;
}
.type-btn.active {
  background: var(--blue-soft);
  color: var(--blue);
}
.date-btn {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255, 255, 255, 0.5);
  color: var(--ink);
  border: none;
  border-radius: 5px;
  font-size: 10.5px;
  font-weight: 600;
  cursor: pointer;
}
.start-btn {
  flex: 2;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--blue);
  color: white;
  border: none;
  border-radius: 5px;
  font-size: 11px;
  font-weight: 600;
  cursor: pointer;
}
.start-btn:disabled {
  opacity: 0.5;
}
```

- [ ] **Step 4: 验证编译**

```bash
cd /home/jason/workspace/mindtap && npx tsc --noEmit
```

Expected: 通过

- [ ] **Step 5: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src/floating/components/ControlRow.tsx src/floating/components/ExpandState.tsx src/floating/styles/floating.css
git commit -m "feat(floating): ControlRow + ExpandState 完整 (textarea + 类型 + 截止 + CTA)"
```

---

### Task 23: 实现折叠/展开切换逻辑 + 完整 App.tsx

**Files:**
- Modify: `src/floating/App.tsx`

- [ ] **Step 1: 替换 App.tsx**

```tsx
// src/floating/App.tsx
import { useState } from "react";
import { FocusBlock } from "./components/FocusBlock";
import { ExpandState } from "./components/ExpandState";

export default function App() {
  const [expanded, setExpanded] = useState(false);

  if (expanded) {
    return (
      <ExpandState />
    );
  }

  return (
    <div
      onClick={() => setExpanded(true)}
      style={{ display: "flex", alignItems: "center", padding: 0 }}
    >
      <FocusBlock />
    </div>
  );
}
```

注意：折叠态点击展开后，**需要浮动窗自动 resize** 到 360×280。这通过 Tauri 2 的 `setSize` 在前端调用，详见 Task 24。

- [ ] **Step 2: 验证编译**

```bash
cd /home/jason/workspace/mindtap && npx tsc --noEmit
```

Expected: 通过

- [ ] **Step 3: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src/floating/App.tsx
git commit -m "feat(floating): App.tsx 折叠/展开切换"
```

---

### Task 24: 浮动窗自动 resize（折叠 320×36 ↔ 展开 360×280）

**Files:**
- Modify: `src/floating/App.tsx`

- [ ] **Step 1: 添加 useFloatingWindow hook + 自动 resize**

```tsx
// src/floating/App.tsx
import { useEffect, useState } from "react";
import { getCurrentWindow } from "@tauri-apps/api/window";
import { FocusBlock } from "./components/FocusBlock";
import { ExpandState } from "./components/ExpandState";

export default function App() {
  const [expanded, setExpanded] = useState(false);
  const win = getCurrentWindow();

  useEffect(() => {
    if (expanded) {
      win.setSize(new LogicalSize(360, 280));
    } else {
      win.setSize(new LogicalSize(320, 36));
    }
  }, [expanded, win]);

  if (expanded) return <ExpandState />;

  return (
    <div onClick={() => setExpanded(true)}>
      <FocusBlock />
    </div>
  );
}

// 顶部添加 import
import { LogicalSize } from "@tauri-apps/api/dpi";
```

完整修正版：

```tsx
// src/floating/App.tsx
import { useEffect, useState } from "react";
import { getCurrentWindow, LogicalSize } from "@tauri-apps/api/window";
import { FocusBlock } from "./components/FocusBlock";
import { ExpandState } from "./components/ExpandState";

export default function App() {
  const [expanded, setExpanded] = useState(false);
  const win = getCurrentWindow();

  useEffect(() => {
    win.setSize(new LogicalSize(expanded ? 360 : 320, expanded ? 280 : 36));
  }, [expanded, win]);

  if (expanded) return <ExpandState />;

  return (
    <div onClick={() => setExpanded(true)}>
      <FocusBlock />
    </div>
  );
}
```

- [ ] **Step 2: 验证编译**

```bash
cd /home/jason/workspace/mindtap && npx tsc --noEmit
```

Expected: 通过

- [ ] **Step 3: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src/floating/App.tsx
git commit -m "feat(floating): 折叠/展开自动 resize (320×36 ↔ 360×280)"
```

---

### Task 25: 浮动窗位置记忆（每次启动恢复到上次位置）

**Files:**
- Create: `src/floating/hooks/useWindowPosition.ts`
- Modify: `src/floating/App.tsx`

- [ ] **Step 1: 创建 useWindowPosition hook**

```ts
// src/floating/hooks/useWindowPosition.ts
import { useEffect } from "react";
import { getCurrentWindow, PhysicalPosition } from "@tauri-apps/api/window";

const KEY = "floating-position";

type Pos = { x: number; y: number };

export function useWindowPosition() {
  useEffect(() => {
    const win = getCurrentWindow();
    const saved = localStorage.getItem(KEY);
    if (saved) {
      try {
        const pos: Pos = JSON.parse(saved);
        win.setPosition(new PhysicalPosition(pos.x, pos.y));
      } catch (e) {
        console.error(e);
      }
    }
    const unlisten = win.onMoved(({ payload }) => {
      localStorage.setItem(KEY, JSON.stringify(payload));
    });
    return () => {
      unlisten.then((u) => u());
    };
  }, []);
}
```

- [ ] **Step 2: App.tsx 集成**

在 `App.tsx` 顶部加：

```tsx
import { useWindowPosition } from "./hooks/useWindowPosition";

export default function App() {
  useWindowPosition();
  // ... 其余不变
}
```

- [ ] **Step 3: 验证编译**

```bash
cd /home/jason/workspace/mindtap && npx tsc --noEmit
```

Expected: 通过

- [ ] **Step 4: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src/floating/hooks/useWindowPosition.ts src/floating/App.tsx
git commit -m "feat(floating): 浮动窗位置记忆 (localStorage + onMoved)"
```

---

## Phase 4：主窗时间线

### Task 26: 实现 RecordTimeline 组件

**Files:**
- Create: `src/timeline/RecordTimeline.tsx`
- Create: `src/timeline/hooks/useRecords.ts`
- Create: `src/timeline/styles/timeline.css`

- [ ] **Step 1: 创建 useRecords hook**

```ts
// src/timeline/hooks/useRecords.ts
import { useEffect, useState } from "react";
import { api, events, type Record } from "../../lib/tauri-bridge";

export type KindFilter = "all" | "task" | "idea" | "check_in";

export function useRecords(kind: KindFilter = "all") {
  const [records, setRecords] = useState<Record[]>([]);

  useEffect(() => {
    refresh();
    const unlisten = events.onRecordUpdated(() => refresh());
    return () => {
      unlisten.then((u) => u());
    };

    async function refresh() {
      try {
        let data: Record[];
        if (kind === "all") {
          data = await api.recordList(100, true);
        } else {
          data = await api.recordListByKind(kind, 100);
        }
        setRecords(data);
      } catch (e) {
        console.error(e);
      }
    }
  }, [kind]);

  return records;
}
```

- [ ] **Step 2: 创建 RecordTimeline.tsx**

```tsx
// src/timeline/RecordTimeline.tsx
import { useState } from "react";
import { useRecords, type KindFilter } from "./hooks/useRecords";
import "./styles/timeline.css";

function formatTime(ms: number): string {
  const d = new Date(ms);
  const now = new Date();
  const diffMs = now.getTime() - ms;
  const diffMin = Math.floor(diffMs / 60000);
  if (diffMin < 1) return "刚刚";
  if (diffMin < 60) return `${diffMin} 分钟前`;
  if (diffMin < 60 * 24) return `${Math.floor(diffMin / 60)} 小时前`;
  return d.toLocaleDateString("zh-CN");
}

export function RecordTimeline() {
  const [kind, setKind] = useState<KindFilter>("all");
  const records = useRecords(kind);

  return (
    <div className="timeline">
      <div className="timeline-tabs">
        <button
          className={kind === "all" ? "tab active" : "tab"}
          onClick={() => setKind("all")}
        >
          全部
        </button>
        <button
          className={kind === "task" ? "tab active" : "tab"}
          onClick={() => setKind("task")}
        >
          任务
        </button>
        <button
          className={kind === "idea" ? "tab active" : "tab"}
          onClick={() => setKind("idea")}
        >
          灵感
        </button>
        <button
          className={kind === "check_in" ? "tab active" : "tab"}
          onClick={() => setKind("check_in")}
        >
          打卡
        </button>
      </div>
      <div className="timeline-list">
        {records.length === 0 && (
          <div className="empty">还没有记录</div>
        )}
        {records.map((r) => (
          <div className="record" key={r.id}>
            <span className={`record-tag tag-${r.kind}`}>
              {r.kind === "task" ? "TASK" : r.kind === "idea" ? "IDEA" : "打卡"}
            </span>
            {r.status && (
              <span className={`status-tag status-${r.status}`}>
                {r.status === "pending"
                  ? "未开始"
                  : r.status === "active"
                    ? "进行中"
                    : r.status === "paused"
                      ? "暂停"
                      : "完成"}
              </span>
            )}
            <span className="record-content">{r.content}</span>
            <span className="record-time">{formatTime(r.created_at)}</span>
          </div>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 3: 创建 timeline.css**

```css
/* src/timeline/styles/timeline.css */
.timeline {
  display: flex;
  flex-direction: column;
  height: 100%;
  padding: 20px;
  gap: 12px;
  background: linear-gradient(135deg, #fef9f3 0%, #eaf3fb 100%);
}

.timeline-tabs {
  display: flex;
  gap: 8px;
  background: rgba(255, 255, 255, 0.6);
  border-radius: 10px;
  padding: 6px 12px;
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
}
.tab {
  background: none;
  border: none;
  padding: 4px 8px;
  font-size: 12.5px;
  font-weight: 600;
  color: var(--ink-2);
  cursor: pointer;
  border-radius: 6px;
}
.tab.active {
  color: var(--blue);
  border-bottom: 2px solid var(--blue);
}

.timeline-list {
  display: flex;
  flex-direction: column;
  gap: 6px;
  flex: 1;
  overflow-y: auto;
}
.empty {
  text-align: center;
  color: var(--ink-3);
  padding: 30px;
  font-size: 13px;
}
.record {
  display: flex;
  align-items: center;
  gap: 8px;
  background: rgba(255, 255, 255, 0.55);
  border-radius: 8px;
  padding: 8px 10px;
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
}
.record-tag {
  font-size: 10px;
  padding: 1px 6px;
  border-radius: 5px;
  font-weight: 700;
  width: 30px;
  text-align: center;
  flex-shrink: 0;
}
.tag-task {
  background: rgba(0, 122, 255, 0.12);
  color: var(--blue);
}
.tag-idea {
  background: rgba(255, 149, 0, 0.15);
  color: var(--orange-deep);
}
.tag-check_in {
  background: rgba(52, 199, 89, 0.15);
  color: var(--green-deep);
}
.status-tag {
  font-size: 9px;
  padding: 1px 5px;
  border-radius: 4px;
  font-weight: 700;
  flex-shrink: 0;
}
.status-pending {
  background: rgba(0, 0, 0, 0.05);
  color: var(--ink-3);
}
.status-active {
  background: var(--green-tag);
  color: var(--green-deep);
}
.status-paused {
  background: var(--orange-tag);
  color: var(--orange-deep);
}
.status-done {
  background: var(--green-soft);
  color: var(--green-deep);
}
.record-content {
  flex: 1;
  font-size: 12.5px;
  font-weight: 600;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.record-time {
  color: var(--ink-3);
  font-size: 10.5px;
  flex-shrink: 0;
}
```

- [ ] **Step 4: 验证编译**

```bash
cd /home/jason/workspace/mindtap && npx tsc --noEmit
```

Expected: 通过

- [ ] **Step 5: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src/timeline
git commit -m "feat(timeline): RecordTimeline 4 tab (全部/任务/灵感/打卡) + record-updated 自动刷新"
```

---

### Task 27: 集成时间线到主窗 App.tsx

**Files:**
- Modify: `src/App.tsx`

- [ ] **Step 1: 读取现有 App.tsx**

读取后保留 V1.0 风格（topbar/sidebar），**只替换主内容区**为时间线。

- [ ] **Step 2: 修改 App.tsx（保留 V1.0 框架，主区替换为 RecordTimeline）**

```tsx
// src/App.tsx（仅展示关键部分，保留 V1.0 topbar/sidebar）
import { RecordTimeline } from "./timeline/RecordTimeline";

export default function App() {
  return (
    <div className="app">
      <Topbar />
      <div className="main-layout">
        <Sidebar />
        <main className="main-content">
          <RecordTimeline />
        </main>
      </div>
    </div>
  );
}
```

**注**：保留 V1.0 现有 Topbar / Sidebar 组件（已在 src/components/）。

- [ ] **Step 3: 验证 dev server**

```bash
cd /home/jason/workspace/mindtap && timeout 5 npx vite build 2>&1 | tail -10
```

Expected: 构建成功

- [ ] **Step 4: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src/App.tsx
git commit -m "feat(timeline): 集成 RecordTimeline 到主窗主内容区"
```

---

## Phase 5：跨平台 + 自启动

### Task 28: macOS NSPanel（不抢焦）

**Files:**
- Create: `src-tauri/src/floating/mod.rs`
- Create: `src-tauri/src/floating/platform.rs`
- Modify: `src-tauri/Cargo.toml`
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: 添加 objc/cocoa 依赖**

在 `[target.'cfg(target_os = "macos")'.dependencies]` 追加：

```toml
objc = "0.2"
cocoa = "0.26"
```

- [ ] **Step 2: 实现 platform.rs（macOS 提升为 NSPanel）**

```rust
// src-tauri/src/floating/platform.rs
#[cfg(target_os = "macos")]
pub fn set_as_panel(_window: &tauri::WebviewWindow) -> Result<(), String> {
    use cocoa::appkit::{NSWindow, NSWindowCollectionBehavior};
    use objc::{msg_send, sel, sel_impl};

    let ns_window: cocoa::base::id = _window.ns_window().map_err(|e| e.to_string())? as cocoa::base::id;
    unsafe {
        let _: () = msg_send![ns_window, setHidesOnDeactivate: false];
        let _: () = msg_send![ns_window, setCanBecomeKeyWindow: false];
        let _: () = msg_send![ns_window, setCollectionBehavior:
            NSWindowCollectionBehavior::NSWindowCollectionBehaviorCanJoinAllSpaces
            | NSWindowCollectionBehavior::NSWindowCollectionBehaviorFullScreenAuxiliary
            | NSWindowCollectionBehavior::NSWindowCollectionBehaviorTransient
        ];
    }
    Ok(())
}

#[cfg(not(target_os = "macos"))]
pub fn set_as_panel(_window: &tauri::WebviewWindow) -> Result<(), String> {
    Ok(())
}
```

- [ ] **Step 3: 创建 mod.rs（启动 floating window）**

```rust
// src-tauri/src/floating/mod.rs
pub mod platform;

use tauri::Manager;

pub fn ensure_window(app: &tauri::AppHandle) -> Result<(), String> {
    if app.get_webview_window("floating").is_some() {
        return Ok(());
    }
    let _w = tauri::WebviewWindowBuilder::new(
        app,
        "floating",
        tauri::WebviewUrl::App("floating.html".into()),
    )
    .title("Mindtap")
    .inner_size(320.0, 36.0)
    .min_inner_size(320.0, 36.0)
    .max_inner_size(360.0, 320.0)
    .transparent(true)
    .decorations(false)
    .always_on_top(true)
    .resizable(false)
    .skip_taskbar(true)
    .focused(false)
    .shadow(false)
    .visible(true)
    .build()
    .map_err(|e| e.to_string())?;

    if let Some(win) = app.get_webview_window("floating") {
        platform::set_as_panel(&win)?;
    }
    Ok(())
}
```

- [ ] **Step 4: lib.rs 启动 floating window**

```rust
mod commands;
mod db;
mod error;
mod floating;

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    // ... 之前
        .setup(|app| {
            db::init(app).map_err(|e| e.to_string())?;
            floating::ensure_window(&app.handle()).map_err(|e| e.to_string())?;
            app.global_shortcut().register(toggle_shortcut)?;
            Ok(())
        })
    // ...
}
```

- [ ] **Step 5: 验证编译（macOS / 跨平台）**

```bash
cd /home/jason/workspace/mindtap/src-tauri && cargo check
```

Expected: macOS 下编译通过（含 objc/cocoa）；其他平台 OK

- [ ] **Step 6: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src-tauri/src/floating src-tauri/Cargo.toml src-tauri/Cargo.lock src-tauri/src/lib.rs
git commit -m "feat(floating-rs): macOS NSPanel 提升 (不抢焦 + 全空间)"
```

---

### Task 29: 自启动 + 关闭策略

**Files:**
- Modify: `src-tauri/Cargo.toml`
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: 添加 tauri-plugin-autostart 依赖**

```toml
tauri-plugin-autostart = "2"
```

- [ ] **Step 2: lib.rs 注册 autostart 插件**

在 `tauri::Builder` 链上追加：

```rust
        .plugin(tauri_plugin_autostart::init(
            tauri_plugin_autostart::MacosLauncher::LaunchAgent,
            Some(vec!["--floating"]),
        ))
```

完整修改后的 lib.rs：

```rust
mod commands;
mod db;
mod error;
mod floating;

use tauri_plugin_global_shortcut::{Code, Modifiers, Shortcut, ShortcutState};

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    let toggle_shortcut = Shortcut::new(
        Some(if cfg!(target_os = "macos") {
            Modifiers::SUPER | Modifiers::SHIFT
        } else {
            Modifiers::CONTROL | Modifiers::SHIFT
        }),
        Code::Space,
    );

    tauri::Builder::default()
        .plugin(tauri_plugin_opener::init())
        .plugin(tauri_plugin_autostart::init(
            tauri_plugin_autostart::MacosLauncher::LaunchAgent,
            Some(vec!["--floating"]),
        ))
        .plugin(
            tauri_plugin_global_shortcut::Builder::new()
                .with_handler(move |app, shortcut, event| {
                    if shortcut == &toggle_shortcut && event.state == ShortcutState::Pressed {
                        let _ = commands::floating_cmd::floating_toggle(app.clone());
                    }
                })
                .build(),
        )
        .setup(|app| {
            db::init(app).map_err(|e| e.to_string())?;
            floating::ensure_window(&app.handle()).map_err(|e| e.to_string())?;
            app.global_shortcut().register(toggle_shortcut)?;
            Ok(())
        })
        .invoke_handler(tauri::generate_handler![ /* ... 同前 */ ])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

- [ ] **Step 3: 验证编译**

```bash
cd /home/jason/workspace/mindtap/src-tauri && cargo check
```

Expected: 编译通过

- [ ] **Step 4: 提交**

```bash
cd /home/jason/workspace/mindtap && git add src-tauri/Cargo.toml src-tauri/Cargo.lock src-tauri/src/lib.rs
git commit -m "feat(tauri): tauri-plugin-autostart 自启动集成"
```

---

## Phase 6：集成测试 + 验收

### Task 30: 端到端 smoke test（3 秒记录）

**Files:**
- Manual E2E

- [ ] **Step 1: 启动 dev server**

```bash
cd /home/jason/workspace/mindtap && npm run tauri dev
```

Expected: Tauri app 启动，浮动窗出现

- [ ] **Step 2: 验证折叠态默认显示**

观察：浮动窗 320×36，EMPTY 标签 + "点击记录第一个任务" + 蓝 +

- [ ] **Step 3: 点击展开 → 输入 → 开始计时**

1. 点浮动窗 → 展开 360×280
2. 输入 "测试 3 秒记录"
3. 点 ⏵ 开始计时
4. **验证耗时 < 3 秒**
5. 浮动窗自动折叠回 320×36
6. 显示 FOCUS 标签 + "测试 3 秒记录" + 时间 00:00:0X + ⏸

- [ ] **Step 4: 验证主窗时间线**

打开主窗 → 时间线出现 1 条 TASK 记录，"进行中" 状态

- [ ] **Step 5: 验证全局快捷键**

按 Cmd+Shift+Space（macOS）/ Ctrl+Shift+Space（Windows）→ 浮动窗 toggle 显示/隐藏

- [ ] **Step 6: 记录验收结果到 docs**

```bash
cat > /tmp/e2e-result.md << 'EOF'
# V1.3 浮动窗口 E2E 验收（2026-06-14）

## ✅ 通过
- [x] 折叠态 EMPTY 默认显示
- [x] 点击展开到 360×280
- [x] textarea 输入 + ⏵ 开始计时 < 3s
- [x] 自动折叠 + FOCUS 显示 + 计时
- [x] 主窗时间线出现记录
- [x] 全局快捷键 toggle 浮动窗
- [x] 暂停：⏸ 暂停 → 时间冻结 → 出现在切换下拉
- [x] 完成：✓ 完成 → 状态 done → 切换下拉消失
- [x] 撤销：↶ 撤销 → done → active
- [x] 切换：⇄ ▼ 下拉 → 点击目标 → Focus 切换

## 📝 待优化
- 详见 docs/superpowers/specs/2026-06-14-浮动窗口快速记录-design.md 第 9 节未决项
EOF
```

- [ ] **Step 7: 提交所有 Phase 1-6 任务**

```bash
cd /home/jason/workspace/mindtap && git status
git log --oneline -30
# 验证每个 Phase 任务都有 commit
```

---

## 验收清单（设计文档第 9.4 节）

实施完成后，逐条勾选：

- [ ] 全局快捷键 toggle 浮动窗（macOS Cmd+Shift+Space / Windows Ctrl+Shift+Space）
- [ ] 浮动窗常驻 alwaysOnTop，跨显示器可拖动
- [ ] 3 秒记录：输入 + 保存 + 折叠 < 3 秒
- [ ] 1 秒查看：折叠态可见 + 时间在走
- [ ] 4 状态机：pending / active / paused / done 完整支持
- [ ] 业务层双写：每次 task/idea/check_in 变更都同步 record
- [ ] 全局唯一 active：L1 不变量
- [ ] 撤销继续：done → active
- [ ] 截止快捷：📅 稍后 / 明天 正确设置 due_at
- [ ] 跨平台：macOS + Windows 原生浮动窗

---

## 关联文件

- **设计文档**：`docs/superpowers/specs/2026-06-14-浮动窗口快速记录-design.md`
- **mockup**：`.superpowers/brainstorm/150600-1781431421/content/final-mockup.html`
- **持久化**：`docs/superpowers/plans/2026-06-14-浮动窗口快速记录.md`（本文件）
- **头脑风暴**：`/home/jason/workspace/mindtap/.superpowers/brainstorm/150600-1781431421/content/floating-*.html` × 17
- **规划记录**：`.planning/2026-06-14-floating-window-design/`
