# Ubiquitous Language

> 轻念 · Mindtap 工作台账核心领域的统一术语表。2026-08-01 建立(QA 调研反馈后)。
> 目的:消除「补货/捕获/记一下/新建/开始/保存」等词混用,统一对话与文档语言。
> 相关:PRD `docs/轻念Mindtap产品需求文档.md` / Domain `docs/domain/v0.2.1-workbench-core-domain.md` / `CONTEXT.md`(材质领域术语)

## 记录与状态

| Term | Definition | Aliases to avoid |
| --- | --- | --- |
| **记录 (Item)** | 产品内一条最小记录单位,内容 + 类型 + 状态;任务族与速记族同实体 | 任务、条目、事项 |
| **内容 (content)** | 记录里写的文字(≤200 字),即「记了什么」 | 任务名、标题、task_title |
| **收件箱 (inbox)** | 捕获后未整理记录所在的状态/区域 | 待整理、新任务 |
| **待办 (todo)** | 已整理、等待开始的状态 | 暂停、挂起 |
| **进行中 (active)** | 正在被投入计时中的状态 | 在跑、活动中 |
| **已完成 (done)** | 完成封存的状态 | 结束、关闭 |
| **归档 (archived)** | 只读、可重新激活的状态 | 存档、历史 |

## 动作

| Term | Definition | Aliases to avoid |
| --- | --- | --- |
| **捕获 (capture)** | 把内容存进收件箱的动作(PRD 1.1 核心) | 记录、记一下、新建 |
| **保存 (save)** | 捕获面板的提交动作:内容 → 收件箱,收起,焦点归还 | 确定、OK |
| **开始 (start)** | 把记录投入「进行中」并计时 | 启动、运行 |
| **暂停 (pause)** | 把「进行中」退回「待办」并停表 | 挂起、停止 |
| **完成 (complete)** | 把记录标为「已完成」 | 结束、关闭 |
| **整理 (triage)** | 收件箱逐条裁决:转待办/仅留档/删除 | 处理、清理 |

## 展示与机制

| Term | Definition | Aliases to avoid |
| --- | --- | --- |
| **折叠态 (folded)** | 浮窗最小形态(单行),常驻展示 | 收起、缩回 |
| **展开态列表 (list)** | 浮窗展开显示全部卡的状态 | 列表页、主界面 |
| **新增面板 (compose)** | 浮窗展开用于捕获输入的状态 | 输入页、新建面板 |
| **任务卡 (card)** | 列表里一张记录的展示单元(内容/时长/冷却/备注) | 条目、块 |
| **失真确认气泡 (bubble)** | 失真检测后询问「还在进行中吗」的独立小窗 | 提示、弹窗 |
| **失真窗口 (pending_ms)** | 失真点到暂停点之间待确认的时间段 | 待结算、悬空时间 |
| **并行进行中 (parallel active)** | 多个记录同时「进行中」,各自计时 | 多任务、多卡 |

## 关键决策(本领域)

- **捕获 ≠ 开始**:捕获只进收件箱,不开始计时(PRD 3.2 规则 1);想计时需「开始」
- **暂停 = 退回待办**:「暂停」不占状态位,即 active→todo(PRD 3.2 规则 5)
- **进行中不跨天**:跨天 active 自动退回待办,时长不膨胀(PRD 3.2 规则 6)
- **重复捕获不合并**:同名记录可并存,系统只轻提示(PRD 3.2 规则 7)
- **并行是根本差异**:Mindtap 的折叠态/统计针对「多 active 并行」设计,不照搬 Toggl 类单 active 应用
- **激活时间需留痕**:每次「开始」的激活时间段都要详细记录,供后续并行统计/合并计算

## 关系

- 一次**捕获**产生一条 **记录**(进**收件箱**)
- 一条**记录**经**开始**进入**进行中**,经**暂停**退回**待办**,经**完成**变**已完成**
- **进行中**可有**并行进行中**多条
- 一条**失真**的**进行中**记录产生一个**失真窗口**,用户确认后结算
- 一条**已完成**记录次日自动进**归档**

## Flagged ambiguities

- **「补货」**:用户例子里的具体待办内容,不是领域术语——内容可以是任何文字,术语只关心「捕获/开始/暂停/完成」这些动作
- **「捕获」vs「记一下」**:PRD 用「捕获」(进收件箱),对话常用「记一下」。统一用**捕获**(动作)+ **记一下**(口语别名,避免在文档中使用)
- **「新建」vs「保存」**:「新建」暗示创建即生效(旧 V0.2.0 直觉),「保存」明确进收件箱不开始。统一**保存**
- **「进行中」vs「活动中」**:统一**进行中**(状态)与 **active**(代码/文档英文)

## Example dialogue

> **Dev:** "用户在折叠态点「+」进入新增面板,输入后点「保存」——这条记录进收件箱了吗?"
> **Domain expert:** "对。**捕获**只进**收件箱**,不**开始**计时。用户想投入才点「开始」,进入**进行中**;多张卡可以**并行进行中**,各自计时。"
> **Dev:** "那「暂停」呢?"
> **Domain expert:** "**暂停** = 退回**待办**,不占状态位。用户切走或点另一张卡「开始」,原卡自动退回待办。"
> **Dev:** "**失真**时呢?"
> **Domain expert:** "**进行中**卡静置超阈值(冷却/跨天)→ 弹**失真确认气泡**问「还在进行中吗」→ 超时自动**暂停** → 失真窗口标**待确认**,用户回来一键**记入/丢弃**。"
