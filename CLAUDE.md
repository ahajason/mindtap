# Mindtap

轻念 · Mindtap 是本地 SQLite 桌面应用，无云同步。产品北极星是**个人工作台账**：3 秒记录、1 秒查看、0 思考成本、全量本地存储。

## 处理原则

- 始终用中文回复。先确认根本需求，只做足以改变决策的调查；有合理默认方案时直接采用并说明假设。
- 改动前以用户需求、权威文档、现有设计契约和可验证代码为准；不把实现快照、统计数字或目录导览当作长期真值。
- 只使用 git 与 SSH；禁止 `gh` CLI 及 GitHub 写入型 MCP 工具。
- 改动系统 API、原生窗口或跨进程 IPC 后，类型检查与单测不能代替实机验证：必须在 `tauri dev` 中触发新机制并确认行为。
- 涉及数据库受管连接时，不得跨 `.await` 持有连接锁。

## UI 与文档

- 所有 UI 改动遵循 `docs/design-system/glassic-ui-spec.md`；不得自创颜色、阴影或模糊 token。玻璃表面只承载控件和导航，内容放在非玻璃背景。
- 写入 `docs/` 前先读 `docs/CLAUDE.md`；技术实现细节留在代码和测试中。
