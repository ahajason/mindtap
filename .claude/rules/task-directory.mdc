---
description: |
  以下场景触发查阅本规则:
  - 写 `docs/tasks/<slug>/task.md` 前
  - 决定任务 slug 三段式怎么命名（version / type / short-desc 取值）
  - 决定要不要新建 `docs/tasks/` 子目录
  - 写 task.md 的 Why / What / Done when 段时
  - 决定 task.md 跟业务代码是否同 commit 时
  - 反向追溯某任务的所有 commit 时
alwaysApply: true
---

# Task Directory

## 位置

`docs/tasks/<version>-<type>-<short-desc>/`

(与现有 `docs/specs/` / `docs/plans/` / `docs/reports/` / `docs/architecture/` 同级)

## 命名

`<version>-<type>-<short-desc>` 三段式:

| 段        | 取值                                                |
| --------- | --------------------------------------------------- |
| version   | 项目阶段版本 (`v0.1` / `v0.2` / `v1.0` / `v1.1`)    |
| type      | `feat` / `fix` / `chore` / `docs` / `refactor`      |
| short-desc| kebab-case, 2-4 词                                  |

**注意**: slug 里的 version 是**项目阶段标签** (哪个版本), 不绑业务域. 业务语义 (floating / settings / db 等) 由 task.md 内部和 commit message 自己写.

## task.md 极简模板 (~10 行)

```markdown
# <type>(<scope>): <subject>
<!-- scope 取业务名, 不是版本号 -->

> 创建: YYYY-MM-DD

## Why

<!-- 1-3 句。痛点 + 业务场景 + 为什么不修不行 -->

## What

<!-- 1 句业务边界 -->

## Done when

- [ ] 业务行为 X 正确
- [ ] 不引入新规范违反项
```

## 绝不写什么 (反向规则)

| 不写                | 为什么                                                    |
| ------------------- | --------------------------------------------------------- |
| `## Files` 段       | git 知道(`git log -- <path>` 反查)                        |
| `## 提交记录` 段    | commit hash 会过期(squash / rebase 后失效); 鸡生蛋问题   |
| `## 状态` 字段      | git 事实推断(看到 commit 就算"已交付")                   |
| `## 关联` 段        | 路径自解释                                                |
| Refs: / See also:   | 不是任何标准, git 不识别                                 |

## 跟 commit 的关系

**模式**: task.md 跟业务代码**同 commit**.

```bash
# 步骤 1: 先写 task.md(前置)
# 步骤 2: 写业务代码
# 步骤 3: 同 commit
git add docs/tasks/<slug>/task.md \
        <业务代码路径>

git commit -m "<type>(<scope>): <subject>

- 业务痛点
- 边界条件
- 不这样改会怎样"
```

**反向追溯**(不要 commit 写 footer, 靠 `git log`):

```bash
# 查某任务的所有 commit
git log --all --oneline -- docs/tasks/<slug>/

# 查某业务的所有 commit
git log --all --oneline -- <业务代码路径>
```

## 跟 commit 的关系（补充）

- **`docs/tasks/<slug>/`**: 任务正式档, git 跟踪
- 版本发版时, 过程产物归档到 `docs/archive/<version>/`, 正式档迁到 `docs/tasks/<next-version>-<type>-<short-desc>/`

> **V0.1.0 init 阶段**: 项目当前处于起步阶段, 任务正式档优先.