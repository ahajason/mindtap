---
description: Vite + Tauri 2 多入口 / 多窗口开发的 5 个硬坑(HTML 路径 / CSS 相对 / 浮动窗 resize / 边框手柄 / ResizeObserver 单押)— 仅 src/floating/** 路径下触发
globs: ["src/floating/**"]
---

# Vite + Tauri 多窗口开发的 5 个坑

> 适用: mindtap src/floating/(浮窗)和任何未来 Tauri 多窗口项目。WebView2 / WebKit 边框窗口 + setSize / ResizeObserver 的硬约束清单。

## 坑 1 — 多入口 HTML 输出位置

**现象**: Vite input 写 `src/floating.html` → 输出 `dist/src/floating.html`;但 `tauri.conf.json` 的 `windows[].url = "floating.html"`(= `dist/floating.html`)→ Tauri 找不到文件,加载 fallback 错误页。

**修法**: 所有 Tauri 入口 HTML 都放**项目根**(跟 `index.html` 平级),不要放 `src/`。Vite 按 input 路径相对 root 决定输出位置。

**验证**: `vite build` 后 `ls dist/` 应看到所有 HTML 在 `dist/` 根,不在 `dist/src/`。

## 坑 2 — 组件相对 CSS 路径

**现象**: 组件 `src/floating/components/FocusBlock.tsx` 写 `import "./focus-block.css"` → 实际 CSS 在 `src/floating/styles/`,build 报 `Could not resolve`。

**修法**: 组件引用父目录 CSS 用 `../styles/xxx.css`;可在 build 时被 resolve 错误暴露。

## 坑 3 — 浮动窗不能根据内容调整宽高(单押 ResizeObserver 不够)

**根因**:
1. `useEffect` 只在 `expanded` 切换时 setSize,**不响应内容尺寸变化**
2. `tauri.conf.json` 的 `maxWidth/maxHeight` 限制窗口无法跟随内容增长
3. 下拉面板 `position: absolute`,浮出窗口边界被裁
4. `getCurrentWindow()` 每次渲染返回新实例,useEffect deps 不稳定

**修法**: **双轨 setSize 配方**:
- `useEffect` 监听 `expanded` 切换 → **显式** `setSize(展开时 : 折叠时)`
- `useLayoutEffect` + `ResizeObserver` 监听根容器 → 处理动态内容(下拉打开、长文本)
- `manualResizingRef` 在手动拖拽期间阻断 ResizeObserver 防反馈循环
- 把 `getCurrentWindow()` 缓存到**模块级单例**

**教训**: **永远不要单押 ResizeObserver**。状态切换用显式 setSize 为主路径,ResizeObserver 只负责运行时自适应。这是 Tauri 边框小窗的硬约束。

## 坑 4 — 边框窗口无法手动伸缩(`resizable:false` + 无 UI 手柄)

**现象**: `decorations: false` + `resizable: false` + 没自己加拖拽手柄 → OS chrome 边角手柄不显示,React UI 里也没手柄,**用户物理上无可抓物**。

**修法**:
1. `tauri.conf.json` + Rust `floating/mod.rs` 把 `resizable: false` → `true`(让 setSize 不被 OS 拦截)
2. 在 `App.tsx` 根容器右下角加 12×12 拖拽手柄 `.resize-handle`
3. mousedown 记录 `(startX, startY, startW, startH)`,mousemove 调 `setSize(startW+dx, startH+dy)`,mouseup 解绑
4. `manualResizingRef` 阻断 ResizeObserver 防反馈循环

**教训**: `resizable: false` 只挡 OS 拖拽,但**不**加手柄 = 完全没路径。

## 坑 5 — ResizeObserver 单轨反馈循环

**现象**: 手动拖拽改窗口大小 → ResizeObserver 触发 setSize → 新尺寸再触发 ResizeObserver → 来回震荡。

**修法**: 用 `manualResizingRef`(boolean) 在拖拽期间 `true`,ResizeObserver 检测到就 skip;mouseup 时设回 `false`。

## 通用的 max bounds 双处同步

`tauri.conf.json` + Rust 模块 `max_inner_size()` 两处都要放宽一致,否则一边宽一边窄 → 实际生效的是较窄的那个。

## self-apply

| # | 检验项 | 本规则 |
|---|---|---|
| 1 | 关键路径列全 | `src/floating/` + `tauri.conf.json` + Rust `floating/mod.rs` + `dist/`(build 验证) |
| 2 | 给具体步骤 | 5 坑每坑有"现象 / 修法 / 教训" |
| 3 | 触发条件 | `globs: src/floating/**` |
| 4 | 反模式例子 | 各坑有"现象"段就是失败例子 |
| 5 | 交叉引用 | 不引(只描述技术约束,不指其他 rule) |
| 6 | 验证机制 | 坑 1 / 2 用 `vite build` 验证 |
| 7 | 当下能说"下次 X,自动 Y" | "下次加新 Tauri 窗口,HTML 自动放根 + max bounds 同步 + 双轨 setSize" |
