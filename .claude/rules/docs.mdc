---
description: 写 docs/ 任何文件前必读 — 文档归位 + 结构纪律 + task 目录
alwaysApply: true
---

# Docs — 文档归位与结构纪律

> 权威细则: `docs/governance/doc-layers.md`。结构为「按大需求分目录」——每个大需求一个目录,`index.md` 唯一入口,文档内不互相引用。

## 归位表

| 内容类型 | 去处 | 关键约束 |
|---|---|---|
| 产品需求 / 用户故事 / 验收 | `docs/轻念Mindtap产品需求文档.md`(唯一真值) | 纯业务,无技术名词 |
| 业务实体 / 状态机 / ADR | 对应需求目录 `domain.md` / `adr/` | 无表名 / 字段 / IPC 命令 |
| 视觉 / 交互 / 组件契约 | 对应需求目录 `design.md` | 无表 / IPC / 模块路径 |
| 范围边界 / bug 归属 | task.md「范围边界」段 | 技术实现细节入单元测试 |
| 共享视觉规范(token) | `docs/design-system/` | 跨需求引用 |
| 实施步骤 / commit 计划 | `docs/plans/YYYY-MM-DD-<version>-<feature>.md` | 不写业务规则 |
| 进行中 task | `docs/<需求>/tasks/<name>/task.md` | 入口固定 task.md |
| 跨版本规则 | `docs/governance/` | 治理横向贯穿 |
| 发版记录 | `docs/reports/` | release notes / retro |

## 需求目录命名

`<编号>-<单词>`(如 `L-1-workbench`)。编号前缀区分通用目录;单词表达业务语义。目录内必须含 `index.md`(唯一入口,唯一允许引用本目录文件的文档)。

## 新功能 / 新 bug 流程

### 新功能

```
1. 主文档补需求(如有新场景)
2. 开 docs/<编号>-<需求>/ 目录(或复用已有)
3. 写 domain.md(如新实体/状态) + design.md(如有 UI)
4. 写 index.md(唯一入口)
5. 开 docs/<编号>-<需求>/tasks/<name>/task.md(含「范围边界」段)
```

### 新 bug

```
1. 读对应任务 task.md 「范围边界」段
2. 在范围内 → 开 docs/<需求>/tasks/<name>-fix-<topic>/task.md
3. 不在范围内 → 不归本任务,另开 task
```

## task.md 模板

```markdown
# <type>(<scope>): <subject>
<!-- scope 取业务名,不是版本号 -->

> 创建: YYYY-MM-DD

## Why
<!-- 1-3 句。痛点 + 业务场景 + 为什么不修不行 -->

## What
<!-- 1 句业务边界 -->

## 范围边界
<!-- 在范围内 / 不在范围内 / bug 归属规则 -->

## Done when
- [ ] 业务行为 X 正确
- [ ] 不引入新规范违反项
```

**绝不写**: `## Files` 段(git 知道)/ `## 提交记录`(hash 会过期)/ `## 状态`(git 推断)/ `## 关联`(索引归 index.md)。

**跟 commit 的关系**: task.md 跟业务代码**同 commit**。反向追溯用 `git log/show/diff`,不在 commit 写 footer。

## 严禁出现(写后 grep 自查)

```bash
# domain.md(业务层)
grep -iE "INTEGER|TEXT|partial unique|invoke|Mutex|src-tauri|tauri-bridge|SQL " docs/<需求>/domain.md
# design.md(视觉层)
grep -iE "INTEGER|TEXT|partial unique|invoke|SQL |src-tauri/" docs/<需求>/design.md
```

命中必须改。

## 反模式

- ❌ 新需求缺 index.md 就开 task
- ❌ 文档内互相贴路径链接(索引职责归 index.md)
- ❌ 技术实现细节写进文档(入单元测试)
