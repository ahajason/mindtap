---
description: |
  以下场景触发查阅本规则:
  - 给函数 / 类 / 方法加注释前——决定写不写、写什么
  - 写新模块第一行注释前
  - 决定要不要保留老代码里的 `// TODO` / `// FIXME` 时
  - 写公开 API 的 JSDoc / rustdoc 时
  - 决定注释写中文还是英文时
  - 代码 review 时判断"这条注释该不该删"时
alwaysApply: true
---

# Comment Style

## 核心原则

**代码即注释**: 好的代码本身可读, 不需要注释解释"在做什么". 注释只写**为什么**这样做, 不写**做了什么**.

| 写 | 不写 |
| --- | --- |
| 为什么这样做 (业务背景 / 决策原因 / 边界条件) | 这段代码在做什么 (代码自己说话) |
| 公开 API 的契约 (参数约束 / 返回值语义) | 函数名 / 参数名显而易见的解释 |
| 跨模块的非显式依赖 | "初始化函数" / "主逻辑" / "工具方法" 这种废话 |
| 暂时绕过某限制的原因 (含背景说明) | 简单的 `//` 行尾复述代码 |

## 反模式

❌ **Narration 注释** (像 narrator 一样复述代码):
```
// 初始化变量
let count = 0;

// 循环遍历数组
for (const item of items) { ... }

// 设置为 true
isReady = true;
```

❌ **Stale 标签** (常驻文件但不维护):
```
// TODO: 优化性能
// FIXME: 这里有 bug
// NOTE: 注意这里
```

❌ **元描述** (描述文件 / 函数本身而非内容):
```
// 这个文件处理用户登录
// 这个方法用来查询数据库
// 主入口函数
```

❌ **历史注释** (解释过去发生了什么, 与当前代码无关):
```
// 之前用的是 for 循环, 改成 map 是因为 ...
// 2024-01-01 jason 修复了 ...
```

❌ **废话段头注释** (在函数 / 类型前加 banner):
```
/**
 * 计算总和
 * @param a 第一个数
 * @param b 第二个数
 * @returns 总和
 */
function add(a, b) { return a + b; }
```

## 正面模式

✅ **WHY 注释** (业务背景 / 决策原因):
```
// macOS 菜单栏遮 ~25px, 首启 (0,0) 被遮
position = (100, 60)
```

✅ **公开 API 契约** (类型系统无法表达的约束):
```
// 返回值非 null 时表示 task 已落库, 可安全展示给用户
```

✅ **跨模块的非显式依赖**:
```
// 调用方必须保证 session 已 init, 否则 panic
```

✅ **RUST / TS 类型签名内的文档** (类型即注释):
```rust
/// 计算窗口首启位置, 避开 macOS 菜单栏.
/// 返回的 position 已 clamp 到主屏可见区.
fn calc_initial_position(menu_bar_height: f64) -> Position { ... }
```

## 边界情况

| 情况 | 处理 |
| --- | --- |
| 公开 API (导出函数 / 公共方法) | 用 rustdoc / JSDoc 写契约, 1-3 行 |
| 内部辅助函数 | 不写注释, 函数名自解释即可 |
| 复杂算法 | 在算法入口写 1 段 WHY, 不在每行写 WHAT |
| 临时绕过 | 写 1 段 WHY + 链接 issue / TODO, 不写 "暂时这样" |
| 配置项 | 在配置 schema 写 WHY, 不用在每处使用处复述 |

## 自检

写完一段代码, 删注释看是否还能读懂——能读懂就删掉. 留下的注释必须满足:

1. **删了会丢失信息**——这条 WHY 注释提供了代码之外的信息
2. **不是代码的复述**——不是把 `for` 注释成"循环遍历"
3. **不会过时**——不依赖未来可能改变的实现细节

3 条任一不满足, 就删.