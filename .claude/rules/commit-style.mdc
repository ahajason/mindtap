---
description: |
  以下场景触发查阅本规则:
  - 写 `git commit -m "..."` 前——决定 message 怎么写
  - 决定何时 git commit——一个改动收口没、能不能提交
  - `git commit --amend` 改 message 时
  - 决定一个任务拆几个 commit（1-3 粒度判断）
  - 写 commit message body 段时——业务层粒度判断
  - 决定 commit message 用什么 type / scope 时
alwaysApply: true
---

# Commit Style

## 格式

```
<type>(<scope>): <subject>

<why 段 — clean-comments 风格, 只写"为什么"不重复"做了什么">
- 业务痛点
- 边界条件
- 不这样改会怎样
```

**字段约束**:
- Subject: 必有, ≤ 72 字符, 中文可读, 不加句号
- Body: 可选, 多段用空行分隔, 每段 1 个 why
- `<scope>`: 可空（`chore:` 无 scope 也合法）, 但**有 scope 时必为业务名**

## 类型集

| type     | 用法                  |
| -------- | --------------------- |
| feat     | 新功能 / 新行为       |
| fix      | 修 bug                |
| refactor | 重构(不改行为)        |
| docs     | 纯文档                |
| test     | 纯测试                |
| chore    | 工具 / 依赖 / 配置    |
| build    | 构建 / CI             |
| revert   | 撤销                  |

## 业务域

`floating` / `settings` / `timeline` / `db` / `tray` / `bridge` / `diagnostics` / `log` / `meta`

scope 选择规则: commit 改的代码 80% 以上在哪个业务域, 就用哪个 scope; 跨多个用最核心的那个.

> **V0.1.0 init 阶段**: 业务域尚未拆分, scope 临时用 `meta` 或留空; 待业务域拆分后用真实 scope.

## 粒度

**1 个任务(`docs/tasks/<slug>/`) = 1-3 个 commit**.

| 任务规模 | commit 数 | 说明                                                |
| -------- | --------- | --------------------------------------------------- |
| 小       | 1         | 单点修复 / 单文件改动                                |
| 中       | 2         | 含测试的中等改动                                    |
| 大       | 3         | 跨前后端 + 测试的大改动                             |

**反模式**:
- ❌ 1 个任务 10+ commit(拆太散)
- ❌ 1 个 commit 跨 3 个不相关任务(聚合过度)

## Body 段写法

**写"为什么"**, 不写"做了什么"(diff 会显示"做了什么"):

**字段**:
- 每条以 `-` 开头
- 写业务痛点 / 边界条件 / 不这样改会怎样
- 不用写"改了 X 加了 Y"这种 diff 复述

## 不引 footer

❌ `Refs:` / `Closes:` / `See also:` / `BREAKING CHANGE:` — 不是任何标准 footer, git 不识别, 冗余.

反向追溯靠 `git log -- <path>`:

```bash
# 查某任务的 commit
git log --all --oneline -- docs/tasks/<slug>/

# 查某业务的 commit
git log --all --oneline -- <业务路径>
```

## 业务层粒度（message 不描写代码）

commit message 只写**业务层意图**（动机 / 决策 / 影响），不写**代码层细节**（dep 版本号 / 函数名 / 文件名 / 改了哪行）。代码细节由 diff 自己说话.

## 提交时机（完整性门槛）

commit 不是"写完一行就 commit"，而是"业务单元完整收口"才 commit.

| 内容类型 | 准入条件 |
|---|---|
| 功能 / bug 修复 | 必须是**可运行并验证通过的完整功能**, 不是半成品 |
| 纯文档 / 配置 | 看用户是否还会继续改, 后续是否还需修改——确定不再改才提交 |

**反模式**:
- ❌ 同一提交还有代码待写, 就先把已写部分 commit 了
- ❌ 规则/CLAUDE.md 还在迭代, 边改边 commit, 提交后还要 amend
- ❌ 一个 commit 里既有代码改动又有未完成的文档改动

**正面模式**:
- ✅ 先把所有改动收口 (代码 / 规则 / 文档 全部到位)
- ✅ 跑最小验证 (`tsc --noEmit` / `cargo check` / 实际启动一次)
- ✅ 一次性 commit, commit message 写完整的"业务单元"

## Subject 长度

- ≤ 72 字符
- 中文可读
- 不加句号