---
description: 引用 .archive/ 或 V1.0 PRD 任何内容前必须先与用户确认 — 归档整体不是事实，仅作参考
---

# 归档仅作参考 — .archive/ 整体不是事实

## 核心规则

**V1.0 PRD (`.archive/docs/projects/v1.0/prd-v1.2.md`) 及其衍生内容 (`.archive/src/`、`.archive/docs/`、`feat/floating-auto-collapse` 分支) 是已归档的历史快照。**

**`.archive/` 任何内容都不算事实**——既不是当前项目代码，也不是当前文档。`.archive/` 文件存在、内容、设计意图、实装代码、bugfix 报告，**全部属于非事实**。

任何**非事实**内容 (设计意图、产品定位、决策取舍、计划排期、术语定义、架构选型、状态机不变量、用户画像、Sprint 计划) 在作为 Agent 行动依据之前，**必须经用户显式确认并记录为事实**，才能引用。

## 事实 vs 非事实

事实 = **当前项目状态**或**通用技术常识**。

### 事实（可作为依据）

| 类型 | 例子 |
|---|---|
| 当前项目代码事实 | 当前 `src/`、`src-tauri/src/` 里的已落地代码 |
| 当前 git 事实 | 当前分支 commit hash、`git log` 输出、当前工作树文件 |
| 当前文档事实 | 当前 `docs/` 下的 spec / plan / report / retro / CONTEXT / ADR |
| 通用技术事实 | Tauri 2 API 存在性、Windows / macOS 平台 API 名称、SQLite 特性等（与 `.archive/` 无关的通用知识）|

### 非事实（不能作为依据）

| 类型 | 例子 |
|---|---|
| **`.archive/` 任何内容** | `.archive/` 文件存在与否、文件内容、实装代码、设计意图、bugfix 报告——**全部属于非事实** |
| 设计意图 / 决策 / 术语 | 设计意图、产品定位、术语含义、状态机不变量、计划日期、用户画像、Sprint 排期、决策理由 |
| 未落地的 schema / DDL | V0.x 还未实施的 SQL DDL、字段定义 |
| 性能 / UX 推断 | "Y 比 X 快"、"用户更想要 Z" |
| 未来版本推断 | "V0.3 会怎样"、"V1.0 会怎样" |

### 边界细化（最容易踩的灰色地带）

| 表述 | 分类 | 原因 |
|---|---|---|
| "X 文件存在于 `src/`" | ✅ 事实 | 当前项目代码 |
| "X 文件存在于 `.archive/`" | ❌ **非事实** | `.archive/` 整体排除，无论文件存在还是内容 |
| "X 文件的当前内容是 Y（`src/` 下文件）" | ✅ 事实 | 当前代码事实 |
| "X 文件的历史内容是 Y（`.archive/` 下文件）" | ❌ **非事实** | `.archive/` 是历史快照，不代表当前项目 |
| "X 文件的**设计意图**是 Y" | ❌ 非事实 | 设计意图在作者脑里，文件内容是结果不是意图 |
| "X 模式适用 V0.2" | ❌ 非事实 | 适用性是推断，需用户确认 |
| "V0.3+ 会需要 X" | ❌ 非事实 | 未来推断，不可作为当下决策依据 |
| "Y 比 X 性能好一个数量级" | ❌ 非事实 | 性能推断，未实测 |
| "用户更想要 Y" | ❌ 非事实 | 用户偏好必须用户亲口说 |
| "X 组件历史上修过 Y bug（`.archive/` 报告）" | ❌ **非事实** | 即使报告存在、内容真实，`.archive/` 报告里的"修复方案值得 V0.2 复用"是设计决策 |
| "通用技术事实: Tauri 2 有 global-shortcut API" | ✅ 事实 | 跟 `.archive/` 无关的通用知识 |
| **未落地的 SQL DDL** | ❌ 非事实 | schema 是设计意图，必须经 spec 阶段用户确认 |

## 触发条件 — 出现以下任一情况必须先停下来问用户

- 引用 `.archive/docs/projects/v1.0/prd-v1.2.md` 任何内容作为产品 / 设计 / 架构依据（**包括**文件存在、内容、设计意图）
- 引用 `.archive/src/` 或 `.archive/src-tauri/src/` 任何代码作为"参考实现"或"V0.2 复用方案"
- 复用 `.archive/` 代码到当前分支时 (即使原意清楚，也应说明 "从归档复用" + 用户确认)
- 把 `.archive/` 内容描述成 "项目当前状态"、"已经做的"、"现有的"
- AGENTS.md 描述与 `.archive/` / 当前代码 冲突时，默认以 **用户当前口径** 为准 (AGENTS.md 自身可能失同步)
- **写 ADR / spec / plan 时，把 `.archive/` 任何内容（设计意图 / 文件存在 / 实装代码 / bugfix 报告）当作当前版本决策依据**（最常见违反）

## 正确做法 (引用归档前)

```text
1. 读到 .archive/ 内容
2. 分类: 这是 .archive/ 内容（默认非事实）还是当前项目 / 通用技术事实？
3. .archive/ 内容 → 默认非事实 → 必须先与用户确认
4. 用户确认 → 记到 CONTEXT.md / docs/projects/v0.2/adr/ 落地
5. 未确认 → 不引用,标记 "候选" 或 "待 V0.2 立项时确认"
6. 引用 .archive/ 时必须标注 "来源: .archive/..." + 标注 [推断]/[候选]
```

## 反模式

### 反模式 1 — 引用 V1.0 PRD 的"目标用户"作为画像依据

```text
1. 读到 .archive/docs/projects/v1.0/prd-v1.2.md 第 §2.1 节
2. 直接引用 "目标用户是 ENFP 创意人群" 作为用户画像依据
3. 写 plan / spec / docs 时把归档内容当作 "项目当前状态"
```

### 反模式 2 — 信 AGENTS.md 抄过来的数字

```text
1. AGENTS.md 写 "17 IPC 命令、18 Rust 测试"
2. Agent 看到这些数字就当真
3. 在 plan 里按这个数量去规划
```

**说明**: 这些数字是 AGENTS.md 从 `.archive/` 抄过来的快照，实际当前 develop (V0.1.6 Style Guide) 没有业务命令、0 个 Rust测试。

### 反模式 3 — 把 V1.0 设计意图当 V0.2 决策依据（2026-07-11 V0.2 ADR 违规案例）

```text
1. V0.2 写 ADR 0001
2. 写到 "task 表 5 态不变量被 timer 副作用污染"
3. 把这当 V0.2 "不复用 task.active" 的理由写进 ADR
```

**问题**: "5 态不变量"是 V1.0 PRD 的设计意图（**非事实**）；"timer 副作用污染"是推断（**非事实**）。把这两条作为 V0.2 决策依据是双层违规。

**修复**: ADR 里写 "V0.2 候选理由: timer 语义 vs task 语义不同（推断，待 V0.2 spec 阶段用户独立确认）"，不引用 `.archive/` 的设计意图做依据。

### 反模式 4 — 写未落地的 SQL DDL 当作已确认 schema

```text
1. V0.2 写 ADR 0002
2. 直接写 "CREATE TABLE timer_session (...)" 完整 DDL
3. 在 ADR 里说 "按此 schema 实施"
```

**问题**: schema 是**设计意图**（非事实），schema 字段名 / 类型 / 索引 / 约束都是设计决策，必须经 spec 阶段用户独立确认。

**修复**: SQL DDL 块标 "⚠️ 草稿，V0.2 spec 阶段会单独写 schema 设计并 grill 用户"。ADR 不绑定具体 DDL，只锁定**职责**（"单表双视图"），不锁定**结构**（具体字段）。

### 反模式 5 — 推断 `.archive/` 内容而无实测

```text
1. V0.2 写 ADR 0005
2. 写 ".archive/ 里的 db 不是生产数据，是 demo 数据"
3. 没 grep 过 .archive/ 下是否有 db 文件
```

**问题**: 这是**推断**（非事实）。`.archive/` 是归档目录，**理论上**可能含实装阶段的 db 快照；推断为"demo 数据"是 Agent 强加假设。

**修复**: ADR 里写 "未实测 .archive/ 下是否存在 db 文件。V0.2 启动时 `find .archive -name '*.db'` 验证，如有则单独评估"。

### 反模式 6 — 把 `.archive/` 文件存在标 [事实]（2026-07-11 V0.2 spec 违规案例）

```text
1. spec 写 "320×36（沿用 .archive/ 实装尺寸，事实）"
2. 或 ADR 写 ".archive/src/timeline/RecordTimeline.tsx 文件存在（事实）"
3. 把"文件存在"当事实依据
```

**问题**: `.archive/` 文件存在 ≠ 当前项目事实。`.archive/` 是历史快照，里面的文件即使存在，也不代表项目当前用这个文件 / 这个尺寸 / 这个组件。

**修复**: `.archive/` 文件存在 → **一律 [推断] 或 [候选]**，不标 [事实]。如要引用 V0.2 沿用某尺寸 / 组件，必须用户独立确认。

### 反模式 7 — 把 `.archive/` 内容当事实引用（2026-07-11 V0.2 spec 违规案例）

```text
1. spec 写 "快捷键 Ctrl+Shift+Space 沿用 .archive/src-tauri/src/lib.rs 实装组合（事实）"
2. spec 写 "V0.2 用户至少能通过 db 文件 (%APPDATA%\com.projects.app\projects.db) 用 sqlite3 查历史——这是 V0.2 范围内的'事后回看'兜底（事实）"
```

**问题**: 
- ".archive/src-tauri/src/lib.rs 写过 Ctrl+Shift+Space" 是 .archive/ 内容，非事实
- "%APPDATA%\com.projects.app\projects.db" 路径来自 V1.0 PRD §3.4.3（.archive/ 内容），不是 V0.2 已确认事实

**修复**: `.archive/` 内容引用必须标 [推断] 或 [候选]。db 路径、V0.2 沿用 .archive/ 实装的尺寸/快捷键/组件，都必须 V0.2 spec 阶段用户独立确认。

### 反模式 8 — 把 `.archive/` 报告作为"修复方案"依据（潜在违规）

```text
1. ADR 写 "SwitchDropdown 是 V1.3 iter1 fix report 的核心修复对象之一, 选 B 的'迭代风险更低'是推断"
2. 或 spec 写 "调试经验沉淀: V1.3 iter1 fix report 4 阶段"
3. 暗示 V0.2 应复用 .archive/ 报告里的修复方案
```

**问题**: 即使报告存在、内容真实，".archive/ 报告里的'修复方案值得 V0.2 复用'"是设计决策，不是事实。

**修复**: 引用 `.archive/` 报告时只标"调试经验已沉淀在 .archive/，具体复用方式待 V0.2 spec 独立评估"，不预设"应复用"。

### 反模式 9 — spec / plan 写 "沿用 X 实装" 但实施时未 grep X（2026-07-11 V0.2.0 retro）

```text
1. V0.2 spec §3.1 写 "拖拽: 沿用 V0.1.6 实装"
2. 实施时没 grep V0.1.6 sidebar.tsx 看实装是什么
3. 也没扫 .archive/docs/superpowers/plans/2026-06-20-commit-hygiene.md line 1065
   "FloatShell V1.5 漏挂 .floating-root 类"
4. commit 时 cargo check + tsc 通过就 push
5. 用户 Windows 实测发现"无法拖动" → 回修 commit 8872906
```

**问题**: "沿用 X 实装"措辞**没经 grep 验证**，与 X 实际状态脱节。V0.2.0 重蹈 5 处 V1.0/V1.5 时代已修 bug（详见 V0.2 retro `docs/reports/v0.2.0-retrospective.md` §1）。

**修复**: 任何 spec/plan/ADR 写"沿用 X 实装"前**必须**:
1. `git log --oneline --all -- src/<X 实际位置>` 看 X 当前代码
2. `grep -rnE "<X 关键属性>" src/` 看 X 关键字段实际值
3. 扫 `.archive/docs/{superpowers,architecture,reports}/` 的失败模式 + 调试报告
4. commit message 验证段写 "已 grep V0.1.6 sidebar.tsx X=Y; 沿用此模式"
5. 不允许 "spec 写 X 沿用 Y 但实施时跳过 grep" 的链路出现

**V0.2.0 重蹈 5 处**（详见 retro §1）:
- #1 `.floating-root` 类漏挂 → commit `5cf9c96` 修
- #2 `floating.css` 只有占位 → commit `5cf9c96` 修
- #3 拖动无声明 → commit `8872906` 修
- #4 `app_exit` 未接线前端 contextmenu → commit `a95fe73` 修
- #5 UI 组件零 vitest → commit `89689d7` 修

## 跟其他规则关系

| 规则 | 关系 |
|---|---|
| `first-step-research.mdc` | 该规则说 "调研优先级 `.archive/src/` > docs"。本规则说 "查到后必须验证事实性"。**互补**: 先查，再验证 |
| `decision-method.mdc` | L2 (项目设计) 引用时，必须先确认 "这个设计是当前版本 (V0.x) 立项时确认的，不是从 .archive/ 抄过来的" |
| `dev-verify-before-commit.mdc` | 同源: 该规则说 "症状消失 ≠ 修复成功"。本规则说 "归档内容存在 ≠ 归档内容是事实"。反向证据不等于有效证据 |
| `self-apply-after-write.mdc` | 本规则本身就是新规则，写完必须跑 7 项 self-apply 检验 (见下) |

## Self-Apply 7 项检验

| # | 检验项 | 本规则 |
|---|---|---|
| 1 | 关键路径列全了吗? | `.archive/src/` `.archive/src-tauri/src/` `.archive/docs/` `.archive/docs/projects/v1.0/` AGENTS.md `feat/floating-auto-collapse` worktree |
| 2 | 有具体执行步骤吗? | "读到 .archive/ → 默认非事实 → 必须用户确认 → 落到 CONTEXT.md / ADR → 才能引用" |
| 3 | 有触发条件吗? | 见 "触发条件" 节 (6 条) |
| 4 | 有反模式例子吗? | 见 "反模式" 节 (8 个，含 2026-07-11 V0.2 ADR/spec 真实违规案例 3 个) |
| 5 | 跟其他规则交叉引用了吗? | 见 "跟其他规则关系" 节 (4 条) |
| 6 | 有验证机制吗? | "每次引用 .archive/ 做依据前问: 这是当前项目事实吗? 用户独立确认过吗? 没确认就停" |
| 7 | 当下能说 "下次出现 X，我会自动做 Y" 吗? | "下次读到 .archive/ 内容做依据，默认非事实，先问用户。AGENTS.md 描述与 .archive/ 冲突时，以用户当前口径为准" |

### Self-Apply 反向检验 — 写完规则后回头扫自己写的内容

**应用本规则时**（写 ADR / spec / plan / CONTEXT.md 时），每写完一段，回过头扫一遍本规则的事实/非事实表 + 反模式列表，确保**没有违反自己沉淀的规则**。

**反例 1**（2026-07-11 早段）：写完 5 份 ADR 后，本应立刻回头扫自己写的 ADR 是否违反了刚写完的 archive-reference-only.mdc，但**没扫**——直到用户明确指出才修正。

**反例 2**（2026-07-11 晚段）：修正完 ADR 后写 spec，又在 spec 里把 ".archive/ 文件存在" / ".archive/ 实装尺寸" 标了 [事实]——重复犯同类错。**再次**被用户指出后才修正。

**修复**: 写完任意 ADR / spec / plan 后，**先**对自己写的内容跑一次本规则的"事实 vs 非事实"分类，特别扫三个高频违规点:
1. ".archive/ 文件存在" 是否被标 [事实]？→ 应改 [推断]/[候选]
2. ".archive/ 实装尺寸 / 快捷键 / 组件" 是否被标 [事实]？→ 应改 [推断]/[候选]
3. ".archive/ 报告 / 文件内容" 是否被当 V0.2 决策依据？→ 应标 [推断] 或移除

## 关联

- `docs/reports/v0.1.6-retrospective.md` — V0.1.6 retro 沉淀的 "反向症状消失 ≠ 修复生效" 教训 (本规则同源: 归档内容存在 ≠ 归档内容是事实)
- `.archive/docs/projects/v1.0/prd-v1.2.md` — V1.0 时代产品宪法，已归档
- `docs/projects/v0.2/README.md` — V0.2 立项文档，本规则首次应用场景
- `AGENTS.md` § "项目" 段当前描述的是 `.archive/` V1.0 状态，与 develop 失同步 (本规则适用)
- `docs/specs/2026-07-11-v0.2.0-floating-window-design.md` — V0.2 spec，含本规则违规案例（反模式 6/7/8 的来源）

## 首次应用案例

V0.2 立项 (2026-07-11) 时，README 草稿 + 8 份 ADR + V0.2.0 spec 引用了 `.archive/` 的多项内容。按本规则:
- **历史遗留违规**: 早期 ADR/spec 把 `.archive/` 文件存在、实装尺寸、组件引用标 [事实]，违反本规则。已逐项修正（标 [推断]/[候选]）。
- **未来约束**: V0.2.1 / V0.2.2 / V0.3 立项时，**默认不引用 `.archive/`**，如需引用必须用户独立确认后落到 CONTEXT.md / ADR / spec。