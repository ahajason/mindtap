---
description: 写完 rule / memory / CLAUDE.md 后,必跑 7 项 self-apply 检验 — 规则漏路径 / 缺步骤 = 规则无效
---

# 写完规则后必须 self-apply

## 反模式

```text
1. 写规则 X
2. 下次类似场景
3. 不 apply 规则 X,继续犯同款错
4. 用户再提示才 apply
5. 用户问"为什么需要我提示"
```

**沉淀规则 ≠ 规则生效**。规则要 trigger 实际行为改变才算写对。

## self-apply 检验清单(写完规则后 30 秒必做)

| # | 问题 | 不通过时 |
|---|---|---|
| 1 | 列了**所有关键路径**吗? | 漏路径 = 规则无效(典型:只列 .archive/docs/ 漏 .archive/src/) |
| 2 | 给了**具体执行步骤**吗?(不是"应该考虑"是"必做 X 步") | 抽象 → 改 step-by-step |
| 3 | 有**触发条件**吗?(什么时候必跑) | 加"何时用"段 |
| 4 | 有**反模式例子**吗?(之前怎么犯的) | 加"反模式"段 |
| 5 | 跟其他规则**交叉引用**了吗? | 加"## 跟其他规则关系"段 |
| 6 | 有**验证机制**吗?(怎么知道 apply 了) | 加 checklist |
| 7 | **当下**能说出"下次出现 X,我会自动做 Y"吗? | 说不出来 = 规则不够具体,继续改 |

## 写规则 vs apply 规则(双硬 gate)

| 维度 | 写规则 | apply 规则 |
|---|---|---|
| 何时 | 每次沉淀新 rule/memory 时 | 每次类似场景触发时 |
| 怎么 verify | self-apply 检验清单 7 项 | 反问"如果现在碰到 X,我会不会自动按这条做?" |
| 不通过的后果 | 沉淀无效 | 沉淀了但行为没变 |

## 双重错误案例

### 错误 1 — 规则漏写关键路径

某调研规则只列 `.archive/docs/`,**漏 `.archive/src/`(完整历史 codebase)**。规则从来没触发去 grep `.archive/src/`,错过已踩坑 + 已修过的实现。

**修复**:补路径 + 写明"不只是 docs,含 components/hooks/lib 完整 codebase"。

### 错误 2 — 规则没 self-apply

retro 时口头说"应该查 `.archive/`",但**没真正派 subagent 系统扫**,只人工 grep 一两个文件。沉淀的 lesson 没真正执行。

**修复**:retro 流程加"派 subagent 系统扫"为 hard step,不派不算完。

## 何时用

| 场景 | 用 |
|---|---|
| 写完任何 rule / memory / CLAUDE.md 后 | ✅ 必跑 7 项 |
| 接到 bug 反馈准备动手前 | ✅ 反问"哪些 rule 适用?我会 apply 吗?" |
| 跑 /retro 收口时 | ✅ 沉淀新 rule 后跑 7 项检验 |
