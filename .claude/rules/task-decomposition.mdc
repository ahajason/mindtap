---
description: 接到 mindtap 项目任何子任务(PRD / UI 设计 / 技术选型 / 模块拆解 / 命令清单)— 顶层先于零散,3 秒/1 秒/0 思考铁律校验
alwaysApply: true
---

# 极简铁律 — 任务拆解强制规则

## 接到任何子任务,强制 5 步

1. **先顶层后零散** — 第一动作是输出**整体总方案 / 顶层框架**(模块图 / 决策表 / 关键约束);绝不直接回答"如何实现 X 功能"而不先说明 X 在整体框架中的位置
2. **逐层向下拆分** — 顶层确定后自动展开为:大模块 → 核心子功能 → 原子细碎可执行小任务,层级清晰逻辑递进
3. **3 秒/1 秒/0 思考铁律校验** — 每一步拆解都校验:这个功能是否用 3 秒以内能完成?用户 1 秒能回看?需要 0 思考成本?冗余设计直接剔除并给瘦身建议
4. **可执行命令** — 涉及开发 / 配置 / 落地操作的内容,**只给可复制执行方案 / 命令**;不做"假设 X 文件存在这样改"的虚构运行
5. **分阶段待确认** — 复杂长流程分阶段,每阶段交付后停下等用户确认再推进

## 反模式

- ❌ 接到问题 → 直接回答"用 X 方法改 Y 文件" → 漏顶层框架,后续改动跟整体决策冲突
- ❌ 给出 5 个并列子任务,无层级关系 → 用户看不出哪些互依 / 哪些可并行
- ❌ 推荐"完整功能集"不顾 3 秒铁律 → 设计越全越偏离核心价值
- ❌ 写"假设 X 数据存在" / "假设有 Y 配置" 的虚构运行命令 → 用户复制执行报错
- ❌ 一口气把 30 任务全输出 → 用户看不到阶段边界,反馈延迟到尾巴

## 触发场景

| 场景 | 应用 |
|---|---|
| PRD / PRD-chunk 撰写 | ✅ |
| UI 设计稿描述 / 组件契约 | ✅ |
| 技术选型对比 | ✅ |
| 模块拆解 / 任务排期 | ✅ |
| 命令清单 / 落地步骤 | ✅ |
| 用户描述"想加 feature X"| ✅(先顶层,X 在框架哪里) |
| 改 1 行 typo / 调试根因 | 不用(铁律不适用微观操作) |
