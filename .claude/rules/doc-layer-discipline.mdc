---
description: 文档结构纪律 — 新功能/新 bug 必须按「大需求目录 + index.md」结构走完才能开 task,缺 index 不开
globs: ["docs/**/*.md"]
---

# 文档结构纪律(Doc Structure Discipline)

> 强制执行入口:任何新功能、新 bug、新 doc 都必须先读 `docs/governance/doc-layers.md`,按「按大需求分目录」结构走。

## 触发条件

以下任一动作前**必读**本规则:

1. 创建 `docs/` 下任何新文件
2. 接到"做新 feature"任务
3. 接到"修 bug"任务
4. 发版前(`chore(release):` commit 前)
5. 评审需求 / 设计文档

## 强制流程

### 新功能

```
1. 主文档补需求(如有新场景)
2. 开 docs/<编号>-<需求>/ 目录(或复用已有需求目录)
3. 写 domain.md(如新实体/状态) + design.md(如有 UI)
4. 写 index.md(唯一入口,列出目录内文档)
5. 开 docs/tasks/<name>/task.md(含「范围边界」段)
```

**缺 index.md 不开 task**。

### 新 bug

```
1. 读对应任务 task.md 「范围边界」段
2. 在范围内 → 开 docs/tasks/<version>.<N+1>-fix-<topic>/task.md
3. 不在范围内 → 不归本任务,另开 task
```

## 关键词自检(写到文档后必跑)

```bash
# domain.md(业务层,禁技术名词)
grep -iE "INTEGER|TEXT|partial unique|invoke|Mutex|src-tauri|tauri-bridge|SQL " docs/<需求>/domain.md
# 命中即违规

# design.md(视觉层,禁表/IPC)
grep -iE "INTEGER|TEXT|partial unique|invoke|SQL |src-tauri/" docs/<需求>/design.md
# 命中即违规

# task.md(禁技术实现细节)
grep -E "接口契约|表结构|模块边界" docs/tasks/<name>/task.md
# 命中即违规
```

任一命中 → 必须改完才能 commit。

## 违规处理

| 违规等级 | 处理 |
|---|---|
| 需求目录缺 index.md | 不开 task,先补 index.md |
| domain.md 出现技术名词 | 删除技术段落,只留业务内容 |
| design.md 出现 IPC 命令 | 删掉,只留视觉/交互 |
| 文档内互相引用路径 | 改为语义描述,引用收敛到 index.md |
| 写入已废弃路径(prd/domain/tech/reports) | 移到需求目录或归档 |

## 关联

- `docs/governance/doc-layers.md` — 总规则
- `docs/governance/versioning-rule.md` — bug 归属 + PATCH 命名
- `docs/governance/l3-gating.md` — L3 实测硬约束
- `.claude/rules/task-directory.mdc` — task slug 命名

## 历史

- 创建: 2026-07-13
- 更新: 2026-08-02 — 从 6 层改为大需求目录结构
