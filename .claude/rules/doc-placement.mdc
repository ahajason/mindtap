---
description: 新功能 / 新 bug / 任何 docs/ 内文档归位 — 写 docs/ 任何文件前必查(权威细则见 docs/governance/doc-layers.md)
alwaysApply: true
---

# 文档归位

> 强制: 写任何 `docs/**` 下文件前先确认归位。结构为「按大需求分目录」——每个大需求一个目录,index.md 唯一入口,文档内不互相引用。

## 归位表

| 内容类型 | 去处 | 关键约束 |
|---|---|---|
| 产品需求 / 用户故事 / 验收 | `docs/轻念Mindtap产品需求文档.md`(唯一真值) | 纯业务,无技术名词 |
| 业务实体 / 状态机 / ADR | 对应需求目录 `domain.md` / `adr/` | 无表名 / 字段 / IPC 命令 |
| 视觉 / 交互 / 组件契约 | 对应需求目录 `design.md` | 无表 / IPC / 模块路径 |
| 范围边界 / bug 归属 | task.md「范围边界」段 | 技术实现细节入单元测试 |
| 共享视觉规范(token) | `docs/design-system/` | 跨需求引用 |
| 实施步骤 / commit 计划 | `docs/plans/YYYY-MM-DD-<version>-<feature>.md` | 不写业务规则 |
| 进行中 task | `docs/tasks/<version>-<type>-<short-desc>/task.md` | 入口固定 task.md |
| 跨版本规则 | `docs/governance/` | 治理横向贯穿 |
| 历史交付 | `docs/archive/` | 只读,参考 |

## 需求目录命名

`<编号>-<单词>`(如 `L-1-workbench`)。编号前缀区分通用目录;单词表达业务语义。目录内必须含 `index.md`(唯一入口,唯一允许引用本目录文件的文档)。

## 已弃用旧路径(不要再写)

- ❌ `docs/prd/` `docs/domain/` `docs/design/` — 并入各需求目录
- ❌ `docs/tech/` — 已移除,技术入测试,范围边界入 task.md
- ❌ `docs/reports/` — 归档到 `archive/history/reports/`
- ❌ `docs/specs/` `docs/architecture/` `docs/projects/` `docs/research/` `docs/references/` `docs/superpowers/` — 已归档或删除

## 严禁出现

需求目录 `domain.md` / `design.md` / task.md 写完后 grep 自查,命中必须改:

```bash
# domain.md(业务层)
grep -iE "INTEGER|TEXT|partial unique|invoke|Mutex|src-tauri|tauri-bridge|SQL " docs/<需求>/domain.md

# design.md(视觉层)
grep -iE "INTEGER|TEXT|partial unique|invoke|SQL |src-tauri/" docs/<需求>/design.md

# task.md(技术实现不写文档)
grep -E "接口契约|表结构|模块边界" docs/tasks/<name>/task.md
```

## 不互相引用

除 index.md 外,需求目录内文档**不互相贴路径链接**。引用共享规范(design-system)以语义描述表达(如"玻璃参数见设计系统规范"),不写路径。

## 触发场景

| 动作 | 应用 |
|---|---|
| 新建需求目录 / 需求文档 | ✅(index.md + domain.md + design.md) |
| 开新 task | ✅(tasks/<name>/task.md 含范围边界段) |
| 写 release notes / retro | ✅(archive/history/reports/ 或 governance) |
| 改 docs/governance/ | ✅(doc-layers.md) |
