---
description: 杂项硬约束 — 设计语言 / 提问阈值 / 任务收尾 / 规则自检 / Tauri 多窗口坑
alwaysApply: true
---

# Misc — 杂项硬约束

## 设计语言(src/** 触发)

所有 UI 改动参考 `docs/design-system/glassic-ui-spec.md`(Liquid Glass spec)。**不自创颜色/阴影/模糊 token**,查 spec 拿现有值;缺值先扩 spec。

- 组件落地: shadcn 源码拷 + Tailwind token + 原生 primitive
- ❌ 自创 token / inline 覆盖 shadcn / 玻璃表面放内容(Liquid Glass 铁律: 玻璃只承载控件/导航,内容落非玻璃背景)

## 何时用 AskUserQuestion

**用**: 2-4 个离散互斥选项 + 用户在等挑 / 跨多文件改动实施前 / 不可逆操作 / 用户明确要方案。

**不用**: 用户在解释问题(等说完)/ 答案在 spec/rule/git history(先查)/ 选项 > 4(文本对话)/ 1 个明显正确的选项(直接做)。

**反模式**: 用户问"为什么"就弹选项 / 问题没听完就弹方案 / 5+ 选项 / description 太长 / 把弹窗当默认(文本对话也是合法工具)。

## 任务收尾 + Worktree 清理(交付后必走)

任何交付完成时**立即主动做,不待用户提醒**:

1. **任务状态更新**: 实质交付完成 → `TaskUpdate {completed}`;新工作开始前 `TaskList` 清 stale task
2. **Worktree 三件套**(push + merge 回 develop 后):
   ```bash
   git worktree remove --force --force <worktree路径>
   git branch -d <branch-name>
   git push origin --delete <branch-name>
   ```

**反模式**: 等用户说"关 worktree"才关 / task 做完不标 completed / push 后留远端 branch。

## 规则自检(改完规则后)

每次写完/改完规则,自检:
- 关键路径列全了吗? 反模式例子给了吗? 触发条件设了吗?
- 能给一句"下次 X 自动 Y"吗?

## Tauri 多窗口硬坑(src/floating/** 触发)

1. **入口 HTML 放项目根**(跟 index.html 平级),不放 src/——否则 `dist/src/floating.html` 与 tauri.conf 的 `floating.html` 不匹配
2. **组件引用父目录 CSS 用 `../styles/xxx.css`**
3. **浮动窗不能单押 ResizeObserver**: 状态切换用显式 setSize 为主,ResizeObserver 只负责运行时自适应;手动拖拽用 `manualResizingRef` 阻断反馈循环
4. **resizable:false 必须自己加拖拽手柄**,否则用户物理上无可抓物
5. **max bounds 双处同步**: `tauri.conf.json` + Rust 模块 `max_inner_size()` 一致
