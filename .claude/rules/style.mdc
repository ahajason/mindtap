---
description: 写 git commit message 前 / 加代码注释前必读 — commit 格式 + 注释风格
alwaysApply: true
---

# Style — Commit + Comment

## Commit 格式

```
<type>(<scope>): <subject>

<why 段 — 只写"为什么"不重复"做了什么">
- 业务痛点
- 边界条件
- 不这样改会怎样
```

| type | 用法 |
|---|---|
| feat | 新功能 / 新行为 |
| fix | 修 bug |
| refactor | 重构(不改行为) |
| docs | 纯文档 |
| test | 纯测试 |
| chore | 工具 / 依赖 / 配置 |
| revert | 撤销 |

- **scope**: 业务名(floating / db / tray 等),跨多域用最核心的
- **粒度**: 1 个任务 1-3 个 commit(小 1 / 中 2 / 大 3);不拆太散也不聚合过度
- **不引 footer**: 不用 `Refs:` / `Closes:` 等(反向追溯用 git log)
- **提交时机**: 业务单元完整收口才 commit(代码 + 规则 + 文档全部到位),不是写一行就 commit

## Comment 风格

**代码即注释**: 好代码本身可读,注释只写**为什么**,不写**做了什么**。

| 写 | 不写 |
|---|---|
| 为什么这样做(业务背景 / 决策原因 / 边界条件) | 这段代码在做什么(代码自己说话) |
| 公开 API 的契约(参数约束 / 返回值语义) | 函数名 / 参数名显而易见的解释 |
| 跨模块的非显式依赖 | "初始化函数" / "主逻辑" 这种废话 |
| 暂时绕过某限制的原因(含背景) | 行尾复述代码 |

**反模式**:
- ❌ Narration 注释(复述代码: `// 循环遍历数组`)
- ❌ Stale 标签(不维护的 `// TODO: 优化性能`)
- ❌ 元描述(`// 这个文件处理登录`)
- ❌ 历史注释(`// 2024-01-01 jason 修复了...`)
- ❌ 废话段头注释(每函数加 banner)

**自检**: 删注释看是否还能读懂——能读懂就删。留下的注释必须: 删了会丢失信息 / 不是代码复述 / 不会过时。3 条任一不满足就删。
