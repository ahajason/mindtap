---
description: 任何代码/文档改动前与交付后必走 — 决策方法 + 调研 + 根因倒推 + 任务拆解
alwaysApply: true
---

# Workflow — 决策 / 调研 / 根因 / 拆解

## 改前:三层决策法(必走)

任何改动前对照三层检查:

| 层 | 内容 | 来源 |
|---|---|---|
| **L1 原始权威** | Apple HIG / WCAG / MDN / Tauri 官方文档 / 库官方 API 文档 | WebFetch / context7 |
| **L2 统一设计** | `docs/design-system/glassic-ui-spec.md` / 已立 convention / 项目 rule | `docs/design-system/` + `.claude/rules/` |
| **L3 具体问题** | 当前 issue / bug / 需求 + **所有 user-facing 副作用** | 用户描述 + **主动枚举**(不只是用户说的) |

**任一层缺失 = 停下来补查再动手**。

## 改后:闭环复盘(必做)

1. 对照当初 L3 列表 vs 实际行为 — 哪些副作用没识别?
2. 调用 `/retro` 走 4 段模板(决策 / 未识别 / 暴露 / 沉淀)
3. 可推广教训 → memory(未来类似场景怎么防)

## 接到任务:先顶层后零散(必走)

1. **先顶层后零散** — 第一动作输出整体方案 / 顶层框架(模块图 / 决策表 / 关键约束),不直接答"如何实现 X"
2. **逐层向下拆分** — 大模块 → 核心子功能 → 原子小任务,层级递进
3. **3 秒/1 秒/0 思考校验** — 每步拆解校验:3 秒能完成?1 秒能回看?0 思考成本?冗余剔除
4. **可执行命令** — 开发/配置/落地只给可复制命令,不做"假设 X 存在"的虚构运行
5. **分阶段待确认** — 复杂长流程分阶段,每阶段交付后停下等确认

## 接到 bug:双层调研 + 根因倒推

### 双层调研(先调研再推断)

```
1. 建任务档占位(问题描述 + 已知猜测 + 待验证项 + Attempt Log)
2. git log --grep + git show 客观事实
3. 扫项目内部历史(retro / release notes / commit 验证段)
4. 派 1-3 个 subagent 并行外部 search(官方文档原文 + 社区对照)
5. 整合 + 推断,append 到 Attempt Log
```

**项目内部历史 > web search**。

### 根因倒推四层法(改 3 次仍 fail 必走)

| 层 | 问的是什么 | 失败模式 |
|---|---|---|
| **L1 代码** | 哪一行/哪个变量/哪条控制流错了 | typo / off-by-one / 空指针 |
| **L2 方法** | API / IPC / hook / callback 链对吗 | API 用错 / IPC race / 时序错 |
| **L3 方案** | 整体架构 / 模块拆分选对了吗 | 同步vs异步 / 库选错 |
| **L4 需求** | 我解决的是 user 真实需求吗 | 替 user 想象 / 走偏 |

**触发条件**(任一即启动倒推):同处代码改 3 次仍 fail / root cause 解释用"可能应该也许" / 解释需 5+ 句 / 验证只覆盖代码层不覆盖行为层 / user 反馈"不对"但 root cause 没变 / 加兜底值"smoke 过"commit。

**纪律**:每层尝试必须有证据(代码/log/调试输出);每层切换必须写明"放弃 Ln 因为 X";不要在同一层反复试;不要跳层;不要回退到更具体层。

## 反模式

- ❌ "用户说改什么就改什么" — 缺 L3 主动枚举
- ❌ "smoke 过了 = 验证完了" — 缺行为层验证
- ❌ "交付完不写 retro" — 缺闭环
- ❌ 接到问题直接答"用 X 改 Y" — 漏顶层框架
- ❌ 同一层改 3 次不换层 — 钻牛角尖
