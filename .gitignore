# 用户本地配置（不进 git）
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Working memory(本地 planning files,不进 git)
task_plan.md
findings.md
progress.md

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node
node_modules/
dist/
dist-ssr/
.vite/
*.local

# Rust / Tauri
src-tauri/target/
src-tauri/Cargo.lock

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp
*.swo
*.sw?
*~

# 环境变量
.env
.env.local

# WSL 元数据（如果项目被软链到 Windows）
.wslconfig

# Claude Code 插件运行时数据(2026-06-21 起已归档到 .archive/.superpowers/)
.superpowers/

# Git worktrees（隔离开发工作区）
.worktrees/

# 工具运行时数据(不入仓)
.codegraph/

# IDE 个人配置(不入仓)
.cursor/
.zed/

# Playwright MCP 临时产物(不入仓)
.playwright-mcp/

# Playwright 运行产物(不入仓)
test-results/
playwright-report/

# 上游代码快照(参考用,不编辑、不入仓;分析文档 `_analysis.md` 仍跟踪)
# 详见 docs/references/README.md
docs/references/source/*/

# 归档目录(2026-06-21 起);还原: git checkout pre-archive-2026-06-21 -- .
.archive/
