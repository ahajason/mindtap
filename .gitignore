# 用户本地配置（不进 git）
.claude/settings.local.json
.claude/scheduled_tasks.lock

# worktree（每个开发者本地路径不同，不进 git）
.claude/worktrees/

# 隐私文件 / key 过滤（防止误 commit API key / token / 密码）
.env
.env.*
*.env
**/.env
**/.env.*
**/*.key
**/*.pem
**/credentials*
**/secrets*
**/*secret*
**/id_rsa*
**/id_ed25519*

# Working memory(本地 planning files,不进 git)
task_plan.md
findings.md
progress.md

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node
node_modules/
dist/
dist-ssr/
.vite/
*.local

# Rust / Tauri
src-tauri/target/
src-tauri/Cargo.lock

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp
*.swo
*.sw?
*~

# 环境变量
.env
.env.local

# WSL 元数据（如果项目被软链到 Windows）
.wslconfig
workspace.lnk

# Claude Code 插件运行时数据(2026-06-21 起已归档到 .archive/.superpowers/)
.superpowers/

# Git worktrees（隔离开发工作区）
.worktrees/

# planning-with-files 中间态(本地规划,不入仓)
.planning/

# OpenCode(已卸载,防重现时再次跟踪)
.opencode/

# throwaway 原型(选完即弃,不留 git)
prototype/

# IDE 个人配置(不入仓)
.cursor/
.zed/

# Playwright MCP 临时产物(不入仓)
.playwright-mcp/

# Playwright 运行产物(不入仓)
test-results/
playwright-report/
